---
phase: 02-hero-intelligence
plan: 07
type: execute
wave: 4
depends_on: [02-03, 02-06]
files_modified:
  - .planning/phases/02-hero-intelligence/screenshots/hero-1440.png
  - .planning/phases/02-hero-intelligence/screenshots/hero-1024.png
  - .planning/phases/02-hero-intelligence/screenshots/hero-390.png
  - .planning/phases/02-hero-intelligence/screenshots/intelligence-1440.png
  - .planning/phases/02-hero-intelligence/screenshots/intelligence-1024.png
  - .planning/phases/02-hero-intelligence/screenshots/intelligence-390.png
  - .planning/phases/02-hero-intelligence/screenshots/modal-market-1440.png
  - .planning/phases/02-hero-intelligence/screenshots/modal-customer-1440.png
  - .planning/phases/02-hero-intelligence/screenshots/modal-brand-1440.png
  - .planning/phases/02-hero-intelligence/screenshots/modal-390.png
autonomous: false
requirements: [HOME-03, HOME-04, HOME-09]
user_setup: []

must_haves:
  truths:
    - "Desktop (1440), tablet (1024), and mobile (390) screenshots of the hero and intelligence section exist and are compared against the reference set, with major visual differences corrected or honestly listed"
    - "The detail modal is screenshotted for market, customer, and brand at 1440 and once at 390 (full/near-full), and compared against the corresponding card references"
    - "No horizontal overflow at any of the three widths; the browser console shows no errors on the homepage"
  artifacts:
    - path: ".planning/phases/02-hero-intelligence/screenshots/"
      provides: "Captured hero, intelligence, and modal screenshots at 1440/1024/390 for the human visual gate"
  key_links:
    - from: "captured screenshots"
      to: "docs/qiera-reference/Homepage/*"
      via: "side-by-side comparison per qiera-visual-fidelity.mdc"
      pattern: "qiera-reference"
---

<objective>
Run the mandatory Phase 2 visual-fidelity gate: start the dev server, capture hero / intelligence / modal screenshots at 1440, 1024, and 390, compare them against the reference set, correct major visible differences, then present the results for human sign-off (the acceptance-criteria arbiter for HOME-03/04/09 fidelity).

Purpose: `qiera-visual-fidelity.mdc` forbids claiming pixel fidelity without screenshot comparison, and the ROADMAP requires screenshots at 1440/1024/390. This is the one plan in Phase 2 with a blocking human checkpoint; keeping it separate leaves 02-01…02-06 fully autonomous.

Output: screenshots under `.planning/phases/02-hero-intelligence/screenshots/` + a human-verified sign-off.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.cursor/rules/qiera-visual-fidelity.mdc
@.planning/DECISIONS.md

Reference targets (UI-SPEC "Screenshot Verification" table):
  Hero              -> docs/qiera-reference/Homepage/top section.png (+ c3448e9e close-up)
  Intelligence grid -> docs/qiera-reference/Homepage/Section 2.png
  Modal (market)    -> docs/qiera-reference/Homepage/Intelligence cards/market intelligence card.png
  Modal (customer)  -> docs/qiera-reference/Homepage/Intelligence cards/customer intelligence card.png
  Modal (brand)     -> docs/qiera-reference/Homepage/Intelligence cards/brand intelligence card.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture and self-compare Phase 2 screenshots; correct major diffs</name>
  <files>.planning/phases/02-hero-intelligence/screenshots/</files>
  <read_first>
    @.cursor/rules/qiera-visual-fidelity.mdc  (Verification section — run app, capture, compare, correct; visual implementation priority order)
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Responsive Behavior table + Screenshot Verification table + all [ASSUMED] values to correct: hero headline sizes, split ratio, vertical padding, scroll-margin-top, grid column counts, modal max-width, card active-bg)
    @src/components/sections/Hero.tsx  (to correct [ASSUMED] hero geometry)
    @src/components/sections/IntelligenceSection.tsx  (to correct [ASSUMED] carousel/grid values)
  </read_first>
  <action>
    Ensure `public/images/qiera/operating-model.png` exists (from 02-01). Start the Next dev server (`npm run dev`) — if a dev server is already running per the terminals folder, reuse it rather than starting a duplicate. Using a headless browser tool (e.g. Playwright/Puppeteer via `npx` if available, or the project's existing screenshot mechanism used in Phase 1 — check the Phase 1 `screenshots/` approach), navigate to the homepage and capture, saving into `.planning/phases/02-hero-intelligence/screenshots/`: hero at 1440/1024/390 (`hero-<w>.png`), intelligence section at 1440/1024/390 (`intelligence-<w>.png`), and the detail modal opened for MARKET, CUSTOMER, and BRAND at 1440 (`modal-<id>-1440.png`) plus the modal at 390 for one dimension (`modal-390.png`, verifying full/near-full + border-radius 0). Then COMPARE each capture against its reference target (table above), walking the visual implementation priority order from `qiera-visual-fidelity.mdc` (page width → section height → grid → alignment → dimensions → typography → spacing → borders/radii → color/gradients → shadows). CORRECT major differences in the corresponding component/tokens by adjusting the [ASSUMED] values (hero headline px, split ratio, section vertical padding, scroll-margin-top, grid/carousel column counts, modal max-width, active-card background) — do not chase sub-pixel polish while major proportions are off. Also verify NO horizontal overflow at any width (temporarily set `overflow-x: hidden` off and check for runaway elements / a horizontal scrollbar) and check the browser console for errors on load. Re-capture after corrections. Produce an honest written diff list (what now matches, what remains off and why) for the checkpoint.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),d='.planning/phases/02-hero-intelligence/screenshots';const req=['hero-1440.png','hero-1024.png','hero-390.png','intelligence-1440.png','intelligence-1024.png','intelligence-390.png','modal-market-1440.png','modal-customer-1440.png','modal-brand-1440.png','modal-390.png'];const missing=req.filter(f=>!fs.existsSync(d+'/'+f)||fs.statSync(d+'/'+f).size===0);if(missing.length){console.error('missing/empty:',missing);process.exit(1)}console.log('all screenshots present')"</automated>
  </verify>
  <acceptance_criteria>
    - All ten screenshots listed in `files_modified` exist and are non-empty.
    - Each capture has been compared to its reference target following the priority order; major differences corrected in code/tokens (with [ASSUMED] values resolved), and a written honest diff list produced.
    - No horizontal overflow at 1440/1024/390; no browser console errors on the homepage.
    - `npx vitest run`, `npx tsc --noEmit`, and `npm run lint` all still pass after corrections.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual fidelity sign-off (hero + intelligence + modal)</name>
  <what-built>
    The complete Phase 2 hero + intelligence section: dark split hero with the operating-model graphic, the 12-card intelligence carousel (mouse/keyboard/touch + counter + dots), and the shared accessible detail modal — screenshotted at 1440/1024/390 and self-compared against the reference set, with major differences corrected.
  </what-built>
  <how-to-verify>
    1. Start the site if not running: `npm run dev`, open http://localhost:3000.
    2. HERO (compare to `docs/qiera-reference/Homepage/top section.png`): confirm the badge, two-tone headline ("measurable growth." in purple), body copy, the Start Intelligence Audit button, and the operating-model graphic on the right; resize to 1024 and 390 and confirm it stacks cleanly with no horizontal scrollbar.
    3. INTELLIGENCE (compare to `Section 2.png`): confirm the eyebrow "1. OUR UNIQUE INSIGHTS", heading, subheading, all 12 cards with correct icons + 3 bullets each, and the counter/arrows. Click Next/Prev (counter changes), press Left/Right arrow keys on the carousel, and on a narrow window swipe/scroll the cards.
    4. MODAL (compare to the market/customer/brand card references): click "Learn more" on Market Intelligence — confirm the two-panel layout (left: title + WHAT IT DELIVERS + evidence note; right: KEY QUESTIONS + WHAT WE ANALYZE + STRATEGIC BENEFITS). Press Tab (focus stays inside), press Escape (closes), and confirm focus returns to the "Learn more" button you clicked. Repeat the open for Customer and Brand. At 390px confirm the modal is full/near-full screen with no rounded corners.
    5. Confirm the browser console shows no errors.
    6. Review the written diff list in the plan output and the captured screenshots under `.planning/phases/02-hero-intelligence/screenshots/`.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 2, or list the specific visual/interaction differences to fix (a gap-closure plan will address them).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server ↔ headless browser | Screenshots are captured from a local dev instance; no production/user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Information disclosure | screenshots committed under .planning/ | accept | Screenshots contain only static marketing content already in the public references; no secrets/PII. (Also: `commit_docs: false` — the user commits manually.) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | If a headless-browser tool must be invoked, prefer the Phase 1 mechanism / an already-present tool; do not add a dependency to `package.json`. If none exists, capture via an on-demand `npx` invocation without persisting a new dependency, and note it in the SUMMARY. No `[SLOP]`/`[SUS]` packages. |
</threat_model>

<verification>
- All ten screenshots present and non-empty under the phase `screenshots/` directory.
- `npx vitest run` green; `npx tsc --noEmit` clean; `npm run lint` clean after any corrections.
- Human checkpoint returns "approved".
</verification>

<success_criteria>
Phase 2 is visually verified: hero, intelligence carousel, and detail modal captured and compared at 1440/1024/390, major differences corrected, no overflow or console errors, and the human reviewer has approved (or itemized fixes for a gap-closure pass).
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-07-SUMMARY.md` when done. Embed/reference the screenshots, record the honest remaining-diff list, and note the human sign-off decision. Do NOT git commit (`commit_docs: false`).
</output>
