---
phase: 02-hero-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/intelligence.ts
  - src/data/intelligence.ts
autonomous: true
requirements: [HOME-04]
user_setup: []

must_haves:
  truths:
    - "A typed IntelligenceDimension contract exists so the modal and cards render any dimension without switch/case logic"
    - "All 12 intelligence dimensions exist as typed data objects, in reference order, each with its overview fields (icon, title, 1-line description, 3 bullets) and its full modal detail (description, WHAT IT DELIVERS list, KEY QUESTIONS, analysis sections, strategic benefits)"
    - "Every bullet, question, and detail string is transcribed from the matching reference image; anything genuinely unreadable is marked with an explicit TODO comment rather than invented"
  artifacts:
    - path: "src/types/intelligence.ts"
      provides: "IntelligenceDimension type contract"
      exports: ["IntelligenceDimension"]
    - path: "src/data/intelligence.ts"
      provides: "The 12 typed intelligence dimension objects in reference order"
      exports: ["intelligenceDimensions"]
      contains: "Market Intelligence"
  key_links:
    - from: "src/data/intelligence.ts"
      to: "src/types/intelligence.ts"
      via: "import type { IntelligenceDimension }"
      pattern: "IntelligenceDimension"
---

<objective>
Create the typed data layer for the Intelligence section: an `IntelligenceDimension` type contract and an `intelligenceDimensions` array containing all 12 dimensions, each carrying both its lightweight overview fields (for the carousel cards) and its full modal detail (for the shared `DetailModal`). Content is transcribed verbatim from the reference images.

Purpose: This is the interface-first contract every downstream Intelligence plan consumes — the cards/carousel (02-06) read the overview fields, the detail content renderer (02-05) reads the modal fields, and both stay generic (no per-dimension conditionals) because the shape is uniform across all 12 objects. Building the data first means 02-05 and 02-06 receive the contract in-plan rather than exploring for it.

Output: `src/types/intelligence.ts`, `src/data/intelligence.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DECISIONS.md
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
<!-- Target shape (from UI-SPEC "Intelligence Data Layer"), adjusted: iconSrc → icon (an IntelligenceIcon NAME string, per 02-01 decision — inline SVG, not a file path). -->
Created here (consumed by 02-05 IntelligenceDetail + 02-06 IntelligenceSection/Card):
  @/types/intelligence -> type IntelligenceDimension = {
    id, slug, categoryLabel, title,
    icon: string,                 // IntelligenceIcon name (was iconSrc in UI-SPEC)
    overviewDescription: string,
    overviewBullets: string[],    // 3 short bullets shown on the overview card
    description: string,          // 3–5 sentence modal left-panel description
    delivers: { heading: string; body: string }[],           // WHAT IT DELIVERS
    keyQuestions: string[],                                    // KEY QUESTIONS WE ANSWER
    analysisSections: { sectionLabel: string; items: { icon?: string; title: string; description: string }[] }[],
    evidenceSectionLabel?: string;
    evidenceItems?: { icon?: string; title: string; description: string }[];
    strategicBenefits: { icon?: string; title: string; description: string }[];
  }
  @/data/intelligence  -> intelligenceDimensions: IntelligenceDimension[]  (length 12, reference order)
</interfaces>

The 12 dimensions in reference order (ids MUST match IntelligenceIcon names from 02-01):
1 market · 2 competitor · 3 customer · 4 brand · 5 product · 6 experience · 7 conversion · 8 commercial · 9 visibility · 10 lifecycle · 11 operational · 12 executive
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the IntelligenceDimension type contract</name>
  <files>src/types/intelligence.ts</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Intelligence Data Layer → TypeScript interface + the 12-row id/title table)
    @src/data/navigation.ts  (Phase 1 typed-data convention — how a type + typed export are structured)
  </read_first>
  <action>
    Create `src/types/intelligence.ts` exporting the `IntelligenceDimension` type exactly as shown in the interfaces block above (which adapts the UI-SPEC interface). The one deliberate change from the UI-SPEC interface: rename `iconSrc: string` to `icon: string` and document with a one-line comment that `icon` is an `IntelligenceIcon` name (inline SVG, per plan 02-01), NOT a file path — this avoids raster icon crops and new dependencies. Keep `analysisSections[].items[].icon`, `evidenceItems[].icon`, and `strategicBenefits[].icon` as OPTIONAL `icon?: string` (also IntelligenceIcon names). Use strict, non-optional types for all required fields (no `any`). This file contains ONLY types — no data, no runtime values.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/intelligence.ts` exports `IntelligenceDimension` with every field from the interfaces block, `icon: string` (not `iconSrc`), and the three nested `icon?` fields optional.
    - No `any` types; file has no runtime values.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Transcribe dimensions 1–6 (market, competitor, customer, brand, product, experience)</name>
  <files>src/data/intelligence.ts</files>
  <read_first>
    @src/types/intelligence.ts  (the contract from Task 1)
    @docs/qiera-reference/Homepage/Section 2.png  (overview icon + title + 3 bullets for ALL 12 cards — source for overviewBullets + overviewDescription)
    @docs/qiera-reference/Homepage/Intelligence cards/market intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/competitor intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/customer intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/brand intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/product intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/Experience intelligence card.png
  </read_first>
  <action>
    Create `src/data/intelligence.ts`, `import type { IntelligenceDimension } from "@/types/intelligence"`, and export `const intelligenceDimensions: IntelligenceDimension[] = [ ... ]`. Add the FIRST SIX dimension objects in reference order: `market`, `competitor`, `customer`, `brand`, `product`, `experience`. For EACH: set `id` (matches the IntelligenceIcon name), `slug` (kebab, e.g. "market-intelligence"), `categoryLabel` (uppercase, e.g. "MARKET INTELLIGENCE"), `title` (e.g. "Market Intelligence"), and `icon` (the dimension id). Transcribe `overviewDescription` (implied one-liner) and the 3 `overviewBullets` from `Section 2.png`. Transcribe the full modal detail from each dimension's own detail card image: `description` (left-panel intro paragraph), `delivers[]` (the "WHAT IT DELIVERS" checklist — heading + body per item, ~6 items on the market card), `keyQuestions[]` (the "KEY QUESTIONS WE ANSWER" bullets), `analysisSections[]` (the "WHAT WE ANALYZE" and "OUR EVIDENCE & QUALITY STANDARDS" grids — each with its `sectionLabel` and item title/description; assign a sensible `icon` name per item from the IntelligenceIcon set), and `strategicBenefits[]` (the bottom icon row — title + short description). Use the `evidenceSectionLabel`/`evidenceItems` fields when a card has a distinct evidence/standards grid (the market card does: "OUR EVIDENCE & QUALITY STANDARDS"); otherwise fold secondary grids into `analysisSections`. Transcribe copy VERBATIM — do not paraphrase, modernize, or invent (fidelity mandate). For any string genuinely unreadable in the reference, insert the literal value you can read plus a trailing `// TODO: unreadable in reference — verify before ship` comment on that line; never fabricate replacement copy. The evidence note copy ("Evidence-Aware Intelligence" + its body) is fixed across cards — take it from the UI-SPEC Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `intelligenceDimensions` contains exactly 6 objects after this task, ids in order: market, competitor, customer, brand, product, experience.
    - Each object satisfies `IntelligenceDimension` (tsc passes), has 3 `overviewBullets`, a non-empty `delivers[]`, non-empty `keyQuestions[]`, at least one `analysisSections[]` entry, and non-empty `strategicBenefits[]`.
    - Copy is transcribed verbatim from the matching reference; any unreadable string carries a `// TODO: unreadable` comment (no invented copy).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Transcribe dimensions 7–12 (conversion, commercial, visibility, lifecycle, operational, executive)</name>
  <files>src/data/intelligence.ts</files>
  <read_first>
    @src/types/intelligence.ts  (the contract)
    @docs/qiera-reference/Homepage/Section 2.png  (overview fields for cards 7–12)
    @docs/qiera-reference/Homepage/Intelligence cards/Conversion intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/commercial intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/visibility intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/lifecycle intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/operational intelligence card.png
    @docs/qiera-reference/Homepage/Intelligence cards/executive intelligence card.png
  </read_first>
  <action>
    Append the remaining SIX dimension objects to the existing `intelligenceDimensions` array in `src/data/intelligence.ts`, in reference order: `conversion`, `commercial`, `visibility`, `lifecycle`, `operational`, `executive`. Follow the identical transcription rules and field structure established in Task 2 (overview fields from `Section 2.png`; full modal detail from each dimension's own detail card image; verbatim copy; `// TODO: unreadable` comments for anything illegible; assign `icon` names from the IntelligenceIcon set). Do not alter the six objects added in Task 2. After appending, the array length is exactly 12.
  </action>
  <verify>
    <automated>node -e "const p=require('node:child_process');p.execSync('npx tsc --noEmit',{stdio:'inherit'})" && node --input-type=module -e "import('./src/data/intelligence.ts').catch(()=>{})"</automated>
  </verify>
  <acceptance_criteria>
    - `intelligenceDimensions` has length exactly 12, ids in full reference order market→executive with no duplicates.
    - The six objects from Task 2 are unchanged; the six new objects each satisfy `IntelligenceDimension` with the same completeness bar (3 overview bullets, non-empty delivers/keyQuestions/analysisSections/strategicBenefits).
    - Copy verbatim; unreadable strings flagged with `// TODO: unreadable`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-transcribed content → typed data module | All strings are authored by the developer from reference images; no user/request input enters this module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Injection (XSS) | dimension strings rendered later as JSX | mitigate | Data is plain typed strings consumed as JSX text by 02-05/02-06 (React auto-escapes); this plan introduces no `dangerouslySetInnerHTML` and no HTML-in-string content. |
| T-02-05 | Information disclosure | fabricated/incorrect copy | accept | Fidelity risk, not a security risk; mitigated by the verbatim-transcription rule and `// TODO: unreadable` markers, and finally by the Phase 2 screenshot gate. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (typed data + types only); no legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` clean.
- `intelligenceDimensions.length === 12`, ids unique and in reference order.
- Spot-check 3 cards (market, customer, brand — the UI-SPEC's designated screenshot cards) against their reference images for copy accuracy.
- No new `package.json` dependencies.
</verification>

<success_criteria>
A strict `IntelligenceDimension` contract plus 12 fully-transcribed, reference-faithful dimension objects exist, letting the cards and modal render generically with zero per-dimension branching.
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-02-SUMMARY.md` when done. List any strings marked `// TODO: unreadable` so the screenshot gate (02-07) can resolve them.
</output>
