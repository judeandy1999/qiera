---
phase: 02-hero-intelligence
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/components/sections/IntelligenceDetail.tsx
autonomous: true
requirements: [HOME-04, HOME-09]
user_setup: []

must_haves:
  truths:
    - "Given any IntelligenceDimension, a presentational component renders the two-panel detail layout: left panel (icon, category label, H2 title, description, WHAT IT DELIVERS checklist, evidence note) and right panel (KEY QUESTIONS grid, analysis/evidence section grids, STRATEGIC BENEFITS grid)"
    - "The layout renders generically from the data — no per-dimension conditional branching — so all 12 dimensions render through the same code path"
    - "The H2 title carries a stable id the modal can point aria-labelledby at; on mobile the two panels stack into one column"
  artifacts:
    - path: "src/components/sections/IntelligenceDetail.tsx"
      provides: "Presentational two-panel intelligence detail content (rendered inside DetailModal)"
      exports: ["IntelligenceDetail"]
      min_lines: 60
  key_links:
    - from: "src/components/sections/IntelligenceDetail.tsx"
      to: "src/types/intelligence.ts"
      via: "import type { IntelligenceDimension } prop"
      pattern: "IntelligenceDimension"
    - from: "src/components/sections/IntelligenceDetail.tsx"
      to: "src/components/ui/IntelligenceIcon.tsx"
      via: "renders <IntelligenceIcon name=... /> for icon container + mini-card/benefit icons"
      pattern: "IntelligenceIcon"
---

<objective>
Build `IntelligenceDetail` — the presentational component that renders one `IntelligenceDimension` as the two-panel modal content (left: icon + category label + H2 + description + WHAT IT DELIVERS checklist + evidence note; right: KEY QUESTIONS grid + analysis/evidence grids + STRATEGIC BENEFITS grid). It is generic across all 12 dimensions and is the content that plugs into `DetailModal` (02-04) as `children`.

Purpose: Separating the detail *content* from the modal *shell* keeps the modal reusable for Phases 3–4 and keeps this renderer a pure, testable presentational component driven entirely by data. It consumes the `IntelligenceDimension` contract (02-02), the `IntelligenceIcon` component and tokens (02-01).

Output: `src/components/sections/IntelligenceDetail.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
Consumed (already built):
  @/types/intelligence   -> IntelligenceDimension (categoryLabel, title, icon, description, delivers[], keyQuestions[], analysisSections[], evidenceSectionLabel?, evidenceItems?, strategicBenefits[])
  @/components/ui/IntelligenceIcon -> IntelligenceIcon({ name, className?, size? })
  tokens: --color-modal-left-bg, --color-accent-tint, --color-accent, --color-ink, --color-ink-secondary, --color-card-border, --radius-card

Created here (consumed by 02-06):
  @/components/sections/IntelligenceDetail -> IntelligenceDetail({ dimension: IntelligenceDimension; headingId: string })
    // headingId is applied to the left-panel <h2 id={headingId}> so DetailModal can aria-labelledby it
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the generic IntelligenceDetail two-panel renderer</name>
  <files>src/components/sections/IntelligenceDetail.tsx</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Detail Modal → "Left panel content structure" + "Right panel content structure" + Typography roles for each element + Color section left/right split + mobile single-column stack)
    @src/types/intelligence.ts  (the IntelligenceDimension shape being rendered)
    @docs/qiera-reference/Homepage/Intelligence cards/market intelligence card.png  (canonical two-panel layout: left checklist + evidence note; right KEY QUESTIONS 2-col, WHAT WE ANALYZE grid, OUR EVIDENCE grid, STRATEGIC BENEFITS row)
  </read_first>
  <action>
    Create `src/components/sections/IntelligenceDetail.tsx` (Server Component — pure presentational, no `'use client'`) exporting `IntelligenceDetail({ dimension, headingId })`. Render a responsive two-column container: single column at `<md`, two columns at `md` (left ≈36%, right ≈64% per UI-SPEC). LEFT PANEL (`background: var(--color-modal-left-bg)`, padding ~40px×32px): (1) an icon container 64×64px, `rounded-[var(--radius-card)]`, `background: var(--color-accent-tint)`, containing `<IntelligenceIcon name={dimension.icon} />` in accent; (2) category label `dimension.categoryLabel` as 12px/400 uppercase, letter-spacing ~0.1em, `var(--color-accent)`; (3) `<h2 id={headingId}>` with `dimension.title` at 32px/600/1.2, `var(--color-ink)`; (4) `dimension.description` paragraph at 16px/400/1.6, `var(--color-ink-secondary)`; (5) a hairline divider (`border-t border-[var(--color-card-border)]`); (6) section label "WHAT IT DELIVERS" (accent caption) then `dimension.delivers.map(...)` — each item: a 20px accent check icon (`<IntelligenceIcon name="check" />`), an item heading (16px/600, `var(--color-ink)`) and body (12px/400, `var(--color-ink-secondary)`); (7) a divider then the evidence note — a shield icon (`<IntelligenceIcon name="shield" />`), "Evidence-Aware Intelligence" (16px/600, accent) and the fixed evidence body copy at 12px/400. RIGHT PANEL (`background: var(--color-surface)`, padding ~40px×32px, `overflow-y-auto`): (1) "KEY QUESTIONS WE ANSWER" accent label then `dimension.keyQuestions` as a 2-column (md) / 1-column (mobile) bullet grid, each 16px/400 `var(--color-ink)` with a "•" marker; (2) `dimension.analysisSections.map(section => ...)` — for each: a divider, the `section.sectionLabel` accent caption, then `section.items` in a 3-col (md) / 2-col (sm) / 1-col grid of mini-cards, each: `<IntelligenceIcon name={item.icon ?? "chart"} size={32} />`, title (16px/600, ink), description (12px/400, ink-secondary); (3) if `dimension.evidenceItems` exists, render a divider + `dimension.evidenceSectionLabel` + the same mini-card grid; (4) a divider then "STRATEGIC BENEFITS" accent label and `dimension.strategicBenefits` in a 5–6-col (md) / 2–3 (sm) / 1 (mobile) grid, each: `<IntelligenceIcon size={24} name={item.icon ?? "rocket"} />`, title (16px/600), description (12px/400). CRITICAL: render everything by mapping over the data arrays — NO `switch`/`if (dimension.id === ...)` branching (the layout must be uniform for all 12). All copy comes from `dimension` props (already transcribed in 02-02); render as JSX text (no `dangerouslySetInnerHTML`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `IntelligenceDetail.tsx` exports `IntelligenceDetail({ dimension, headingId })`, has NO `'use client'`, imports `IntelligenceDimension` (type) and `IntelligenceIcon`.
    - Left panel renders icon container, category label, `<h2 id={headingId}>{dimension.title}</h2>`, description, WHAT IT DELIVERS checklist mapped from `delivers`, and the evidence note.
    - Right panel renders KEY QUESTIONS (2-col md), each `analysisSections` entry as a mini-card grid, optional `evidenceItems` grid, and STRATEGIC BENEFITS grid — all mapped from data with NO per-dimension conditional branches.
    - Two-column at `md`, single-column stack below `md`; colors use the Phase 2 tokens; no `dangerouslySetInnerHTML`.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed dimension data → presentational render | Only developer-authored typed strings are rendered; no user/request input reaches this component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Injection (XSS) | dimension strings rendered as JSX | mitigate | All text rendered as JSX children (React auto-escaped); no `dangerouslySetInnerHTML`; `icon` names only index the IntelligenceIcon map. |
| T-02-12 | Denial of Service | unknown `item.icon` name | mitigate | IntelligenceIcon (02-01) renders a fallback glyph for unknown names; the `?? "chart"`/`?? "rocket"` defaults further guard missing optional icons. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (presentational component only); no legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` clean.
- Grep: no `switch`/`dimension.id ===` branching; no `dangerouslySetInnerHTML`; `IntelligenceIcon` used for icons.
- Visual correctness vs the three designated card references is validated at the 02-07 screenshot gate.
</verification>

<success_criteria>
A single, data-driven `IntelligenceDetail` renders the faithful two-panel layout for any of the 12 dimensions, exposes a `headingId` for `aria-labelledby`, and stacks on mobile — with clean tsc/lint.
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-05-SUMMARY.md` when done. Note any grid-column counts that need correction at the screenshot gate.
</output>
