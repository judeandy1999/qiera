---
phase: 02-hero-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - public/images/qiera/operating-model.png
  - src/components/ui/IntelligenceIcon.tsx
autonomous: true
requirements: [HOME-03, HOME-04]
user_setup: []

must_haves:
  truths:
    - "The seven Phase 2 light-surface color tokens exist as CSS custom properties inside the existing single @theme inline block in globals.css (no second @theme block)"
    - "The operating-model hero illustration exists as a raster asset at public/images/qiera/operating-model.png (copied from the reference, not re-drawn)"
    - "A dependency-free IntelligenceIcon component renders an inline SVG for each of the 12 intelligence dimension ids, plus a shared set of small mini-card icons, all fillable with the accent token"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Phase 2 light-surface + modal color tokens added to the existing @theme inline block"
      contains: "--color-ink-secondary"
    - path: "public/images/qiera/operating-model.png"
      provides: "Hero right-column circular operating-model illustration (raster, non-reconstructable per FOUND-05)"
    - path: "src/components/ui/IntelligenceIcon.tsx"
      provides: "Inline-SVG icon component keyed by name; covers 12 dimension icons + reusable mini-card glyphs"
      exports: ["IntelligenceIcon"]
  key_links:
    - from: "src/components/ui/IntelligenceIcon.tsx"
      to: "globals.css accent token"
      via: "SVG fill/stroke references var(--color-accent)"
      pattern: "var\\(--color-accent\\)"
---

<objective>
Lay the shared visual foundation every other Phase 2 plan consumes: (1) add the seven Phase 2 light-surface / modal color tokens to the existing `@theme inline` block in `globals.css`, (2) place the non-reconstructable operating-model circular diagram as a raster asset under `public/images/qiera/`, and (3) build a dependency-free `IntelligenceIcon` inline-SVG component that provides the 12 dimension category icons and a reusable set of small mini-card glyphs.

Purpose: Tokens, the hero illustration, and icons are cross-cutting dependencies for the Hero (02-03), the modal detail content (02-05), and the intelligence cards/carousel (02-06). Building them once in Wave 1 lets those plans run without re-deriving tokens or duplicating icon SVGs, and keeps all CSS custom properties in one place (Pitfall 3 — never split the `@theme` block).

Output: extended `globals.css`, `public/images/qiera/operating-model.png`, `src/components/ui/IntelligenceIcon.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DECISIONS.md
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-PATTERNS.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
<!-- Current globals.css @theme inline block (Phase 1 — extend in place, do NOT add a second @theme block). -->
From src/app/globals.css (existing):
  @theme inline {
    --font-sans / --font-mono
    --color-shell #05060b, --color-secondary #10121b, --color-surface #ffffff, --color-ink #0b0d17,
    --color-accent #6366f1, --color-accent-strong #7c3aed,
    --color-text-primary #f5f5f7, --color-text-secondary #9ca3af,
    --color-border rgba(255,255,255,0.08), --color-focus-ring #8b7cf6,
    --radius-button 999px, --radius-card 16px, --container-max 1280px
  }
  body { background: var(--color-shell); color: var(--color-text-primary); font-family: var(--font-sans)... }

Created here (consumed by 02-03 Hero, 02-05 IntelligenceDetail, 02-06 IntelligenceSection/Card):
  @/components/ui/IntelligenceIcon -> IntelligenceIcon({ name: string; className?: string; size?: number })
</interfaces>

Reference (Phase 1 inline-SVG precedent): the `Logo` "Q" mark was recreated as inline SVG with `fill="var(--color-accent)"` rather than cropped from a PNG. Follow the same approach for the small, reconstructable intelligence icons. The operating-model circular diagram is the exception — it is explicitly non-reconstructable (FOUND-05) and is shipped as a raster copy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2 color tokens to the existing @theme inline block</name>
  <files>src/app/globals.css</files>
  <read_first>
    @src/app/globals.css  (the entire current file — one @import, one @theme inline block, one body rule)
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Color section → "New tokens for Phase 2" table — exact token names + values)
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  (globals.css section — Pitfall 3: never create a second @theme block)
  </read_first>
  <action>
    Extend the SINGLE existing `@theme inline` block in `src/app/globals.css` (do not create a second `@theme` block — Pitfall 3) by appending the seven Phase 2 light-surface / modal tokens exactly as specified in the UI-SPEC "New tokens for Phase 2" table: `--color-card-border: rgba(0, 0, 0, 0.08)`, `--color-card-active-border: #6366F1`, `--color-card-active-bg: rgba(99, 102, 241, 0.04)`, `--color-accent-tint: rgba(99, 102, 241, 0.08)`, `--color-ink-secondary: #6B7280`, `--color-modal-backdrop: rgba(0, 0, 0, 0.70)`, `--color-modal-left-bg: rgba(99, 102, 241, 0.06)`. Keep the existing Phase 1 tokens and the `body` rule unchanged. Do not touch `--color-ink` / `--color-surface` (already present from Phase 1). Values marked [ASSUMED] in the UI-SPEC are self-correcting at the Phase 2 screenshot gate — enter them as given now.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All seven token names above are present exactly once inside the single `@theme inline` block; `grep -c "@theme inline" src/app/globals.css` returns `1`.
    - Phase 1 tokens and the `body` rule are unchanged (no removed or renamed Phase 1 token).
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Place the operating-model illustration as a raster asset</name>
  <files>public/images/qiera/operating-model.png</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Operating Model Graphic section — FOUND-05 raster copy requirement, source filename, do-not-reconstruct note)
    @.planning/DECISIONS.md  (#12 reference path docs/qiera-reference/)
  </read_first>
  <action>
    Copy the standalone operating-model circular diagram from `docs/qiera-reference/Homepage/1ef8838f-af5b-4a7e-a227-894452882e37.png` to `public/images/qiera/operating-model.png`. Create the `public/images/qiera/` directory if it does not already exist. This is a permitted raster asset for a non-reconstructable illustration per FOUND-05 (the project rule against raster crops targets full-page-screenshot backgrounds, not illustration assets). Do NOT attempt to re-draw the full gradient arc / dot-matrix / colored node ring as SVG — the UI-SPEC explicitly forbids it because the result will not match. Use a shell copy (PowerShell `Copy-Item`), not a heredoc. After copying, record the pixel dimensions of the source file (reported as 834×834 in the UI-SPEC) in the SUMMARY so 02-03 can set correct `width`/`height`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');process.exit(fs.existsSync('public/images/qiera/operating-model.png')&&fs.statSync('public/images/qiera/operating-model.png').size>0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `public/images/qiera/operating-model.png` exists and is a non-empty file (byte-identical copy of the reference source).
    - No attempt was made to reconstruct the diagram as SVG.
    - Source dimensions are recorded in the plan SUMMARY for downstream `width`/`height`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build the dependency-free IntelligenceIcon inline-SVG component</name>
  <files>src/components/ui/IntelligenceIcon.tsx</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Card icon container + Detail Modal analysis/evidence/benefit icon notes; interface `icon`/`iconSrc` usage)
    @src/components/ui/Logo.tsx  (Phase 1 inline-SVG precedent — how a redrawn SVG references var(--color-accent))
    @docs/qiera-reference/Homepage/Section 2.png  (the 12 small category icons visible on the overview cards — the icons this component must approximate)
  </read_first>
  <action>
    Create `src/components/ui/IntelligenceIcon.tsx` (Server Component — no `'use client'`; it renders static SVG) exporting `IntelligenceIcon({ name, className, size = 24 })`. Implement a `name` → inline `<svg>` map covering, at minimum: the 12 dimension category icon names matching the intelligence dimension ids (`market`, `competitor`, `customer`, `brand`, `product`, `experience`, `conversion`, `commercial`, `visibility`, `lifecycle`, `operational`, `executive`) inspecting `Section 2.png` for the intended glyph (e.g. market = bar-chart, competitor = people, customer = single user, brand = diamond/gem, product = cube, conversion = funnel, commercial = dollar, visibility = magnifier), PLUS a reusable set of generic line glyphs for modal mini-cards and strategic benefits (e.g. `globe`, `pie`, `trend`, `compass`, `bank`, `bars`, `shield`, `check`, `rocket`, `users`, `target`, `scale`, `warning`, `chart`). Each SVG: `viewBox="0 0 24 24"`, `width`/`height` = `size`, `aria-hidden="true"` (icons are decorative — the adjacent text label is the accessible name), stroke or fill referencing `var(--color-accent)` (or `currentColor` so callers can recolor via a text-color class). Provide a graceful fallback: if `name` is unknown, render a neutral default glyph (e.g. a small circle) rather than throwing, so a data icon-name typo degrades visibly instead of crashing. Keep every glyph as hand-written SVG path data — do NOT install an icon package (project rule: no unnecessary libraries; Phase 1 recreated its one glyph inline). This component supersedes the UI-SPEC's `iconSrc`-as-file-path assumption: icons are inline SVG (zero new deps, no raster crops), so the data layer (02-02) stores an `icon` NAME string consumed here, and no per-icon image files are created.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `IntelligenceIcon.tsx` exports `IntelligenceIcon`, has NO `'use client'`, and imports NO external icon/SVG library.
    - The `name` map resolves all 12 dimension ids (`market`…`executive`) plus the reusable mini-card glyph set; an unknown `name` renders a fallback glyph without throwing.
    - Every rendered `<svg>` is `aria-hidden="true"` and its color derives from `var(--color-accent)` or `currentColor` (no hardcoded hex).
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference asset → public/ static file | A developer-controlled illustration is copied into the served `public/` tree — no user input crosses this boundary |
| developer icon map → rendered SVG | `IntelligenceIcon` renders only static, developer-authored SVG path data; the `name` prop comes from typed data, not request input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | static asset in `public/` | accept | The copied PNG is a build-time developer asset; no runtime write path exists. Integrity is the source reference file itself. |
| T-02-02 | Injection (XSS) | `IntelligenceIcon` SVG rendering | mitigate | SVG is authored as static JSX path data with no `dangerouslySetInnerHTML`; the `name` prop only selects among a fixed map and never interpolates into markup. |
| T-02-03 | Denial of Service | unknown icon `name` | mitigate | Unknown `name` renders a fallback glyph rather than throwing, so a data typo cannot crash the render tree. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages are installed in this plan (inline SVG + file copy + CSS only); no legitimacy gate required. |
</threat_model>

<verification>
- `grep -c "@theme inline" src/app/globals.css` == 1; the seven new token names all present.
- `public/images/qiera/operating-model.png` exists and is non-empty.
- `npx tsc --noEmit` clean; `npm run lint` clean.
- No new entries added to `package.json` dependencies/devDependencies.
</verification>

<success_criteria>
Phase 2 tokens live in the single `@theme inline` block, the operating-model illustration is available as a served raster asset, and `IntelligenceIcon` renders faithful inline SVGs for all 12 dimensions plus reusable mini-card glyphs — all with zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-01-SUMMARY.md` when done. Record the operating-model source pixel dimensions and the final list of icon `name`s implemented (so 02-02 can reference valid names).
</output>
