---
phase: 02-hero-intelligence
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/Hero.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-03]
user_setup: []

must_haves:
  truths:
    - "The homepage renders a dark hero: a left column with the operating-model badge, a two-tone headline whose last phrase is accent-colored, a body paragraph, and the Start Intelligence Audit CTA; and a right column with the operating-model illustration"
    - "The hero uses the split layout at desktop and stacks (text first, graphic below) on mobile, with no horizontal overflow at 1440/1024/390"
    - "The CTA reuses the existing Phase 1 Button (primary) unchanged and links to /start-intelligence-audit"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "RSC hero section: split layout, badge, h1 (accent span), body, CTA, operating-model Image"
      exports: ["Hero"]
      min_lines: 30
    - path: "src/app/page.tsx"
      provides: "Homepage rendering <Hero /> (placeholder copy removed)"
      contains: "Hero"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/Hero.tsx"
      via: "import { Hero } and render <Hero />"
      pattern: "Hero"
    - from: "src/components/sections/Hero.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import { Button } for the primary CTA"
      pattern: "from \"@/components/ui/Button\""
    - from: "src/components/sections/Hero.tsx"
      to: "public/images/qiera/operating-model.png"
      via: "next/image <Image src=... priority>"
      pattern: "operating-model.png"
---

<objective>
Build the dark hero section (HOME-03): a two-column split with the left content stack (badge → two-tone `<h1>` → body paragraph → primary CTA) and the right operating-model illustration, then render it as the first thing on the homepage in place of the Phase 1 placeholder.

Purpose: The hero is the primary conversion driver and the first visual anchor of the site. It reuses the Phase 1 `Button` and design tokens unchanged, and consumes the `operating-model.png` asset from 02-01. Wiring `<Hero />` into `page.tsx` now (leaving room below for the Intelligence section from 02-06) establishes the homepage composition order.

Output: `src/components/sections/Hero.tsx`, updated `src/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DECISIONS.md
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
Consumed (already built):
  @/components/ui/Button  -> Button({ href, variant?, className?, children })  // primary appends a trailing → automatically
  public/images/qiera/operating-model.png  // raster hero illustration from plan 02-01 (source 834×834)
  tokens: --color-shell, --color-text-primary, --color-text-secondary, --color-accent, --container-max, --radius-button

Created here (consumed by 02-06 which renders <IntelligenceSection/> below <Hero/> in page.tsx):
  @/components/sections/Hero  -> Hero(): JSX.Element   (RSC)
</interfaces>

Existing page.tsx (placeholder to replace):
```1:9:src/app/page.tsx
export default function Home() {
  return (
    <div className="mx-auto flex max-w-[var(--container-max)] flex-1 items-center justify-center px-6 py-16 md:px-8 lg:px-12">
      <p className="text-center text-base ... text-[var(--color-text-secondary)]">
        QIERA homepage — sections arriving in later phases.
      </p>
    </div>
  );
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Hero section (RSC)</name>
  <files>src/components/sections/Hero.tsx</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Hero Section + Copywriting Contract → Hero table + Operating Model Graphic — exact copy, split percentages, badge style, headline two-tone rule, Image alt/priority/dimensions)
    @src/components/ui/Button.tsx  (the CTA component signature — do not modify it)
    @docs/qiera-reference/Homepage/top section.png  (hero geometry, badge, headline line breaks, graphic placement)
    @docs/qiera-reference/Homepage/c3448e9e-55eb-488a-9f4c-3d6530f45392.png  (copy close-up — higher-res source of the headline/body text; takes precedence over top section.png for wording)
  </read_first>
  <action>
    Create `src/components/sections/Hero.tsx` as a Server Component (no `'use client'`) exporting `Hero()`. Root `<section>` with background `var(--color-shell)`, inner container `mx-auto max-w-[var(--container-max)]` and the shell horizontal padding pattern (`px-6 md:px-8 lg:px-12`), generous vertical padding (~96px desktop / ~64px mobile — [ASSUMED], correct at the screenshot gate). Layout: a responsive grid/flex that is a single stacked column at `<lg` (text first, graphic below) and a two-column split at `lg` (left ≈44%, right ≈56%, gap ≈64px) per the UI-SPEC Hero split. LEFT column (flex column, vertically centered): (1) the badge — an outline pill `<div>` (or `<span>`), `border: 1px solid rgba(99,102,241,0.5)`, transparent background, `rounded-[var(--radius-button)]`, padding 8px 16px, text `THE QIERA OPERATING MODEL` at 12px/400 uppercase letter-spacing ~0.08em in `var(--color-text-primary)`; (2) the headline as a single `<h1>` at ~60px/600/1.1 [ASSUMED] containing two `<span>`s — the first "A proven system to turn intelligence into " in `var(--color-text-primary)`, the second "measurable growth." in `var(--color-accent)` (do NOT use two heading elements; the accent phrase is a span); (3) a `<p>` body paragraph at 16px/400/1.6 in `var(--color-text-secondary)`, max-width ~480px, copy transcribed VERBATIM from the Copywriting Contract; (4) `<Button href="/start-intelligence-audit" variant="primary">Start Intelligence Audit</Button>` (content-width, not full-width — the primary Button already appends the trailing → so do NOT add another arrow). RIGHT column: `<Image src="/images/qiera/operating-model.png" ... priority>` from `next/image` with `width={600} height={600}`, `sizes="(max-width: 767px) 100vw, 56vw"`, and the descriptive `alt` text specified in the UI-SPEC Operating Model Graphic section (naming the six steps + centre Q). No border/radius on the image (the asset carries its own framing). Copy all hero text strings VERBATIM from the Copywriting Contract — do not paraphrase.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Hero.tsx` exports `Hero`, has NO `'use client'`, imports `Button` from `@/components/ui/Button` and `Image` from `next/image`.
    - Exactly one `<h1>` exists; its final phrase "measurable growth." is inside a span colored `var(--color-accent)`, the rest uses `var(--color-text-primary)`.
    - Badge text is `THE QIERA OPERATING MODEL`; body paragraph matches the Copywriting Contract verbatim; CTA is `<Button href="/start-intelligence-audit" variant="primary">` with no manually added arrow glyph.
    - `<Image>` references `/images/qiera/operating-model.png`, has `priority`, non-empty descriptive `alt`, and `width`/`height` set.
    - Layout stacks below `lg` and splits at `lg` (Tailwind responsive classes present); `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render Hero on the homepage (replace placeholder)</name>
  <files>src/app/page.tsx</files>
  <read_first>
    @src/app/page.tsx  (current placeholder to remove)
    @src/app/layout.tsx  (confirms Header/main/Footer shell already wraps page content — page.tsx renders inside <main>)
  </read_first>
  <action>
    Replace the placeholder body of `src/app/page.tsx` so `Home()` renders `<Hero />` (import from `@/components/sections/Hero`) as the first section. Remove the "QIERA homepage — sections arriving in later phases." placeholder paragraph and its centering wrapper. Keep `Home` a Server Component (no `'use client'`). Do NOT add the Intelligence section here — 02-06 (a later wave) appends `<IntelligenceSection />` below `<Hero />`; structure the return as a fragment or a lightweight wrapper so 02-06 can add a sibling without restructuring. Do not add its own `<main>` (the root layout already provides `<main className="flex-1">`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports and renders `<Hero />`; the placeholder paragraph string is gone.
    - `Home` has no `'use client'` and does not introduce a second `<main>`.
    - The structure allows a sibling section to be appended after `<Hero />` without rewriting `Home`.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static hero content → RSC render | Hero renders only static developer copy + a static image; no user/request input crosses into it |
| served image asset → browser | `operating-model.png` is a build-time asset served from `public/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Injection (XSS) | hero headline/body/badge JSX | mitigate | All copy rendered as JSX text (React auto-escaped); no `dangerouslySetInnerHTML`; the accent phrase is a plain `<span>`. |
| T-02-07 | Tampering (open redirect) | CTA `href` | accept | `href` is the hardcoded internal route `/start-intelligence-audit`; no request-derived URL. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (uses existing Button + next/image); no legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` clean.
- Exactly one `<h1>` in Hero; accent span present; CTA points to `/start-intelligence-audit`.
- `<Image>` uses `priority` and references the 02-01 asset path.
- Visual comparison against `top section.png` is deferred to the 02-07 screenshot gate.
</verification>

<success_criteria>
The homepage opens with a faithful dark hero — split layout at desktop, stacked at mobile, two-tone headline, verbatim copy, reused primary Button, and the operating-model illustration — with clean tsc/lint and no second `<main>`.
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-03-SUMMARY.md` when done. Note any [ASSUMED] hero measurements (headline size, vertical padding, split ratio) for correction at the screenshot gate.
</output>
