---
phase: 02-hero-intelligence
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified:
  - src/components/sections/IntelligenceCard.tsx
  - src/components/sections/IntelligenceSection.tsx
  - src/components/sections/IntelligenceSection.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-04, HOME-09]
user_setup: []

must_haves:
  truths:
    - "The homepage shows an Intelligence section (id='intelligence') with the eyebrow, heading, subheading, carousel controls, and all 12 overview cards, each with icon, title, 3 bullets, and a Learn more control"
    - "The carousel advances and reverses via prev/next buttons (mouse), arrow keys (keyboard), and swipe/scroll (touch), with a live counter and pagination dots reflecting position"
    - "Activating a card's Learn more opens the shared DetailModal showing that dimension's IntelligenceDetail; Escape / close / backdrop close it and return focus to the Learn more control that opened it"
  artifacts:
    - path: "src/components/sections/IntelligenceCard.tsx"
      provides: "Presentational overview card (icon, title, 3 bullets, Learn more button)"
      exports: ["IntelligenceCard"]
    - path: "src/components/sections/IntelligenceSection.tsx"
      provides: "Client section: header, carousel (mouse/keyboard/touch), dots, 12 cards, modal orchestration"
      exports: ["IntelligenceSection"]
      min_lines: 60
    - path: "src/components/sections/IntelligenceSection.test.tsx"
      provides: "Asserts carousel next/prev changes active position and all 12 titles render"
    - path: "src/app/page.tsx"
      provides: "Homepage rendering <Hero /> then <IntelligenceSection />"
      contains: "IntelligenceSection"
  key_links:
    - from: "src/components/sections/IntelligenceSection.tsx"
      to: "src/data/intelligence.ts"
      via: "import { intelligenceDimensions } and map to cards"
      pattern: "intelligenceDimensions"
    - from: "src/components/sections/IntelligenceSection.tsx"
      to: "src/components/ui/DetailModal.tsx"
      via: "renders <DetailModal> wrapping <IntelligenceDetail> for the selected dimension"
      pattern: "DetailModal"
    - from: "src/app/page.tsx"
      to: "src/components/sections/IntelligenceSection.tsx"
      via: "renders <IntelligenceSection /> below <Hero />"
      pattern: "IntelligenceSection"
---

<objective>
Assemble the Intelligence section (HOME-04): a presentational `IntelligenceCard`, and a client `IntelligenceSection` that renders the section header + carousel controls + all 12 overview cards with mouse/keyboard/touch operation, pagination dots, and modal orchestration (Learn more → shared `DetailModal` + `IntelligenceDetail`, HOME-09 focus restore to the trigger). Ship a carousel test and wire the section onto the homepage under the hero with `id="intelligence"`.

Purpose: This is the plan that makes the Intelligence feature real and interactive — it composes every prior Wave 1–2 artifact (data 02-02, icon/tokens 02-01, modal shell 02-04, detail content 02-05) into the working section and completes HOME-04. It renders below the already-wired `<Hero />` (02-03).

Output: `IntelligenceCard.tsx`, `IntelligenceSection.tsx`, `IntelligenceSection.test.tsx`, updated `page.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hero-intelligence/02-UI-SPEC.md
@.planning/DECISIONS.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
Consumed (already built):
  @/data/intelligence    -> intelligenceDimensions: IntelligenceDimension[]  (length 12)
  @/types/intelligence   -> IntelligenceDimension
  @/components/ui/IntelligenceIcon -> IntelligenceIcon({ name, className?, size? })
  @/components/ui/DetailModal      -> DetailModal({ open, onClose, title, labelledById?, children })
  @/components/sections/IntelligenceDetail -> IntelligenceDetail({ dimension, headingId })
  tokens: --color-surface, --color-ink, --color-ink-secondary, --color-accent, --color-accent-tint,
          --color-card-border, --color-card-active-border, --color-card-active-bg, --radius-card, --container-max

Existing page.tsx after 02-03: renders <Hero /> as the first section (append <IntelligenceSection /> as a sibling below it).

Created here:
  @/components/sections/IntelligenceCard    -> IntelligenceCard({ dimension, active?, onLearnMore, learnMoreRef? })
  @/components/sections/IntelligenceSection  -> IntelligenceSection()  ('use client')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the presentational IntelligenceCard</name>
  <files>src/components/sections/IntelligenceCard.tsx</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Intelligence Overview Cards → card anatomy, active state, icon container, card content, "Learn more →" a11y label)
    @src/types/intelligence.ts  (IntelligenceDimension — overview fields used here)
    @docs/qiera-reference/Homepage/Section 2.png  (card visual: icon top-left, title, 3 accent-bulleted lines, "Learn more →" link, active card left-border highlight)
  </read_first>
  <action>
    Create `src/components/sections/IntelligenceCard.tsx` (Server Component — presentational; no `'use client'`) exporting `IntelligenceCard({ dimension, active, onLearnMore, learnMoreRef })`. Card container: `background: var(--color-surface)`, `border: 1px solid var(--color-card-border)`, `rounded-[var(--radius-card)]`, padding 24px. Active state (`active === true`): keep the 4-side border but override the LEFT border to `2px solid var(--color-card-active-border)` and set `background: var(--color-card-active-bg)` — do not restyle all borders. Content: a 48×48px icon container (`rounded-[12px]`, `background: var(--color-accent-tint)`) holding `<IntelligenceIcon name={dimension.icon} />`; the title `dimension.title` (16px/600, `var(--color-ink)`); a hairline divider (`border-[var(--color-card-border)]`); the 3 `dimension.overviewBullets` as a list where each bullet marker is an accent circle glyph and text is `var(--color-ink-secondary)` (16px/400); and a "Learn more →" control. The Learn more control MUST be a `<button type="button">` (it opens a modal, it does not navigate — UI-SPEC HOME-09) with `aria-label={`Learn more about ${dimension.title}`}`, text "Learn more →" in `var(--color-accent)`, ≥44px touch height on mobile, a visible `var(--color-focus-ring)` focus ring, `onClick={onLearnMore}`, and `ref={learnMoreRef}` forwarded to the button element (so the section can restore focus to it). The card container itself is NOT clickable (cursor default). Render bullets/title as JSX text (no `dangerouslySetInnerHTML`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `IntelligenceCard.tsx` exports `IntelligenceCard`, renders the icon container, title, 3 bullets from `overviewBullets`, and a Learn more `<button>` (not an `<a>`).
    - The Learn more button has `aria-label="Learn more about {title}"`, calls `onLearnMore`, forwards `learnMoreRef`, and has a visible focus ring; the card container is not itself a clickable/navigating element.
    - Active state overrides only the left border (2px accent) + active bg; inactive uses the standard card border.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the client IntelligenceSection (header, carousel, dots, modal orchestration)</name>
  <files>src/components/sections/IntelligenceSection.tsx</files>
  <read_first>
    @.planning/phases/02-hero-intelligence/02-UI-SPEC.md  (Intelligence Section header layout + eyebrow/heading/subheading copy + carousel nav controls; Overview Cards grid/carousel breakpoints + pagination dots + ARIA; Detail Modal activation; Accessibility Contract HOME-09 — carousel keyboard/touch, modal open/label/focus restore; Copywriting Contract Intelligence Section)
    @src/components/ui/DetailModal.tsx  (props: open/onClose/title/labelledById/children)
    @src/components/sections/IntelligenceDetail.tsx  (props: dimension/headingId)
    @src/data/intelligence.ts  (the 12 dimensions to render)
  </read_first>
  <behavior>
    - All 12 dimension titles render (as cards).
    - Clicking Next advances the active page/index (counter increments, active dot moves); clicking Prev reverses it; navigation is bounded (does not go below 1 or past 12/last page).
    - ArrowRight/ArrowLeft on the focused carousel region advance/reverse like Next/Prev.
    - Clicking a card's Learn more opens the modal (dialog visible) with that dimension's title as the accessible name; closing (Escape/close/backdrop) returns focus to that same Learn more button.
  </behavior>
  <action>
    Create `src/components/sections/IntelligenceSection.tsx` with `'use client'` exporting `IntelligenceSection()`. Root `<section id="intelligence">` with `scroll-mt-[72px]` (scroll-margin-top for the sticky header, HOME-02 — [ASSUMED 72px], correct at screenshot gate), `background: var(--color-surface)`, inner `mx-auto max-w-[var(--container-max)]` with the shell padding pattern and ~96px vertical padding. HEADER row (`justify-between`, wraps on small): left stack = eyebrow "1. OUR UNIQUE INSIGHTS" (12px/400 uppercase accent), `<h2>` "Growth Intelligence Across Every Dimension" (32px/600, ink), subheading `<p>` (16px/400, ink-secondary) — all copy VERBATIM from the Copywriting Contract; right = carousel controls: a Prev `<button aria-label="Previous intelligence dimension">` (40×40 circular, ≥44px hit area, `←`), a live counter "{current} / 12" (wrap in `aria-live="polite"`), and a Next `<button aria-label="Next intelligence dimension">` (`→`). CARDS: import `intelligenceDimensions` and render `IntelligenceCard` for each. Responsive presentation per UI-SPEC: desktop (≥1280) 6-col × 2-row grid (all 12 visible — carousel controls still update the "active" highlight); tablet/mobile a 1-per-view (mobile) / 3-per-view (tablet) carousel using CSS `scroll-snap-type: x mandatory` on a horizontally-scrollable track (native touch swipe, no JS library). CAROUSEL STATE: track a `page`/`index` in `useState`; Prev/Next buttons update it (bounded); disable Prev at the first position and Next at the last (`disabled` + `aria-disabled` + opacity 0.4). KEYBOARD: the scroll track (or a wrapping region) has `tabIndex={0}`, an accessible label, and an `onKeyDown` mapping ArrowRight→next / ArrowLeft→prev. PAGINATION DOTS: below the track, one 8×8px dot per page centered, active dot `var(--color-accent)` opacity 1 / inactive `var(--color-ink-secondary)` opacity 0.4, implemented as `<button>`s in a `role="tablist"` (or an aria-live position indicator) with keyboard parity; clicking a dot jumps to that page. When advancing on tablet/mobile, sync the scroll position (e.g. `scrollIntoView`/`scrollTo` on the track) so touch scroll and button/keyboard nav stay consistent. MODAL ORCHESTRATION: hold `selected: IntelligenceDimension | null` (and remember the triggering Learn more button via a ref map or a stored ref) in state; each card's `onLearnMore` sets `selected` and opens the modal; render a single `<DetailModal open={selected !== null} onClose={() => setSelected(null)} title={selected?.title ?? ""} labelledById="intelligence-modal-heading"><IntelligenceDetail dimension={selected} headingId="intelligence-modal-heading" /></DetailModal>` (guard rendering children only when `selected`). DetailModal already restores focus to the pre-open `activeElement` (the Learn more button) — verify this is the trigger by not moving focus elsewhere before opening. Do NOT hardcode any dimension text here (all from data). No `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `IntelligenceSection.tsx` begins with `'use client'`, exports `IntelligenceSection`, imports `intelligenceDimensions`, `IntelligenceCard`, `DetailModal`, `IntelligenceDetail`.
    - Root is `<section id="intelligence">` with a scroll-margin-top utility; eyebrow/heading/subheading copy matches the Copywriting Contract verbatim.
    - Prev/Next buttons have the exact aria-labels, are bounded (disabled at ends), a live counter shows "{n} / 12"; a keyboard-focusable region maps ArrowLeft/ArrowRight; pagination dots render with active/inactive styling and keyboard parity.
    - A single `DetailModal` is rendered and opened by any card's Learn more with `title`/`labelledById` wired to `IntelligenceDetail`'s `headingId`.
    - No hardcoded dimension copy; no `dangerouslySetInnerHTML`; `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Carousel test + wire IntelligenceSection onto the homepage</name>
  <files>src/components/sections/IntelligenceSection.test.tsx, src/app/page.tsx</files>
  <read_first>
    @src/components/layout/MobileNav.test.tsx  (userEvent + testing-library conventions in this repo)
    @src/components/sections/IntelligenceSection.tsx  (component under test)
    @src/app/page.tsx  (current homepage after 02-03 — renders <Hero />)
  </read_first>
  <behavior>
    - After render, all 12 dimension titles are queryable.
    - The counter starts at "1 / 12"; clicking Next changes it (e.g. to "2 / 12") and clicking Prev returns it; Prev is disabled at position 1.
    - (If practical) clicking a Learn more opens a dialog; Escape closes it.
  </behavior>
  <action>
    Create `src/components/sections/IntelligenceSection.test.tsx` (Vitest + @testing-library/react + @testing-library/user-event). Tests: (1) "renders all 12 dimensions" — render `<IntelligenceSection />`, assert 12 known titles are present (e.g. getByText for "Market Intelligence" and "Executive Intelligence", or assert 12 "Learn more about …" buttons via `getAllByRole('button', { name: /Learn more about/ })` has length 12); (2) "next/prev advance the carousel" — assert the counter shows "1 / 12", assert the Prev button is disabled, click the Next button ("Next intelligence dimension"), assert the counter updates (e.g. "2 / 12") and Prev becomes enabled, click Prev, assert it returns to "1 / 12". Keep assertions resilient to the desktop-all-visible layout (the counter + active state is the position source of truth, not visibility). Then update `src/app/page.tsx` to render `<IntelligenceSection />` (import from `@/components/sections/IntelligenceSection`) as a sibling immediately AFTER `<Hero />`. Do not remove or restructure `<Hero />`; keep `Home` a Server Component (it can render a client child without becoming a client component itself).
  </action>
  <verify>
    <automated>npx vitest run src/components/sections/IntelligenceSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts all 12 dimensions render and that Next/Prev change the counter with Prev disabled at position 1; it passes via `npx vitest run src/components/sections/IntelligenceSection.test.tsx`.
    - `page.tsx` renders `<Hero />` then `<IntelligenceSection />`; `Home` remains a Server Component with a single `<main>` (from layout).
    - Full suite `npx vitest run` stays green; `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ↔ client section island | Carousel + modal orchestration run client-side JS driven only by static typed data and user interaction events |
| typed dimension data → cards/modal | All rendered content is developer-authored typed data (02-02); no request/user input is rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Injection (XSS) | card/modal content rendering | mitigate | Content rendered as JSX text from typed data; no `dangerouslySetInnerHTML` in card or section. |
| T-02-14 | Denial of Service | carousel index / keyboard handlers | mitigate | Index changes are bounded (clamped to 0..lastPage); no unbounded loops; handlers are simple state setters. |
| T-02-15 | Repudiation/Tampering | modal focus restore | accept | Focus restore delegated to native `<dialog>` + DetailModal's explicit stored-trigger refocus (built + tested in 02-04). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (CSS scroll-snap + existing components + existing test libs); no legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run` full suite green (carousel test + all prior tests).
- `npx tsc --noEmit` clean; `npm run lint` clean.
- Grep: `id="intelligence"` present; Prev/Next aria-labels exact; no `dangerouslySetInnerHTML`; only `IntelligenceSection` (+ MobileNav, DetailModal) carry `'use client'` among Phase 2 components.
- Interactive fidelity (mouse/keyboard/touch, modal visuals) confirmed at the 02-07 screenshot gate.
</verification>

<success_criteria>
The homepage renders a faithful, interactive Intelligence section: all 12 cards, a carousel operable by mouse/keyboard/touch with counter + dots, and Learn more opening the shared accessible modal with correct focus restore — proven by a passing carousel test and clean tsc/lint.
</success_criteria>

<output>
Create `.planning/phases/02-hero-intelligence/02-06-SUMMARY.md` when done. Note the chosen tablet/mobile carousel page counts and any [ASSUMED] values (scroll-margin-top, per-view counts) for the screenshot gate.
</output>
