---
phase: 01-foundation-shell
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - src/components/ui/Logo.tsx
  - src/components/ui/Button.tsx
  - public/images/qiera/.gitkeep
autonomous: true
requirements: [FOUND-02, FOUND-05]
user_setup: []

must_haves:
  truths:
    - "A reusable Logo renders the QIERA 'Q' mark (accent-filled inline SVG) + 'QIERA' wordmark, with an accessible name for screen readers"
    - "A reusable Button renders primary (accent pill + trailing arrow) and secondary (outline pill) variants using tokens only — no hardcoded hex"
    - "public/images/qiera exists as the home for any non-reconstructable raster art (FOUND-05), even though Phase 1 ships the logo as SVG"
  artifacts:
    - path: "src/components/ui/Logo.tsx"
      provides: "Inline-SVG brand mark + wordmark (RSC)"
      exports: ["Logo"]
    - path: "src/components/ui/Button.tsx"
      provides: "Token-driven primary/secondary pill button (RSC)"
      exports: ["Button"]
    - path: "public/images/qiera/.gitkeep"
      provides: "Committed raster-asset directory per FOUND-05"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/lib/cn.ts"
      via: "import { cn }"
      pattern: "from \"@/lib/cn\""
    - from: "src/components/ui/Button.tsx"
      to: "globals.css tokens"
      via: "var(--color-accent) / var(--radius-button)"
      pattern: "--color-accent|--radius-button"
---

<objective>
Build the two shared UI primitives the shell depends on — `Logo` (inline-SVG "Q" mark + wordmark, RSC) and `Button` (token-driven primary/secondary pill variants, RSC) — and establish the `public/images/qiera` raster-asset directory required by FOUND-05.

Purpose: Header and Footer (Plan 04) both render `Logo`; Header renders the primary CTA `Button`; Phase 5's Final CTA will reuse the secondary variant. Defining these primitives first (interface-first ordering) means the layout plan consumes finished contracts instead of exploring. Logo is an SVG recreation, not a raster crop of a reference screenshot (Pattern 3 + `qiera-visual-fidelity.mdc` asset rule); `public/images/qiera` still ships now so FOUND-05's raster path exists and the documented Logo fallback is available.

Output: `src/components/ui/Logo.tsx`, `src/components/ui/Button.tsx`, `public/images/qiera/.gitkeep`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-PATTERNS.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
<!-- Contracts this plan CREATES for Plan 04 (Header/Footer). Implement to these exactly. -->

src/components/ui/Logo.tsx (RSC, no 'use client'):
  export function Logo(props: { className?: string; showTagline?: boolean }): JSX.Element
    // inline <svg> "Q" mark filled var(--color-accent), viewBox 0 0 32 32, role="img", aria-label="QIERA"
    // wordmark "QIERA" text: 20px / 600 / var(--color-text-primary)
    // when showTagline: render "E-commerce Growth Intelligence" 14px/400/var(--color-text-secondary) beneath

src/components/ui/Button.tsx (RSC, no 'use client'):
  export function Button(props: {
    href: string;
    variant?: 'primary' | 'secondary';   // default 'primary'
    className?: string;
    children: React.ReactNode;
  }): JSX.Element
    // renders <a> pill: rounded var(--radius-button), text 20/600
    // primary  : bg var(--color-accent), text var(--color-text-primary), trailing '→' (aria-hidden) with 4px gap
    // secondary: transparent bg, 1px border var(--color-accent) @ ~50% opacity, text var(--color-accent), no arrow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Logo (inline SVG mark + wordmark) and the raster asset directory</name>
  <files>src/components/ui/Logo.tsx, public/images/qiera/.gitkeep</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/components/ui/Logo.tsx" section — base shape + required delta: mark + wordmark + optional tagline)
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Logo section — wordmark 20/600, tagline 14/400, mark fill = accent; Typography table)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Pattern 3 — SVG recreation not raster crop; FOUND-05 fallback)
  </read_first>
  <action>
    Create `src/components/ui/Logo.tsx` as a Server Component (no `'use client'`) exporting `Logo({ className, showTagline }: { className?: string; showTagline?: boolean })`. Render an inline `<svg>` "Q" ring/letterform mark (viewBox `0 0 32 32`, `role="img"`, `aria-label="QIERA"`, fill `var(--color-accent)`) redrawn by hand to approximate the reference mark — do NOT crop the mark from any reference PNG (violates `qiera-visual-fidelity.mdc` asset rule / RESEARCH Pattern 3). Beside the mark render the wordmark "QIERA" at 20px / weight 600 / `var(--color-text-primary)` (use token-referencing Tailwind arbitrary-value utilities, never hardcoded hex). When `showTagline` is true, render "E-commerce Growth Intelligence" beneath at 14px / 400 / `var(--color-text-secondary)`. Include a visually-hidden `QIERA` text (e.g. `sr-only`) OR rely on the svg `aria-label` — ensure exactly one accessible name "QIERA" is exposed (avoid double-announcing). Also create `public/images/qiera/.gitkeep` (empty) so the FOUND-05 raster directory exists and is committed even though no raster is needed yet (the SVG-mismatch fallback would drop `logo.png` here). Exact SVG path geometry is `[ASSUMED]` and screenshot-verified in Plan 05; if it can't match closely, the documented fallback is a raster `public/images/qiera/logo.png` via `next/image` (FOUND-05) — note this in the summary if used.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/Logo.tsx` exports `Logo` with the `{ className?, showTagline? }` props and has NO `'use client'` directive.
    - The mark is an inline `<svg>` with `fill` referencing `var(--color-accent)` (not a hardcoded hex, not an `<img>`/`next/image` crop of a reference screenshot).
    - Exactly one accessible name "QIERA" is exposed (via svg `aria-label` or `sr-only`, not both duplicated).
    - `public/images/qiera/.gitkeep` exists.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create token-driven Button with primary + secondary variants</name>
  <files>src/components/ui/Button.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/components/ui/Button.tsx" section — variant-driven target shape using cn())
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Buttons table — primary fill/text/radius/arrow; secondary outline; Copywriting: primary CTA renders trailing '→')
    @src/lib/cn.ts  (helper created in Plan 02)
  </read_first>
  <action>
    Create `src/components/ui/Button.tsx` as a Server Component (no `'use client'`) exporting `Button({ href, variant = 'primary', className, children }: { href: string; variant?: 'primary' | 'secondary'; className?: string; children: React.ReactNode })`. Render an `<a href>` styled as a pill: `rounded-[var(--radius-button)]`, base text 20px / font-semibold (600), horizontal + vertical padding matching the reference CTA, and composed via `cn()` from `@/lib/cn`. Primary variant: background `var(--color-accent)`, text `var(--color-text-primary)`, and a trailing arrow glyph `→` wrapped in `<span aria-hidden="true">` with a 4px (`gap-1`/xs) gap after the label (per UI-SPEC Buttons + Copywriting). Secondary variant: transparent background, `1px` border in `var(--color-accent)` at ~50% opacity (e.g. an rgba/color-mix arbitrary value), text `var(--color-accent)`, and NO arrow. Use only token-referencing utilities — do NOT copy the boilerplate `page.tsx` hardcoded `hover:bg-[#383838]` / `dark:` classes. FOUND-02 requires BOTH variants to exist now even though Header only uses primary (Phase 5's Final CTA consumes secondary). Do not add hover/focus polish beyond a token-based focus-visible ring if trivial; detailed states are refined in Plan 05 screenshot pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/Button.tsx` exports `Button` with the exact prop shape above, defaulting `variant` to `'primary'`, and has NO `'use client'` directive.
    - Primary variant references `var(--color-accent)` for background and renders a trailing `→` inside `aria-hidden="true"`.
    - Secondary variant renders a bordered, transparent-background pill referencing `var(--color-accent)` and renders NO arrow.
    - Radius uses `var(--radius-button)`; no hardcoded hex colors appear in the file.
    - `Button` imports `cn` from `@/lib/cn`.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static assets / markup → browser | Only developer-authored SVG/JSX is rendered; no user input, no `dangerouslySetInnerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `Button` `href` prop | accept | `href` is supplied by developer-controlled callers (nav data), not user input, in this phase; no sanitization needed yet. |
| T-01-05 | Injection (XSS) | `Logo` / `Button` rendering | mitigate | Render children/labels as normal JSX text and inline static SVG only; no `dangerouslySetInnerHTML` anywhere — React auto-escapes, eliminating markup injection. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm run lint` clean.
- Manual: grep both component files confirm no hardcoded hex and no `'use client'`.
- `public/images/qiera/.gitkeep` present.
</verification>

<success_criteria>
Reusable `Logo` (SVG mark + wordmark, a11y name) and `Button` (token-driven primary + secondary pill) exist as Server Components with zero hardcoded colors, and the FOUND-05 raster directory is committed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-03-SUMMARY.md` when done (note whether the SVG mark matched the reference or the raster fallback was used, and whether Header will pass `showTagline`).
</output>
