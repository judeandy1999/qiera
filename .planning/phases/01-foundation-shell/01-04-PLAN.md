---
phase: 01-foundation-shell
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/Footer.test.tsx
  - src/components/layout/MobileNav.test.tsx
autonomous: true
requirements: [FOUND-03]
user_setup: []

must_haves:
  truths:
    - "A sticky dark Header shows the Logo, the v1 nav links, and the Start Intelligence Audit CTA at desktop, collapsing nav + CTA behind a menu toggle at mobile"
    - "A dark Footer shows the same v1 nav set, the © 2026 QIERA copyright, and the three legal links — driven by the same navigation.ts as the Header (no drift)"
    - "The mobile nav opens on the toggle button and closes on Escape, with focus trapped while open (native `<dialog>`); focus return to the toggle is browser-native and verified manually at screenshot widths if jsdom does not assert it"
    - "Neither Header nor Footer renders Pricing, Resources, or Security"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky RSC header (Logo + desktop nav + CTA + MobileNav mount)"
      exports: ["Header"]
    - path: "src/components/layout/Footer.tsx"
      provides: "RSC footer (nav + legal + © 2026)"
      exports: ["Footer"]
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Client island: native <dialog> mobile menu"
      exports: ["MobileNav"]
    - path: "src/components/layout/Footer.test.tsx"
      provides: "Asserts footer renders the same v1 nav set + © 2026 + legal links, no Pricing/Resources/Security"
    - path: "src/components/layout/MobileNav.test.tsx"
      provides: "Asserts toggle opens dialog and Escape closes it"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/data/navigation.ts"
      via: "import { primaryNav, primaryCta }"
      pattern: "from \"@/data/navigation\""
    - from: "src/components/layout/Footer.tsx"
      to: "src/data/navigation.ts"
      via: "import { primaryNav, legalNav }"
      pattern: "from \"@/data/navigation\""
    - from: "src/components/layout/Header.tsx"
      to: "src/components/layout/MobileNav.tsx"
      via: "renders <MobileNav items={primaryNav} />"
      pattern: "MobileNav"
---

<objective>
Build the three shell components (FOUND-03): a sticky dark `Header` (RSC), a dark `Footer` (RSC), and an accessible `MobileNav` client island backed by the native `<dialog>` element. All three consume the same `src/data/navigation.ts`, guaranteeing the Header/Footer nav never drift and Pricing/Resources/Security never appear. Ships two Vitest tests: Footer nav-parity and MobileNav open/Escape.

Purpose: This is the visible shell that every future page inherits. The native `<dialog>` (per RESEARCH Pattern 4) delivers focus trap / Escape / focus restore with zero dependencies, satisfying FOUND-03 acceptance criterion 5. Header is built `sticky` now to avoid Phase 5 rework (Pitfall 5).

Output: `Header.tsx`, `Footer.tsx`, `MobileNav.tsx`, `Footer.test.tsx`, `MobileNav.test.tsx`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-PATTERNS.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/DECISIONS.md
@.cursor/rules/qiera-visual-fidelity.mdc

<interfaces>
<!-- Consumes contracts from Plan 02 (nav data) + Plan 03 (Logo, Button). Creates Header/Footer for Plan 05 wiring. -->

Consumed (already built):
  @/data/navigation  -> primaryNav: NavItem[], primaryCta: NavItem, legalNav: NavItem[]
  @/components/ui/Logo    -> Logo({ className?, showTagline? })
  @/components/ui/Button  -> Button({ href, variant?, className?, children })

Created here (Plan 05 mounts Header/Footer in layout.tsx):
  @/components/layout/Header  -> Header(): JSX.Element   (RSC, sticky top-0 z-40)
  @/components/layout/Footer  -> Footer(): JSX.Element   (RSC)
  @/components/layout/MobileNav -> MobileNav({ items: NavItem[] })  ('use client', native <dialog>)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sticky Header (RSC) with desktop nav + CTA + MobileNav mount</name>
  <files>src/components/layout/Header.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/components/layout/Header.tsx" section — structural pattern, sticky classes, container/padding)
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Header section — sticky top-0 z-40, shell bg, 24px vertical padding, container-max, nav gaps, breakpoints; Header visual hierarchy)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Pitfall 5 — sticky-ready; Pitfall 2 — drive from navigation.ts, never off a screenshot)
  </read_first>
  <action>
    Create `src/components/layout/Header.tsx` as a Server Component (no `'use client'`) exporting `Header()`. Root `<header>` uses `sticky top-0 z-40` with background `var(--color-shell)` (Pitfall 5 — build sticky now to avoid Phase 5 rework). Inner container: `mx-auto`, `max-w-[var(--container-max)]`, `flex items-center justify-between`, with responsive horizontal padding (desktop ≥1280 = 48px, tablet = 32px, mobile = 24px per UI-SPEC) and ~24px vertical padding. Render left→right: `<Logo />` (decide `showTagline` by checking the header reference crop — UI-SPEC only mandates the tagline in the Footer, so default Header to mark+wordmark only unless the reference clearly shows it); a desktop `<nav aria-label="Primary">` (`hidden md:flex`, gap ~16px) mapping `primaryNav` to `<a href={item.href}>` links at 14px/400 in `var(--color-text-primary)`; the primary CTA via `<Button href={primaryCta.href} className="hidden md:inline-flex">{primaryCta.label}</Button>`; and `<MobileNav items={primaryNav} />` (visible only below `md`). Nav links MUST come from `primaryNav` (imported from `@/data/navigation`) — never hardcode the labels off a reference screenshot (Pitfall 2 — the references include Pricing which is locked out). All interactive elements get a visible token-based focus ring (`var(--color-focus-ring)`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Header.tsx` exports `Header`, has NO `'use client'` directive, and imports `primaryNav` + `primaryCta` from `@/data/navigation`.
    - Root element uses `sticky top-0 z-40` and background token `var(--color-shell)`.
    - Desktop nav renders by mapping `primaryNav` (not a hardcoded list) and is hidden below `md`; the primary CTA `Button` is hidden below `md`; `<MobileNav>` is rendered for mobile.
    - No occurrence of "Pricing", "Resources", or "Security" anywhere in the file.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build Footer (RSC) + Footer nav-parity test</name>
  <files>src/components/layout/Footer.tsx, src/components/layout/Footer.test.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/components/layout/Footer.tsx" section — structure, pipe-separator-as-styled-span)
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Footer section — rows, divider, © 2026, legal links; Copywriting Contract — exact strings)
    @.planning/DECISIONS.md  (#10 © 2026; #2/#13/#14 nav set + spelling)
  </read_first>
  <action>
    Create `src/components/layout/Footer.tsx` as a Server Component exporting `Footer()`, background `var(--color-shell)`, inner `max-w-[var(--container-max)]` container with responsive padding matching Header. Row 1: `<Logo showTagline />` (tagline "E-commerce Growth Intelligence" is required in the footer per UI-SPEC) on the left, and a `<nav aria-label="Footer">` on the right mapping `primaryNav` to links at 14px/400. Divider: a `border-t border-[var(--color-border)]` full-width rule. Row 2 (below divider): copyright `© 2026 QIERA. All rights reserved.` (14/400, `var(--color-text-secondary)`, per D-10 — overrides the Footer.png "2025") on the left; a `<ul>` of `legalNav` links (Privacy Policy, Terms of Use, Cookie Policy) on the right, with a pipe separator rendered as `<span aria-hidden="true">|</span>` between items (NOT literal text, so screen readers don't announce the pipe — UI-SPEC a11y requirement). Import `primaryNav` and `legalNav` from `@/data/navigation` (same source as Header — this is the mechanism preventing drift). Then create `src/components/layout/Footer.test.tsx` (Vitest + @testing-library/react) that renders `<Footer />` and asserts: all six `primaryNav` labels are present; the copyright text `© 2026 QIERA. All rights reserved.` is present; the three legal link labels are present; and Pricing/Resources/Security are NOT present (`queryByText` returns null).
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Footer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Footer.tsx` exports `Footer`, has NO `'use client'`, and imports `primaryNav` + `legalNav` from `@/data/navigation`.
    - Copyright renders exactly `© 2026 QIERA. All rights reserved.` (year 2026, per D-10).
    - Legal links render Privacy Policy, Terms of Use, Cookie Policy; the `|` separator is inside `aria-hidden="true"`.
    - `Footer.test.tsx` asserts the six nav labels + copyright + three legal labels present, and Pricing/Resources/Security absent — and passes via `npx vitest run src/components/layout/Footer.test.tsx`.
    - `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build accessible MobileNav (native dialog) + open/Escape test</name>
  <files>src/components/layout/MobileNav.tsx, src/components/layout/MobileNav.test.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/components/layout/MobileNav.tsx" section — base + required deltas: aria labels, 44px targets, panel bg)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Pattern 4 verbatim base; Code Examples → MobileNav.test.tsx; Don't-Hand-Roll — no manual focus trap)
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Mobile navigation section + Copywriting Contract — exact aria-label strings)
  </read_first>
  <behavior>
    - Clicking the toggle button (accessible name "Open menu") calls `dialog.showModal()` and the dialog (accessible name "Mobile navigation") becomes visible.
    - Pressing Escape fires the dialog `cancel`/`close` event, syncs React state to closed, and the dialog is no longer visible.
    - The close button has accessible name "Close menu".
    - Nav links inside the panel come from the `items` prop (primaryNav), not a hardcoded list.
  </behavior>
  <action>
    Create `src/components/layout/MobileNav.tsx` with `'use client'` exporting `MobileNav({ items }: { items: NavItem[] })` (import `NavItem` from `@/data/navigation`). Base it on RESEARCH Pattern 4: `useRef<HTMLDialogElement>`, `useState(open)`, and an effect that calls `dialog.showModal()` when opening and `dialog.close()` when closing; sync state to closed via the dialog's `onClose`/`onCancel` (native Escape) — do NOT hand-roll a `keydown` focus-trap listener (the native `<dialog>` provides focus trap, inert background, Escape, and focus restore). Apply UI-SPEC deltas: the toggle is an icon-only `<button type="button">` with `aria-label="Open menu"`, `aria-haspopup="dialog"`, `aria-expanded={open}`, `aria-controls="mobile-nav"`, a minimum 44×44px hit area, and a visible `var(--color-focus-ring)` focus ring; the `<dialog id="mobile-nav" aria-label="Mobile navigation">` panel background is `var(--color-secondary)` (#10121B, explicit — the default browser dialog is white); a dedicated close `<button aria-label="Close menu">` also ≥44×44px; nav links map `items` stacked vertically at 14–16px, each calling `setOpen(false)` on click. No open/close animation in Phase 1 (UI-SPEC: simplest compliant default is none; any future transition must respect `prefers-reduced-motion`). Then create `src/components/layout/MobileNav.test.tsx` (Vitest + @testing-library/react + @testing-library/user-event) per RESEARCH Code Examples: setup `userEvent`, render with a sample item, click the "menu"/"Open menu" button and assert the dialog is visible, then `user.keyboard('{Escape}')` and assert the dialog is not visible.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/MobileNav.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `MobileNav.tsx` begins with `'use client'` and exports `MobileNav({ items })`.
    - Uses native `<dialog>` via `showModal()`/`close()`; NO manual `keydown`/focus-trap implementation exists in the file.
    - Toggle has `aria-label="Open menu"` + `aria-expanded`; dialog has `aria-label="Mobile navigation"`; close button has `aria-label="Close menu"`; toggle and close have ≥44×44px hit areas.
    - Dialog panel background references `var(--color-secondary)`.
    - Panel links are mapped from the `items` prop (not hardcoded).
    - `npx vitest run src/components/layout/MobileNav.test.tsx` passes (opens on toggle, closes on Escape); `npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer nav data → rendered shell | Header/Footer/MobileNav render only static, typed nav data — no request/user input crosses into these components this phase |
| browser DOM ↔ client island | MobileNav runs client-side JS (state + dialog control) but takes only serializable, developer-controlled props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering (open redirect) | nav `href` values rendered in Header/Footer/MobileNav | accept | All hrefs come from the typed, developer-controlled `navigation.ts`; none derived from request input this phase. Revisit at Phase 7 (`?topic=` params). |
| T-01-07 | Spoofing/Tampering (clickjacking) | native `<dialog>` mobile menu | accept | `<dialog>` renders in the browser top layer and is not itself iframe-embeddable content; site-wide `frame-ancestors`/`X-Frame-Options` is a Phase 10 / deployment concern, not addable in a component here. |
| T-01-05 | Injection (XSS) | link labels / children rendering | mitigate | All labels rendered as JSX text (React auto-escaped); no `dangerouslySetInnerHTML` in any of the three components. |
</threat_model>

<verification>
- `npx vitest run` (full suite: navigation + Footer + MobileNav) green.
- `npx tsc --noEmit` clean; `npm run lint` clean.
- Manual grep: no "Pricing"/"Resources"/"Security" in Header.tsx or Footer.tsx; only MobileNav.tsx has `'use client'`.
</verification>

<success_criteria>
Sticky dark Header, dark Footer (© 2026, legal links), and an accessible native-`<dialog>` MobileNav all render the single-source v1 nav set with zero Pricing/Resources/Security; Footer nav-parity and MobileNav open/Escape tests pass.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-04-SUMMARY.md` when done (note whether Header renders the tagline, and any a11y observations about `<dialog>` focus restore under jsdom).
</output>
