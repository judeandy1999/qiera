---
phase: 01-foundation-shell
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/app/globals.css
  - src/types/navigation.ts
  - src/data/navigation.ts
  - src/data/navigation.test.ts
  - src/lib/cn.ts
autonomous: true
requirements: [FOUND-01, FOUND-02]
user_setup: []

must_haves:
  truths:
    - "Every QIERA design token (shell/secondary/surface/accent/text/border/focus colors, button + card radii, container width) is available as a CSS custom property and a Tailwind utility"
    - "Body text renders in Geist (not a generic sans-serif or Arial), proving the next/font → @theme inline → font-sans chain is intact"
    - "The site no longer flips colors based on the visitor's OS dark-mode preference"
    - "primaryNav contains exactly Intelligence, Process, Solutions, Leverages, Network, Contact — and Header/Footer/MobileNav can all import that one array"
  artifacts:
    - path: "src/app/globals.css"
      provides: "QIERA tokens inside a single @theme inline block; boilerplate dark-mode + Arial removed"
      contains: "--color-shell"
    - path: "src/data/navigation.ts"
      provides: "Single source of truth for primaryNav, primaryCta, legalNav"
      exports: ["primaryNav", "primaryCta", "legalNav"]
    - path: "src/types/navigation.ts"
      provides: "Shared NavItem type"
      contains: "NavItem"
    - path: "src/lib/cn.ts"
      provides: "Tiny conditional-className helper (no clsx dependency)"
      exports: ["cn"]
    - path: "src/data/navigation.test.ts"
      provides: "Locks the exact v1 nav label set, excludes Pricing/Resources/Security"
  key_links:
    - from: "src/data/navigation.ts"
      to: "src/types/navigation.ts"
      via: "import type NavItem"
      pattern: "NavItem"
    - from: "src/app/globals.css @theme inline"
      to: "next/font --font-geist-sans"
      via: "--font-sans: var(--font-geist-sans)"
      pattern: "--font-sans:\\s*var\\(--font-geist-sans\\)"
---

<objective>
Establish the design-token layer and the typed navigation single-source-of-truth that every shell component depends on. Rewrites `globals.css` with QIERA's tokens (FOUND-02), removes the two boilerplate footguns (OS dark-mode override + Arial body font), and creates the `src/data` / `src/types` / `src/lib` structure (FOUND-01) with a test that locks the v1 nav set.

Purpose: Tokens and nav data are the contracts consumed by Logo, Button, Header, Footer, and MobileNav in later waves. Getting the `@theme inline` block right (single block, font chain intact) prevents the silent Geist-fallback pitfall (`01-RESEARCH.md` Pitfall 3). Driving nav from one typed file prevents Header/Footer drift and accidental reintroduction of Pricing/Resources/Security (Pitfall 2, Decision #2).

Output: rewritten `src/app/globals.css`; new `src/types/navigation.ts`, `src/data/navigation.ts`, `src/data/navigation.test.ts`, `src/lib/cn.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DECISIONS.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/phases/01-foundation-shell/01-PATTERNS.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@src/app/globals.css

<interfaces>
<!-- Contracts this plan CREATES for downstream waves (Logo/Button/Header/Footer/MobileNav consume these). -->
<!-- Executor: implement to these signatures exactly. Values sourced from 01-UI-SPEC.md + 01-PATTERNS.md. -->

Tokens to expose (all inside the single existing @theme inline block in globals.css):
  Colors: --color-shell #05060B, --color-secondary #10121B, --color-surface #ffffff,
          --color-ink #0b0d17, --color-accent #6366F1, --color-accent-strong #7C3AED,
          --color-text-primary #F5F5F7, --color-text-secondary #9CA3AF,
          --color-border rgba(255,255,255,0.08), --color-focus-ring #8B7CF6
  Radii:  --radius-button 999px, --radius-card 16px
  Layout: --container-max 1280px
  Fonts (KEEP existing): --font-sans var(--font-geist-sans), --font-mono var(--font-geist-mono)

Navigation exports (src/data/navigation.ts):
  type NavItem = { label: string; href: string }
  primaryNav: NavItem[]   // Intelligence /#intelligence, Process /#process, Solutions /#solutions,
                          //   Leverages /#leverages, Network /network, Contact /contact
  primaryCta: NavItem     // { label: 'Start Intelligence Audit', href: '/start-intelligence-audit' }
  legalNav: NavItem[]     // Privacy Policy /privacy, Terms of Use /terms, Cookie Policy /cookies

Helper (src/lib/cn.ts):
  cn(...classes: Array<string | false | null | undefined>): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css with QIERA tokens (delete OS dark-mode + Arial)</name>
  <files>src/app/globals.css</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/app/globals.css" section — exact @theme inline values to use, and the "What to keep / delete / add" list)
    @.planning/phases/01-foundation-shell/01-UI-SPEC.md  (Color + Layout & Component Contracts tables — the authoritative token values)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Pitfall 1 + Pitfall 3 — why the media query and second @theme block are forbidden)
    @src/app/globals.css  (current 27-line boilerplate)
  </read_first>
  <action>
    Rewrite `src/app/globals.css` keeping `@import "tailwindcss";` and the EXISTING single `@theme inline` block's font lines (`--font-sans: var(--font-geist-sans)`, `--font-mono: var(--font-geist-mono)`). Into that SAME `@theme inline` block (never a second `@theme` block — Pitfall 3 causes a silent Geist→sans-serif fallback), add the QIERA tokens with the exact values from `01-UI-SPEC.md` / `01-PATTERNS.md`: colors `--color-shell #05060B`, `--color-secondary #10121B`, `--color-surface #ffffff`, `--color-ink #0b0d17`, `--color-accent #6366F1`, `--color-accent-strong #7C3AED`, `--color-text-primary #F5F5F7`, `--color-text-secondary #9CA3AF`, `--color-border rgba(255,255,255,0.08)`, `--color-focus-ring #8B7CF6`; radii `--radius-button 999px`, `--radius-card 16px`; layout `--container-max 1280px`. DELETE the entire `@media (prefers-color-scheme: dark)` block (Pitfall 1 — QIERA's dark shell / light content split is a fixed brand design, not an OS toggle) and DELETE the `font-family: Arial, Helvetica, sans-serif;` line in `body` (it overrides the Geist chain). Set `body` background to `var(--color-surface)` and color to `var(--color-ink)`. Treat all hex values as `[ASSUMED]` samples per `qiera-visual-fidelity.mdc` — they are screenshot-verified later in Plan 05, not final here. Do NOT hardcode these hexes anywhere in component files later; components must reference the tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contains exactly ONE `@theme` / `@theme inline` block.
    - That block defines `--color-shell`, `--color-secondary`, `--color-accent`, `--color-accent-strong`, `--color-text-primary`, `--color-text-secondary`, `--color-border`, `--color-focus-ring`, `--radius-button`, `--radius-card`, `--container-max`, AND still defines `--font-sans: var(--font-geist-sans)`.
    - No `@media (prefers-color-scheme: dark)` block remains.
    - No `Arial` / `Helvetica` font-family declaration remains in `body`.
    - `npm run lint` and `npx tsc --noEmit` pass.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed navigation single-source (data + types) with lock test</name>
  <files>src/types/navigation.ts, src/data/navigation.ts, src/data/navigation.test.ts</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/data/navigation.ts" + "src/types/navigation.ts" sections — verbatim structure and the note to keep NavItem re-exported)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Code Examples → navigation.ts + navigation.test.ts)
    @.planning/DECISIONS.md  (#2 omit Pricing/Resources/Security; #13 "Leverages"; #14 "Contact")
  </read_first>
  <behavior>
    - `primaryNav.map(i => i.label)` equals exactly ['Intelligence','Process','Solutions','Leverages','Network','Contact'] in that order.
    - `primaryNav` labels do NOT include 'Pricing', 'Resources', or 'Security' (per D-02).
    - Label spelling is 'Leverages' (not 'Leverage', per D-13) and 'Contact' (not 'Contact Us', per D-14).
    - `primaryCta.label === 'Start Intelligence Audit'` and `href === '/start-intelligence-audit'`.
    - `legalNav.map(i => i.label)` equals ['Privacy Policy','Terms of Use','Cookie Policy'].
  </behavior>
  <action>
    Create `src/types/navigation.ts` exporting `type NavItem = { label: string; href: string }`. Create `src/data/navigation.ts` importing `NavItem` (type-only import) and re-exporting it (so consumers can `import { NavItem } from "@/data/navigation"` without knowing about the split, per PATTERNS note), then exporting: `primaryNav: NavItem[]` = Intelligence `/#intelligence`, Process `/#process`, Solutions `/#solutions`, Leverages `/#leverages`, Network `/network`, Contact `/contact` (per D-02/D-13/D-14 — the locked spelling overrides the Footer.png reference which shows "Leverage"/"Contact Us"); `primaryCta: NavItem` = { label: 'Start Intelligence Audit', href: '/start-intelligence-audit' }; `legalNav: NavItem[]` = Privacy Policy `/privacy`, Terms of Use `/terms`, Cookie Policy `/cookies`. The hash hrefs are forward-looking anchors for Phase 5 section IDs (Assumption A3 — harmless no-op until then); the `/network`, `/contact`, `/start-intelligence-audit` routes are built in later phases and 404 harmlessly in the interim (this is NOT a "dead link" in the D-02 sense, which is specifically about Pricing/Resources/Security). Then create `src/data/navigation.test.ts` (Vitest) asserting the behaviors above using `toEqual` on the label arrays plus explicit `not.toContain` for Pricing/Resources/Security.
  </action>
  <verify>
    <automated>npx vitest run src/data/navigation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/data/navigation.test.ts` passes.
    - `src/data/navigation.ts` exports `primaryNav`, `primaryCta`, `legalNav`, and re-exports the `NavItem` type.
    - `src/types/navigation.ts` exports `NavItem`.
    - The test explicitly asserts the exact 6-label order AND `not.toContain` for 'Pricing', 'Resources', 'Security'.
    - `npx tsc --noEmit` passes (strict TypeScript, no `any`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create the cn() className helper</name>
  <files>src/lib/cn.ts</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/lib/cn.ts" section — exact shape)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Alternatives Considered — why NOT to install clsx in Phase 1)
  </read_first>
  <action>
    Create `src/lib/cn.ts` exporting `cn(...classes: Array<string | false | null | undefined>): string` that filters falsy values and joins the rest with a single space. Do NOT install `clsx` or `tailwind-merge` (Decision: no unnecessary packages; Phase 1's conditional-class needs are a couple of boolean toggles — a ~3-line helper is justified over a dependency). This helper is consumed by `Button.tsx` (variant switching) in Plan 03.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/cn.ts` exports a `cn` function with the exact signature above.
    - Falsy inputs (`false`, `null`, `undefined`, `''`) are excluded from the output string.
    - No new dependency added to `package.json`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored data → rendered nav | Nav labels/hrefs originate only from a typed, developer-controlled file — no request/user input crosses into nav rendering in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering (open redirect) | `href` values in `navigation.ts` | accept | All hrefs are hard-coded developer strings, not derived from any request input; nothing to mitigate this phase. Revisit in Phase 7 when `/contact?topic=partnership` constructs hrefs from user-influenceable params. |
| T-01-03 | Information disclosure | `globals.css` tokens | accept | Design tokens are public brand values with no secret content; shipping them in CSS is expected. |
</threat_model>

<verification>
- `npx vitest run src/data/navigation.test.ts` green.
- `npx tsc --noEmit` clean.
- `npm run lint` clean.
- Manual: grep `globals.css` confirms one `@theme` block, no `prefers-color-scheme`, no `Arial`.
</verification>

<success_criteria>
QIERA tokens live in a single `@theme inline` block with the Geist chain intact; the typed nav single-source exports the locked v1 link set (test-enforced); `cn()` exists with no new dependency.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-02-SUMMARY.md` when done (record final token values used and confirm whether NavItem was split to `src/types` or co-located).
</output>
