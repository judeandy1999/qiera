---
phase: 01-foundation-shell
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.mts
autonomous: false
requirements: [FOUND-01]
user_setup: []

must_haves:
  truths:
    - "Running `npx vitest run` starts the runner and reports 'no tests found' (or runs any present tests) without a config/resolution error"
    - "package.json exposes a `test` script that runs the suite once (no watch mode)"
    - "The existing Next.js 16 / React 19 / Tailwind v4 scaffold is unchanged except for added devDependencies and one script (no reinit, no runtime deps added)"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Vitest config with jsdom env + @/* alias + React JSX transform"
      contains: "environment: 'jsdom'"
    - path: "package.json"
      provides: "Dev-only test toolchain + test script"
      contains: "vitest"
  key_links:
    - from: "vitest.config.mts"
      to: "tsconfig.json paths (@/*)"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths\\(\\)"
---

<objective>
Stand up the Wave 0 test infrastructure so every downstream plan can attach an automated `<verify>` command. Installs the Next.js-documented Vitest + React Testing Library toolchain (dev-only), creates `vitest.config.mts`, and adds a `test` script — without touching the locked runtime scaffold.

Purpose: The repo has zero test infrastructure. Per `01-RESEARCH.md` Validation Architecture and `01-VALIDATION.md` Wave 0 Requirements, this must exist before nav/MobileNav/Footer tests in later waves can run. `vitest` was flagged `[SUS]` (typosquat-of-`vite` false positive) by slopcheck, so a blocking human-verify gate precedes the install per the Package Legitimacy Protocol.

Output: `vitest.config.mts`, updated `package.json` (devDependencies + `test` script), `package-lock.json`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/DECISIONS.md
@.planning/phases/01-foundation-shell/01-RESEARCH.md
@.planning/phases/01-foundation-shell/01-VALIDATION.md
@package.json
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Human-verify `vitest` package legitimacy before install</name>
  <what-built>
    Nothing installed yet. This gate confirms the package identities BEFORE `npm install -D` runs, because `01-RESEARCH.md` Package Legitimacy Audit flagged `vitest` as `[SUS]` — slopcheck reported `TYPOSQUAT_RISK` ("Suspiciously close to 'vite'"). Research evidence (4.5-year history, 400+ versions, same `vitest-dev`/Vite org, and it is the runner named in this repo's own shipped Next.js docs at `node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md`) indicates a false positive, but the protocol requires an explicit human confirmation before adding it to `package.json`.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vitest and confirm: publisher/org is `vitest-dev` / Vitest team, repository is `github.com/vitest-dev/vitest`, weekly downloads are in the millions, and it is NOT a lookalike (not `vite`, not `vi-test`, not `vitest-cli`, etc.).
    2. Confirm the exact package spelling to install is `vitest` (not `vite`).
    3. Optionally spot-check the supporting packages on npmjs.com: `@vitejs/plugin-react`, `vite-tsconfig-paths`, `jsdom`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/user-event`, `@testing-library/jest-dom` — all are long-lived, well-known testing packages ([OK] in the audit).
  </how-to-verify>
  <verify>
    <automated>MANUAL — package legitimacy confirmation is human-only; never auto-approved (see T-01-SC in threat_model).</automated>
  </verify>
  <acceptance_criteria>
    - Human has viewed npmjs.com/package/vitest and confirmed publisher `vitest-dev` + repo `github.com/vitest-dev/vitest`.
    - Human has explicitly approved installing the package spelled exactly `vitest`.
    - This checkpoint is NOT auto-advanced regardless of any `auto_advance` setting.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dev-only test toolchain, create vitest.config.mts, add test script</name>
  <files>package.json, package-lock.json, vitest.config.mts</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (Standard Stack → Supporting table for exact package list; Code Examples → "Minimal vitest.config.mts for this project")
    @package.json  (do not touch `dependencies`; only add `devDependencies` + one script)
    @tsconfig.json  (confirm the `@/*` → `./src/*` path alias that vite-tsconfig-paths must honor)
  </read_first>
  <action>
    After the Task 1 gate is approved, run a single dev-only install (per FOUND-01: extend the scaffold, do NOT reinitialize): `npm install -D vitest @vitejs/plugin-react vite-tsconfig-paths jsdom @testing-library/react @testing-library/dom @testing-library/user-event @testing-library/jest-dom`. These are ALL devDependencies — Phase 1 adds zero runtime/production dependencies (native `<dialog>`, `next/font`, `next/image`, and a hand-written `cn()` cover every UI need per RESEARCH Don't-Hand-Roll). Create `vitest.config.mts` at the project root using `defineConfig` from `vitest/config`, registering plugins `tsconfigPaths()` and `react()` (order per the shipped Next.js Vitest doc) and setting `test.environment` to `'jsdom'`. Add a `"test": "vitest run"` script to `package.json` (use `vitest run` — the one-shot mode — never a watch flag, per `01-VALIDATION.md` "No watch-mode flags"). Do NOT run `git commit` (`commit_docs: false`, Decision — user commits manually).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits without a configuration/module-resolution error (reporting "no test files found" is an acceptable pass at this point — no test files exist yet in this plan).
    - `package.json` `devDependencies` contains all eight packages listed above; `dependencies` still lists only `next`, `react`, `react-dom` (unchanged).
    - `package.json` `scripts.test` equals `"vitest run"` (no `--watch`).
    - `vitest.config.mts` exists at repo root, imports `@vitejs/plugin-react` and `vite-tsconfig-paths`, and sets `environment: 'jsdom'`.
    - No changes to `next.config.ts`, `tsconfig.json`, or any `src/` file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev toolchain | Third-party package code is installed and executed at test time (untrusted supply chain crosses here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install -D` of the test toolchain (esp. `vitest` [SUS]) | mitigate | Blocking human-verify gate (Task 1) confirms `vitest` publisher/repo on npmjs.com before install; audit in `01-RESEARCH.md` shows all other packages [OK]. Checkpoint is never auto-approved. |
| T-01-01 | Tampering | `vitest.config.mts` (dev-only, not shipped to prod) | accept | Config is developer-authored, runs only in the local/CI test environment, and is not part of the deployed marketing site bundle — no production attack surface. |
</threat_model>

<verification>
- `npx vitest run` runs cleanly (no config error).
- `npm run lint` still passes (no new lint errors introduced by config file).
- `dependencies` in `package.json` unchanged from the locked scaffold.
</verification>

<success_criteria>
Vitest runs via `npm test` / `npx vitest run` with jsdom + `@/*` alias support, using only dev dependencies, leaving the runtime scaffold untouched.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-01-SUMMARY.md` when done (note whether `Geist_Mono` keep/drop decision is deferred to Plan 05, and record the installed package versions).
</output>
