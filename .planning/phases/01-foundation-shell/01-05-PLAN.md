---
phase: 01-foundation-shell
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements: [FOUND-01, FOUND-04]
user_setup: []

must_haves:
  truths:
    - "Every route renders the shared Header above and Footer below its page content"
    - "The document title/description are QIERA's, not 'Create Next App'"
    - "Geist remains the loaded, rendered font family (FOUND-04)"
    - "The homepage shows a neutral placeholder — no create-next-app boilerplate, no next.svg/vercel.svg, no dark: variants"
    - "Full suite + lint + type-check pass, and the shell is screenshot-verified against references at 1440/768/390"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout wiring Header/Footer + QIERA metadata, Geist preserved"
      contains: "Header"
    - path: "src/app/page.tsx"
      provides: "Minimal placeholder homepage (boilerplate removed)"
      contains: "QIERA"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import { Header }"
      pattern: "from \"@/components/layout/Header\""
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import { Footer }"
      pattern: "from \"@/components/layout/Footer\""
---

<objective>
Wire the shell into the app and gate the phase: mount `Header`/`Footer` in `layout.tsx` with QIERA metadata while preserving the Geist `next/font` wiring (FOUND-04), replace the create-next-app homepage with a minimal placeholder, then run the full verification (tests + lint + type-check) and a blocking screenshot comparison against the references at 1440/768/390 (FOUND-01 acceptance criterion 6 + `qiera-visual-fidelity.mdc`).

Purpose: Until layout wires Header/Footer, the shell isn't visible on any route. The boilerplate `page.tsx` must be removed (its `dark:` variants + `next.svg`/`vercel.svg` conflict with the fixed-brand design and asset rules). The screenshot checkpoint is where "matches the dark-shell references" is actually proven — the project rule forbids claiming visual fidelity without compared screenshots.

Output: rewritten `src/app/layout.tsx`, `src/app/page.tsx`; a verified, screenshot-compared Phase 1 shell.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-shell/01-PATTERNS.md
@.planning/phases/01-foundation-shell/01-UI-SPEC.md
@.planning/DECISIONS.md
@.cursor/rules/qiera-visual-fidelity.mdc
@src/app/layout.tsx
@src/app/page.tsx

<interfaces>
<!-- Consumes finished shell from Plan 04. -->
  @/components/layout/Header -> Header()
  @/components/layout/Footer -> Footer()
Existing (KEEP unchanged): Geist/Geist_Mono next/font wiring + <html className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Header/Footer + QIERA metadata into layout; replace boilerplate page</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-PATTERNS.md  ("src/app/layout.tsx" + "src/app/page.tsx" sections — exact target shapes, what to keep/change/delete)
    @src/app/layout.tsx  (current — preserve the Geist block + html className chain)
    @src/app/page.tsx  (current boilerplate — to be fully replaced)
    @.planning/phases/01-foundation-shell/01-RESEARCH.md  (metadata via export const metadata; Pitfall 1 — no dark: variants)
  </read_first>
  <action>
    Edit `src/app/layout.tsx`: KEEP the entire `Geist`/`Geist_Mono` `next/font/google` wiring and the `<html>` `className` chain unchanged (FOUND-04 — Geist stays; this is already the documented pattern). CHANGE the `metadata` export to QIERA values — title `"QIERA — E-commerce Growth Intelligence"` and a QIERA description (per ROADMAP "Wire root layout.tsx metadata for QIERA"; use the Next.js `Metadata` object, not manual `<head>` tags). Import `Header` from `@/components/layout/Header` and `Footer` from `@/components/layout/Footer` and wrap: `<Header />`, then `<main className="flex-1">{children}</main>`, then `<Footer />` inside the existing `<body className="min-h-full flex flex-col">` (the existing flex classes already support sticky-header + growing main + footer — no change to them). Then fully REWRITE `src/app/page.tsx` to a minimal placeholder Server Component: a centered container with a single neutral line like "QIERA homepage — sections arriving in later phases." Delete ALL boilerplate: the `next/image` import, `next.svg`/`vercel.svg`, external Vercel/Next links, and every `dark:` variant class (Pitfall 1 — no OS-driven dark mode). Homepage sections are Phases 2–5 — do NOT build Hero/Intelligence/etc. here. Do NOT run `git commit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` imports and renders `Header` and `Footer`, with `{children}` inside a `<main className="flex-1">`.
    - `metadata.title` contains "QIERA" (no "Create Next App"); the Geist `next/font` wiring and `<html>` className chain are unchanged.
    - `page.tsx` contains NO `next/image` import, NO `next.svg`/`vercel.svg`, NO external vercel.com/nextjs.org links, and NO `dark:` classes.
    - `npx tsc --noEmit`, `npm run lint`, and `npx vitest run` (full suite) all pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run full phase verification (suite + lint + type-check) and capture screenshots</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    @.planning/phases/01-foundation-shell/01-VALIDATION.md  (Sampling Rate → per-wave-merge + phase-gate commands)
    @.cursor/rules/qiera-visual-fidelity.mdc  (verification workflow steps 4–9)
  </read_first>
  <action>
    Run the phase gate commands from `01-VALIDATION.md`: `npx vitest run` (full suite — navigation + Footer + MobileNav all green), `npm run lint` (clean), `npx tsc --noEmit` (clean). Then start the dev server (`npm run dev`) and capture screenshots of the rendered shell (Header + placeholder + Footer) at the three widths mandated by ROADMAP Phase 1 and UI-SPEC: 1440, 768, 390. Save/collect the screenshots for the Task 3 comparison. Inspect the browser console for errors/warnings and note any. This task produces the evidence the checkpoint reviews; it does not itself sign off. If any command fails, fix the underlying component (in its own file) and re-run before proceeding — but note that structural fixes to Header/Footer/MobileNav belong to their Plan 04 files, so a failure here usually means a wiring bug in layout/page.
  </action>
  <verify>
    <automated>npx vitest run && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` reports all tests passing (navigation, Footer, MobileNav).
    - `npm run lint` and `npx tsc --noEmit` report zero errors.
    - Screenshots captured at 1440, 768, and 390 of the running shell.
    - Any browser console errors/warnings are recorded (for the summary), or confirmed none.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Screenshot comparison vs references (visual fidelity sign-off)</name>
  <what-built>
    The full Phase 1 shell is wired and running: sticky dark Header (Logo + v1 nav + Start Intelligence Audit CTA, mobile menu below md), a minimal placeholder homepage, and the dark Footer (© 2026, legal links). Automated suite + lint + type-check are green, and screenshots were captured at 1440/768/390 in Task 2.
  </what-built>
  <how-to-verify>
    1. Compare the 1440 Header screenshot against `docs/qiera-reference/Homepage/75976647-1ef2-4aac-8fab-fcbd143601f5.png` and `docs/qiera-reference/Homepage/top section.png` (header + CTA/token cross-check). Confirm: dark shell background, purple Q logo, nav order Intelligence · Process · Solutions · Leverages · Network · Contact, purple pill CTA "Start Intelligence Audit" with trailing arrow. Confirm Pricing / Resources / Security are ABSENT (intentional per Decision #2 — the reference shows Pricing; our shell must not).
    2. Compare the 1440 Footer screenshot against `docs/qiera-reference/Homepage/Footer.png`. Confirm: same v1 nav set (spelled "Leverages"/"Contact", NOT "Leverage"/"Contact Us"), `© 2026 QIERA. All rights reserved.` (NOT 2025), and Privacy Policy | Terms of Use | Cookie Policy.
    3. At 768 and 390: confirm no horizontal overflow, the desktop nav + CTA collapse behind the menu toggle, the toggle opens the mobile panel, Escape closes it, focus is visible on the toggle, and text is not clipped.
    4. Confirm body text renders in Geist (not a generic sans-serif) and the page does not invert with OS dark mode.
    5. List any remaining visual differences honestly (do not claim pixel-perfection without this comparison).
  </how-to-verify>
  <verify>
    <automated>MANUAL — pixel/visual fidelity comparison is human-only per qiera-visual-fidelity.mdc (not unit-testable).</automated>
  </verify>
  <acceptance_criteria>
    - Header at 1440 matches the dark-shell reference EXCEPT Pricing/Resources/Security are absent (intentional).
    - Footer at 1440 matches Footer.png with the locked overrides applied (© 2026, "Leverages", "Contact", legal links).
    - 768 and 390 show no horizontal scroll, no clipped text, and a working keyboard-operable mobile menu with visible focus.
    - Geist confirmed loaded; no OS-dark-mode inversion.
    - Remaining differences are listed honestly in the summary; no unqualified "pixel-perfect" claim.
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 1, or list the visual differences to fix first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| framework head management → document metadata | Metadata is developer-authored and resolved server-side; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | root `layout.tsx` metadata / head | accept | Metadata values are static developer strings via the Next.js `Metadata` object (SSR-safe, deduplicated); no user-controlled head injection surface. |
| T-01-09 | Information disclosure | placeholder `page.tsx` | accept | Placeholder renders a single static neutral string; no data, secrets, or user input present. |
</threat_model>

<verification>
- Full suite `npx vitest run` green; `npm run lint` + `npx tsc --noEmit` clean.
- Screenshots at 1440/768/390 compared to header/footer references; differences listed.
- Manual: `layout.tsx` still contains the untouched Geist block; `page.tsx` has no boilerplate/`dark:`/external links.
</verification>

<success_criteria>
The shared Header/Footer render on every route with QIERA metadata and preserved Geist; the homepage is a clean placeholder; the automated gate is green; and the shell has been screenshot-compared against the references at all three widths with an honest difference list.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-shell/01-05-SUMMARY.md` when done: record the Geist_Mono keep/drop decision, the container-max value used, any token hexes corrected during the screenshot pass, the remaining visual-difference list, and confirmation that no git commit was made (user commits manually).
</output>
