---
phase: 05-cta-homepage-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/final-cta.ts
  - src/data/final-cta.ts
  - src/components/sections/FinalCtaSection.tsx
  - src/components/sections/FinalCtaSection.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-01, HOME-08]
user_setup: []

must_haves:
  truths:
    - "Homepage order is Hero → Intelligence → Process → Solutions → Leverages → Final CTA"
    - "Final CTA dark band renders exact UI-SPEC strings with primary/secondary Button hrefs"
    - "Section has id=final-cta and aria-labelledby pointing at final-cta-heading"
  artifacts:
    - path: "src/types/final-cta.ts"
      provides: "FinalCtaContent type"
      exports: ["FinalCtaContent", "FinalCtaLink"]
    - path: "src/data/final-cta.ts"
      provides: "Typed Final CTA copy + hrefs"
      exports: ["finalCta"]
      contains: "Begin with clarity. Grow with intelligence."
    - path: "src/components/sections/FinalCtaSection.tsx"
      provides: "RSC Final CTA band"
      exports: ["FinalCtaSection"]
      min_lines: 40
    - path: "src/components/sections/FinalCtaSection.test.tsx"
      provides: "HOME-08 string/href/a11y assertions"
    - path: "src/app/page.tsx"
      provides: "FinalCtaSection after Leverages"
      contains: "FinalCtaSection"
  key_links:
    - from: "src/components/sections/FinalCtaSection.tsx"
      to: "src/data/final-cta.ts"
      via: "import finalCta"
      pattern: "finalCta"
    - from: "src/components/sections/FinalCtaSection.tsx"
      to: "src/components/ui/Button.tsx"
      via: "Button primary + secondary"
      pattern: "variant=\"(primary|secondary)\""
    - from: "src/app/page.tsx"
      to: "src/components/sections/FinalCtaSection.tsx"
      via: "render after LeveragesSection"
      pattern: "FinalCtaSection"
---

<objective>
Ship typed Final CTA data and an RSC FinalCtaSection wired after Leverages on the homepage (HOME-01 order + HOME-08 dark band), using only locked reference strings and existing Button variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cta-homepage-polish/05-CONTEXT.md
@.planning/phases/05-cta-homepage-polish/05-UI-SPEC.md
@.planning/REQUIREMENTS.md
@src/components/ui/Button.tsx
@src/app/page.tsx
@src/components/sections/Hero.tsx

**Research note:** `05-RESEARCH.md` was missing at plan time — plan assumes CONTEXT + UI-SPEC + existing shell patterns (Level 0). No new packages.

<interfaces>
From src/components/ui/Button.tsx:
```typescript
type ButtonProps = {
  href: string;
  variant?: "primary" | "secondary";
  className?: string;
  children: ReactNode;
};
export function Button({ href, variant = "primary", className, children }: ButtonProps);
// primary appends aria-hidden → after children
```

From 05-UI-SPEC.md data shape (lock exactly):
```typescript
export type FinalCtaLink = { label: string; href: string };
export type FinalCtaContent = {
  eyebrow: string;
  title: string;
  body: string;
  primary: FinalCtaLink;
  secondary: FinalCtaLink;
  caption: string;
};
```

Exact strings (D-01, D-03–D-05; UI-SPEC Copywriting Contract):
- eyebrow: `START AN INTELLIGENCE AUDIT`
- title: `Begin with clarity. Grow with intelligence.`
- body: `Our Intelligence Audit identifies what's holding your eCommerce growth back, uncovers your highest-impact opportunities, and gives you a clear roadmap for what to do next.`
- primary.label: `Start Intelligence Audit` → href `/start-intelligence-audit`
- secondary.label: `Contact` → href `/contact`
- caption: `Takes less than 2 minutes`
Forbidden: `Start My Intelligence Audit`; inventing/paraphrasing copy (D-01, D-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + final-cta data with exact UI-SPEC strings</name>
  <files>src/types/final-cta.ts, src/data/final-cta.ts</files>
  <read_first>
    @.planning/phases/05-cta-homepage-polish/05-UI-SPEC.md (Copywriting Contract + data shape)
    @.planning/phases/05-cta-homepage-polish/05-CONTEXT.md (D-01–D-06)
    @src/types/process.ts
    @src/data/navigation.ts
  </read_first>
  <action>
    Create `src/types/final-cta.ts` exporting `FinalCtaLink` and `FinalCtaContent` matching the UI-SPEC shape. Create `src/data/final-cta.ts` exporting `finalCta: FinalCtaContent` with the EXACT strings and hrefs listed in context interfaces (per D-01, D-03, D-04, D-05). Include both eyebrow and caption (UI-SPEC resolves CONTEXT uncertainty #2). Do not invent alternate copy. Do not use `Start My Intelligence Audit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Types export FinalCtaContent / FinalCtaLink.
    - finalCta contains every UI-SPEC string verbatim and primary/secondary hrefs `/start-intelligence-audit` and `/contact`.
    - tsc exits 0.
  </acceptance_criteria>
  <done>Typed finalCta data exists with exact UI-SPEC strings and locked hrefs; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: FinalCtaSection RSC + page wire + Vitest</name>
  <files>src/components/sections/FinalCtaSection.tsx, src/components/sections/FinalCtaSection.test.tsx, src/app/page.tsx</files>
  <read_first>
    @src/data/final-cta.ts
    @src/components/ui/Button.tsx
    @src/components/sections/Hero.tsx
    @src/app/page.tsx
    @.planning/phases/05-cta-homepage-polish/05-UI-SPEC.md (Layout Contract)
    @src/components/sections/LeveragesSection.test.tsx
  </read_first>
  <action>
    Create Server Component `FinalCtaSection` (no `"use client"`) per D-07–D-11 and UI-SPEC Layout: `id="final-cta"`, `scroll-mt-[72px]` (or shared token if already present — do not invent a new offset here; Wave 2 owns unify), `bg-[var(--color-shell)]`, container `mx-auto max-w-[var(--container-max)]` with `px-6 md:px-8 lg:px-12`, vertical padding ~`py-12 md:py-16` (48/64px ASSUMED). Desktop lg+: 2-col grid copy | CTA cluster `items-center`; below lg: flex-col gap-8. Eyebrow: 12px / 400 / uppercase / tracking 0.08em / accent. H2 id=`final-cta-heading` with title string; section `aria-labelledby="final-cta-heading"`. Body secondary text. CTA cluster: `Button` primary + secondary with data labels/hrefs (Button appends → on primary — do not duplicate arrow in label). Caption under primary/cluster at ~14px muted secondary. Omit waves/stripes/icons (D-11). Wire in `page.tsx` immediately after `LeveragesSection` (D-16); keep Home as RSC. Write `FinalCtaSection.test.tsx` asserting: exact eyebrow/title/body/caption text; primary link href `/start-intelligence-audit` and accessible name includes `Start Intelligence Audit`; secondary href `/contact` and name `Contact`; `#final-cta` present; `aria-labelledby` equals `final-cta-heading`.
  </action>
  <verify>
    <automated>npx vitest run src/components/sections/FinalCtaSection.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - FinalCtaSection is RSC; uses Button primary+secondary; exact strings; id/aria-labelledby correct.
    - page.tsx order: Hero, Intelligence, Process, Solutions, Leverages, FinalCtaSection.
    - Vitest file green; tsc exits 0.
  </acceptance_criteria>
  <done>FinalCtaSection wired after Leverages; Vitest proves strings, hrefs, id=final-cta, aria-labelledby.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| static typed marketing data → RSC | No user input; hrefs are app-internal paths |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-05-01 | Tampering | accept | Public marketing copy; no secrets |
| T-05-02 | Elevation | accept | Links to future form routes only; no privileged actions |
| T-05-SC | Tampering | mitigate | No new npm packages |
</threat_model>

<verification>
- Vitest FinalCtaSection green; tsc clean; page order includes Final CTA after Leverages
</verification>

<success_criteria>
HOME-08 Final CTA is data-driven dark band with primary/secondary buttons and exact reference strings; HOME-01 homepage order includes Final CTA before Footer (layout Footer unchanged).
</success_criteria>

<output>
Create `.planning/phases/05-cta-homepage-polish/05-01-SUMMARY.md` when done. Do not git commit (commit_docs: false).
</output>
