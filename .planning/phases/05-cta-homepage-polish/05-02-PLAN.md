---
phase: 05-cta-homepage-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/globals.css
  - src/components/sections/IntelligenceSection.tsx
  - src/components/sections/ProcessSection.tsx
  - src/components/sections/SolutionsSection.tsx
  - src/components/sections/LeveragesSection.tsx
  - src/components/sections/FinalCtaSection.tsx
  - src/app/homepage-anchors.test.tsx
  - src/data/navigation.test.ts
autonomous: true
requirements: [HOME-02]
user_setup: []

must_haves:
  truths:
    - "Hash targets intelligence/process/solutions/leverages clear the sticky header via unified scroll-mt"
    - "final-cta uses the same scroll-mt token as other homepage sections"
    - "primaryNav hash hrefs still match section IDs; smoke test proves IDs exist on Home"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Optional --header-scroll-offset + reduced-motion-safe smooth scroll"
    - path: "src/app/homepage-anchors.test.tsx"
      provides: "Section id + order smoke on Home"
    - path: "src/components/sections/FinalCtaSection.tsx"
      provides: "scroll-mt aligned with peers"
  key_links:
    - from: "src/data/navigation.ts"
      to: "section ids on homepage"
      via: "/#intelligence … /#leverages"
      pattern: "/#(intelligence|process|solutions|leverages)"
    - from: "homepage sections"
      to: "sticky Header"
      via: "scroll-mt / --header-scroll-offset"
      pattern: "scroll-mt|header-scroll-offset"
---

<objective>
Polish HOME-02 hash navigation: measure sticky header offset, unify `scroll-mt` across Intelligence/Process/Solutions/Leverages/Final CTA, and add Vitest smoke that section IDs exist and nav hash hrefs match (D-12–D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cta-homepage-polish/05-CONTEXT.md
@.planning/phases/05-cta-homepage-polish/05-UI-SPEC.md
@.planning/phases/05-cta-homepage-polish/05-01-SUMMARY.md
@src/components/layout/Header.tsx
@src/data/navigation.ts
@src/app/globals.css

**Research note:** `05-RESEARCH.md` missing — assume UI-SPEC Hash contract + existing `scroll-mt-[72px]` pattern.

<interfaces>
Header (sticky):
```typescript
<header className="sticky top-0 z-40 bg-[var(--color-shell)]">
  <div className="mx-auto flex max-w-[var(--container-max)] items-center justify-between px-6 py-6 md:px-8 lg:px-12">
```

Nav hashes to keep (D-12) — do not add Final CTA to primary nav:
```typescript
{ label: "Intelligence", href: "/#intelligence" },
{ label: "Process", href: "/#process" },
{ label: "Solutions", href: "/#solutions" },
{ label: "Leverages", href: "/#leverages" },
```

Section IDs (UI-SPEC): `intelligence`, `process`, `solutions`, `leverages`, `final-cta`.
Today all four prior sections use `scroll-mt-[72px]`. Prefer `--header-scroll-offset: 72px` (or measured value) if changing multiple files.
Do NOT implement scroll-spy / active-nav-on-scroll (D-14 deferred).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure header offset and unify scroll-mt</name>
  <files>src/app/globals.css, src/components/sections/IntelligenceSection.tsx, src/components/sections/ProcessSection.tsx, src/components/sections/SolutionsSection.tsx, src/components/sections/LeveragesSection.tsx, src/components/sections/FinalCtaSection.tsx</files>
  <read_first>
    @src/components/layout/Header.tsx
    @src/app/globals.css
    @.planning/phases/05-cta-homepage-polish/05-UI-SPEC.md (Hash / Sticky Nav Contract)
    @src/components/sections/IntelligenceSection.tsx
    @src/components/sections/FinalCtaSection.tsx
  </read_first>
  <action>
    With `npm run dev` running, measure real sticky Header height at 1440 / 768 / 390 (Playwright one-shot `boundingBox`/`getBoundingClientRect` or equivalent — do not add package.json deps). Document measured heights in 05-02-SUMMARY. If 72px is insufficient or drifts, set a single CSS custom property on `:root` (e.g. `--header-scroll-offset: {N}px`) and replace hard-coded `scroll-mt-[72px]` on Intelligence, Process, Solutions, Leverages, and FinalCta with `scroll-mt-[length:var(--header-scroll-offset)]` or equivalent Tailwind/CSS that consumes the var (Claude discretion on exact class — one shared token). If 72px remains correct at all three widths, keep 72px but still centralize via the CSS var so values cannot drift. Optionally add `html { scroll-behavior: smooth }` only inside `@media (prefers-reduced-motion: no-preference)` per UI-SPEC; if smooth scroll is added, note it in SUMMARY. Do not change nav labels/hrefs (D-12). Do not add scroll-spy (D-14). Do not redesign section interiors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All five sections share one scroll-mt source (CSS var preferred).
    - SUMMARY records measured header heights at 1440/768/390 and chosen offset.
    - No scroll-spy; navigation.ts hash hrefs unchanged.
    - tsc exits 0.
  </acceptance_criteria>
  <done>Unified scroll-mt (CSS var) on five sections; header measurements documented; no scroll-spy.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage anchors Vitest smoke</name>
  <files>src/app/homepage-anchors.test.tsx, src/data/navigation.test.ts</files>
  <read_first>
    @src/app/page.tsx
    @src/data/navigation.ts
    @src/data/navigation.test.ts
    @src/components/sections/FinalCtaSection.test.tsx
  </read_first>
  <action>
    Add `src/app/homepage-anchors.test.tsx` that renders `Home` from `page.tsx` and asserts `#intelligence`, `#process`, `#solutions`, `#leverages`, `#final-cta` exist in the DOM (HOME-02 + HOME-01 order smoke). Extend `navigation.test.ts` (or add assertions in the anchors file) so each of `/#intelligence`, `/#process`, `/#solutions`, `/#leverages` appears on `primaryNav` and matches those ids — no Final CTA nav item required. Do not assert Share/Download or invent copy. Run full vitest suite green.
  </action>
  <verify>
    <automated>npx vitest run src/app/homepage-anchors.test.tsx src/data/navigation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Anchors test proves five section ids on Home.
    - Nav hash hrefs match the four locked section targets.
    - Full `npx vitest run` stays green; tsc exits 0.
  </acceptance_criteria>
  <done>homepage-anchors + navigation tests green; five section ids and four hash hrefs match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| hash fragment → section id | Client navigation only; no server trust change |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-05-03 | Spoofing | accept | In-page hashes only; no auth |
| T-05-04 | DoS | accept | CSS scroll-mt / optional smooth scroll — no JS loop |
| T-05-SC | Tampering | mitigate | Prefer npx one-shots for measure; no new package.json deps |
</threat_model>

<verification>
- Unified scroll-mt; anchors + navigation tests green; header measurements documented in SUMMARY
</verification>

<success_criteria>
HOME-02: hash targets for intelligence/process/solutions/leverages (and final-cta scroll-mt peer) clear sticky header; nav hrefs still match ids; smoke tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-cta-homepage-polish/05-02-SUMMARY.md` when done. Do not git commit (commit_docs: false).
</output>
