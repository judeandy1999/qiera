---
phase: 03-process
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/process.ts
  - src/data/process.ts
autonomous: true
requirements: [HOME-05]
user_setup: []

must_haves:
  truths:
    - "A typed ProcessStep contract exists covering overview + full modal fields"
    - "processSteps exports exactly 6 objects in reference order with no Orchestrate Competency step"
    - "Copy is transcribed from Process card references / RESEARCH; unreadable strings flagged with // TODO: unreadable"
  artifacts:
    - path: "src/types/process.ts"
      provides: "ProcessStep + ProcessSubstep types"
      exports: ["ProcessStep", "ProcessSubstep"]
    - path: "src/data/process.ts"
      provides: "Six typed process steps"
      exports: ["processSteps"]
      contains: "Discover Opportunities"
  key_links:
    - from: "src/data/process.ts"
      to: "src/types/process.ts"
      via: "import type { ProcessStep }"
      pattern: "ProcessStep"
---

<objective>
Create the typed Process data layer: `ProcessStep` contract and exactly six step objects (HOME-05), transcribed from RESEARCH / Process card references, with no seventh Orchestrate Competency step (Decision #1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-process/03-RESEARCH.md
@.planning/phases/03-process/03-UI-SPEC.md
@.planning/DECISIONS.md
@docs/qiera-reference/Homepage/Process cards/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ProcessStep type contract</name>
  <files>src/types/process.ts</files>
  <read_first>
    @.planning/phases/03-process/03-RESEARCH.md (TypeScript Type Contract section — exact fields)
    @src/types/intelligence.ts (typed-data convention)
  </read_first>
  <action>
    Create `src/types/process.ts` exporting `ProcessSubstep` and `ProcessStep` exactly matching the RESEARCH TypeScript Type Contract: id, stepNumber, title, icon, overviewDescription, primaryObjective, evidenceFirst, overview, whatWeDo[], typicalInputs[], keyOutputs[], successCriteria[], evidenceState (0–5), whyItMatters, nextStepTitle. Types only — no runtime values. No `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exports ProcessStep and ProcessSubstep with all RESEARCH fields.
    - No runtime data; no `any`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Transcribe all 6 processSteps</name>
  <files>src/data/process.ts</files>
  <read_first>
    @src/types/process.ts
    @.planning/phases/03-process/03-RESEARCH.md (Step-by-step data summary + Visual Analysis overview cards)
    @docs/qiera-reference/Homepage/3rd Section - process.png
    @docs/qiera-reference/Homepage/Process cards/
  </read_first>
  <action>
    Create `src/data/process.ts` exporting `const processSteps: ProcessStep[]` with exactly 6 objects in order: discover, identify (Identify Constraints), architect, build, derisk (De-Risk Projects), measure (Measure Impact). Use overview descriptions from the overview strip table. Fill modal fields from RESEARCH step summaries and Process card PNGs (exclude any duplicate overview image in Process cards). Icons: discover→telescope, identify→visibility or search, architect→layoutTemplate, build→product/boxes, derisk→shield, measure→target/chart — matching RESEARCH. evidenceState per RESEARCH (E2 for steps 1–3 → index 2; E3 for 4–6 → index 3). nextStepTitle cycles: step6 nextStepTitle = "Discover Opportunities". Transcribe verbatim; mark unreadable with `// TODO: unreadable`. NEVER add a 7th Orchestrate Competency step.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `processSteps.length === 6` with ids unique and titles matching the six reference titles.
    - No string "Orchestrate" appears in the file.
    - Each object has non-empty whatWeDo (≈5–6), typicalInputs, keyOutputs, successCriteria, whyItMatters, nextStepTitle.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer-transcribed content → typed module | No user input |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-02 | Injection | mitigate | Plain strings; JSX consumers escape |
| T-03-SC | Tampering | mitigate | No packages installed |
</threat_model>

<verification>
- processSteps.length === 6; no Orchestrate
- tsc clean
</verification>

<success_criteria>
Strict ProcessStep contract + 6 reference-faithful steps ready for cards and modal.
</success_criteria>

<output>
Create `.planning/phases/03-process/03-02-SUMMARY.md` when done. List any TODO: unreadable strings.
</output>
