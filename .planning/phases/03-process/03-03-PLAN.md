---
phase: 03-process
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/components/sections/ProcessCard.tsx
  - src/components/sections/ProcessDetail.tsx
autonomous: true
requirements: [HOME-05, HOME-09]
user_setup: []

must_haves:
  truths:
    - "ProcessCard renders numbered circle, icon, title, overviewDescription, and Read more → button (not Learn more)"
    - "ProcessDetail renders left panel (STEP X OF 6, objective, evidence first) and right panel (overview, WHAT WE DO, 4-col info grid, Evidence State) plus WHY THIS MATTERS bar with Next Step control"
    - "ProcessDetail is data-driven with no per-step id switch branching"
  artifacts:
    - path: "src/components/sections/ProcessCard.tsx"
      provides: "Overview process step card"
      exports: ["ProcessCard"]
    - path: "src/components/sections/ProcessDetail.tsx"
      provides: "Modal content for one ProcessStep"
      exports: ["ProcessDetail"]
      min_lines: 80
  key_links:
    - from: "src/components/sections/ProcessDetail.tsx"
      to: "src/types/process.ts"
      via: "import type { ProcessStep }"
      pattern: "ProcessStep"
    - from: "src/components/sections/ProcessCard.tsx"
      to: "src/components/ui/IntelligenceIcon.tsx"
      via: "IntelligenceIcon name={step.icon}"
      pattern: "IntelligenceIcon"
---

<objective>
Build ProcessCard (overview) and ProcessDetail (modal body) per UI-SPEC — distinct from Intelligence* layouts — consuming ProcessStep data and IntelligenceIcon (HOME-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-process/03-UI-SPEC.md
@.planning/phases/03-process/03-RESEARCH.md
@src/components/sections/IntelligenceCard.tsx
@src/components/sections/IntelligenceDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProcessCard</name>
  <files>src/components/sections/ProcessCard.tsx</files>
  <read_first>
    @.planning/phases/03-process/03-UI-SPEC.md (Process Section overview cards)
    @src/types/process.ts
    @src/components/sections/IntelligenceCard.tsx (ref/button pattern only — different visuals)
  </read_first>
  <action>
    Create `ProcessCard.tsx` with `'use client'` (receives onReadMore + readMoreRef). Props: step (ProcessStep), onReadMore callback, optional readMoreRef (Ref to HTMLButtonElement). Layout: flex column, centered text, border card-border, radius-card, padding lg (24px), h-full. Top: filled accent numbered circle (stepNumber) then IntelligenceIcon in accent-tint circle. Title Body-Bold ink. overviewDescription Body ink-secondary. Button type=button text "Read more →" with aria-label including step.title, cursor-pointer, min-h 44px, accent color, focus ring, onClick=onReadMore, ref=readMoreRef. No active border state. No dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exports ProcessCard; uses "Read more →" (not Learn more).
    - Numbered circle shows step.stepNumber; IntelligenceIcon uses step.icon.
    - Learn/Read more is a button with forwarded ref and aria-label including step title.
    - tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build ProcessDetail</name>
  <files>src/components/sections/ProcessDetail.tsx</files>
  <read_first>
    @.planning/phases/03-process/03-UI-SPEC.md (ProcessDetail / modal layout)
    @.planning/phases/03-process/03-RESEARCH.md (Common modal layout + Evidence State bar)
    @src/types/process.ts
    @src/components/ui/IntelligenceIcon.tsx
  </read_first>
  <action>
    Create Server-friendly presentational `ProcessDetail({ step, headingId, onNextStep })` — if onNextStep requires client, mark file `'use client'`. Layout per UI-SPEC: left ~25% modal-left-bg (STEP X OF 6, numbered circle, icon, h2 id=headingId, description, PRIMARY OBJECTIVE, EVIDENCE FIRST with shield); right ~75% surface (OVERVIEW with crosshair/target, WHAT WE DO horizontal substeps mapping whatWeDo, 4-col grid TYPICAL INPUTS / KEY OUTPUTS / SUCCESS CRITERIA / EVIDENCE & CONFIDENCE with E0–E5 bar highlighting evidenceState index, Confidence Range as static decorative visual not interactive range input). Full-width bottom bar accent-tint: lightbulb + WHY THIS MATTERS + Next Step pill button calling onNextStep, label includes step.nextStepTitle, min-h 44px, cursor-pointer, hover accent-strong. Map all arrays — no switch(step.id). Stack panels on mobile. No dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exports ProcessDetail; h2 uses id={headingId}.
    - Renders whatWeDo, typicalInputs, keyOutputs, successCriteria, evidence bar for E0–E5 with step.evidenceState highlighted.
    - Next Step button present and invokes onNextStep.
    - No per-step id branching; tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| typed ProcessStep → JSX | Developer content only |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-03 | Injection | mitigate | JSX text escaping; icon names via map |
| T-03-SC | Tampering | mitigate | No new packages |
</threat_model>

<verification>
- tsc clean; ProcessCard/ProcessDetail export correctly
</verification>

<success_criteria>
Overview card + modal detail renderers ready for ProcessSection wiring.
</success_criteria>

<output>
Create `.planning/phases/03-process/03-03-SUMMARY.md` when done.
</output>
