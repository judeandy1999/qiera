---
phase: 03-process
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/IntelligenceIcon.tsx
autonomous: true
requirements: [HOME-05]
user_setup: []

must_haves:
  truths:
    - "IntelligenceIcon resolves process icon names telescope, layoutTemplate, lightbulb, fileCheck, clipboardList, trophy, and crosshair to lucide-react components"
    - "Unknown icon names still render the Circle fallback without throwing"
  artifacts:
    - path: "src/components/ui/IntelligenceIcon.tsx"
      provides: "Extended lucide icon map for Process section"
      exports: ["IntelligenceIcon"]
      contains: "Telescope"
  key_links:
    - from: "src/components/ui/IntelligenceIcon.tsx"
      to: "lucide-react"
      via: "import Telescope, LayoutTemplate, Lightbulb, FileCheck, ClipboardList, Trophy, Crosshair"
      pattern: "Telescope"
---

<objective>
Extend `IntelligenceIcon` with the seven process-specific lucide icons required by Phase 3 (HOME-05 overview cards + ProcessDetail), without adding packages or changing the public component API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-process/03-RESEARCH.md
@.planning/phases/03-process/03-UI-SPEC.md
@src/components/ui/IntelligenceIcon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add process lucide icons to IntelligenceIcon map</name>
  <files>src/components/ui/IntelligenceIcon.tsx</files>
  <read_first>
    @src/components/ui/IntelligenceIcon.tsx
    @.planning/phases/03-process/03-RESEARCH.md (section "New lucide-react Icons Required")
    @.planning/phases/03-process/03-UI-SPEC.md (icon library rule — access lucide only via IntelligenceIcon)
  </read_first>
  <action>
    Import from lucide-react and register these keys on the existing `icons` map: `telescope`→Telescope, `layoutTemplate`→LayoutTemplate, `lightbulb`→Lightbulb, `fileCheck`→FileCheck, `clipboardList`→ClipboardList, `trophy`→Trophy, `crosshair`→Crosshair. Keep the public API `IntelligenceIcon({ name, className?, size? })` unchanged. Do not install packages. Do not import lucide from Process components — only extend this map. Preserve the Circle fallback for unknown names.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `IntelligenceIcon.tsx` imports Telescope, LayoutTemplate, Lightbulb, FileCheck, ClipboardList, Trophy, Crosshair from lucide-react.
    - The icons map contains keys: telescope, layoutTemplate, lightbulb, fileCheck, clipboardList, trophy, crosshair.
    - `npx tsc --noEmit` exits 0.
    - package.json dependencies are unchanged (no new installs).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer icon map → rendered SVG | name prop selects among a fixed map |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-01 | Injection | mitigate | name only indexes fixed map; no dangerouslySetInnerHTML |
| T-03-SC | Tampering | mitigate | No new packages |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- Icon keys listed in RESEARCH present in map
</verification>

<success_criteria>
Process icon names resolve through IntelligenceIcon with zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/03-process/03-01-SUMMARY.md` when done.
</output>
