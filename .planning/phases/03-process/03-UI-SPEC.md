---
phase: 3
slug: process
status: approved
shadcn_initialized: false
preset: none
created: 2026-07-11
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Sources consulted: `.planning/phases/03-process/03-RESEARCH.md` (PRIMARY — direct visual inspection of all 7 reference images), `.planning/phases/02-hero-intelligence/02-UI-SPEC.md` (inherited tokens), `.planning/DECISIONS.md` (locked decisions #1 and #4), `src/app/globals.css` (confirmed token inventory), `.cursor/rules/qiera-visual-fidelity.mdc`.

All fields are pre-populated from RESEARCH.md (direct image inspection) and Phase 2 UI-SPEC (inherited tokens). No user questions were needed. Where pixel measurements were sampled from screenshots rather than design files, values are marked **[ASSUMED]** — they self-correct after screenshot comparison per `qiera-visual-fidelity.mdc`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — native HTML + Tailwind v4 utilities only (inherited constraint; no shadcn, no second UI/styling framework) |
| Icon library | lucide-react 1.24.0 — accessed exclusively via `IntelligenceIcon.tsx` lookup table (extend with new process icons; do NOT import lucide directly in process components) |
| Font | Geist (`next/font/google`, wired via `@theme inline` in `src/app/globals.css`) — locked (DECISIONS #11, FOUND-04) |

`components.json` not found. shadcn gate: not applicable — forbidden by project constraint. All CSS custom properties live in `globals.css` `@theme inline` block. **Phase 3 adds zero new CSS tokens** — every required value is already declared from Phases 1–2.

---

## Spacing Scale

Inherited from Phases 1–2 without change. All values are multiples of 4.

| Token | Value | Usage in Phase 3 |
|-------|-------|-----------------|
| xs | 4px | Icon-to-label gap in substep items; Evidence State cell internal gap |
| sm | 8px | Gap between info grid column label and content; gap between substep items in WHAT WE DO strip; carousel dot gap |
| md | 16px | Card internal padding top/bottom; gap between card numbered circle and icon; between info grid columns |
| lg | 24px | Card padding (all sides); gap between sections in modal right panel; gap between left-panel elements |
| xl | 32px | Section internal column gap; modal panel left/right padding |
| 2xl | 48px | Process section vertical padding (desktop); gap above/below section header |
| 3xl | 64px | Outer horizontal padding on wide desktop (≥1280px) |
| panel | 40px | Modal panel top/bottom padding (left + right panels) — existing token, reuse |

**Phase 3 exceptions (same rules as Phase 2):**
- **"Read more →" touch target** on mobile: minimum 44px height — pad with invisible hit-area
- **Modal close button hit area**: minimum 44×44px
- **Next Step button**: minimum 44px height on all viewports — visually pill-shaped but meets touch minimum

---

## Typography

Ceiling inherited from Phase 2 checker: **≤4 sizes, ≤2 weights**. Phase 3 stays within this envelope. Font: Geist.

| Role | Size | Weight | Line Height | Notes |
|------|------|--------|-------------|-------|
| Caption | 12px | 400 (regular) | 1.4 | Section eyebrow ("OUR PROCESS"), STEP X OF 6 label, PRIMARY OBJECTIVE / EVIDENCE FIRST / OVERVIEW / WHAT WE DO / WHY THIS MATTERS section labels, info grid column headers (TYPICAL INPUTS etc.), substep descriptions, Evidence State level sub-labels (Hypothesis…Validated), Evidence State E-level codes. Uppercase, letter-spacing 0.08–0.1em for all label roles. |
| Body | 16px | 400 (regular) | 1.6 | Card description paragraphs, left-panel overview description, left-panel PRIMARY OBJECTIVE body, right-panel OVERVIEW paragraph, WHY THIS MATTERS body, "Read more →" trigger copy |
| Body-Bold | 16px | 600 (semibold) | 1.5 | Card step titles ("Discover Opportunities"), substep titles in WHAT WE DO, info grid item content headings where applicable, "WHY THIS MATTERS" label itself, Next Step button text |
| Heading | 32px | 600 (semibold) | 1.2 | Section H2 ("A Proven 6-Step Methodology"), modal step H2 (step title, referenced by `aria-labelledby`) **[ASSUMED — collapsed from any larger value to match Phase 2 ceiling; correct against 1440 screenshot]** |

> **Hero-Display (60px / 600 / 1.1)** from Phase 2 is NOT used in Phase 3. The 4 sizes used in Phase 3 are: 12 / 16 / 16 / 32.

Active size+weight combinations in Phase 3 components:

- **Section eyebrow** — Caption (12px / 400 / 1.4): uppercase, tracking 0.08em, `--color-accent`. Copy: "OUR PROCESS"
- **Section H2** — Heading (32px / 600 / 1.2): `--color-ink`. Copy: "A Proven 6-Step Methodology"
- **Card numbered circle text** — Caption (12px / 400 / 1.4): white on `--color-accent` fill. Numerals "1"–"6"
- **Card step title** — Body-Bold (16px / 600 / 1.5): `--color-ink`
- **Card description** — Body (16px / 400 / 1.6): `--color-ink-secondary`
- **"Read more →" link** — Body (16px / 400 / 1.6): `--color-accent`
- **STEP X OF 6** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **Modal step H2** — Heading (32px / 600 / 1.2): `--color-ink`
- **Modal left-panel description** — Body (16px / 400 / 1.6): `--color-ink-secondary`
- **PRIMARY OBJECTIVE label** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **PRIMARY OBJECTIVE body** — Body (16px / 400 / 1.6): `--color-ink-secondary`
- **EVIDENCE FIRST label** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **EVIDENCE FIRST body** — Caption (12px / 400 / 1.4): `--color-ink-secondary`
- **OVERVIEW label** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **OVERVIEW paragraph** — Body (16px / 400 / 1.6): `--color-ink`
- **WHAT WE DO label** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **Substep titles** — Body-Bold (16px / 600 / 1.5): `--color-ink`
- **Substep descriptions** — Caption (12px / 400 / 1.4): `--color-ink-secondary`
- **Info grid column headers** — Caption (12px / 400 / 1.4): `--color-accent`, uppercase, tracking 0.1em
- **Info grid list items** — Body (16px / 400 / 1.6): `--color-ink-secondary`
- **Evidence State E-codes** (E0–E5) — Caption (12px / 600 / 1.4): white (highlighted) or `--color-ink-secondary` (inactive)
- **Evidence State labels** (Hypothesis…Validated) — 10px / 400 **[EXCEPTION: 10px is the only value below the 12px Caption floor; justified by the tight E-bar cell geometry where 12px wraps unacceptably at 6 cells wide. This is a single-element exception — do not proliferate]**
- **WHY THIS MATTERS label** — Body-Bold (16px / 600 / 1.5): `--color-ink`
- **WHY THIS MATTERS body** — Body (16px / 400 / 1.6): `--color-ink-secondary`
- **Next Step button** — Body-Bold (16px / 600 / 1.5): white on `--color-accent`

---

## Color

All tokens inherited from Phases 1–2. **Phase 3 adds zero new CSS tokens.**

### Inherited tokens (already in globals.css — do not redeclare)

| Token | Value | Phase 3 Usage |
|-------|-------|---------------|
| `--color-surface` | `#FFFFFF` | Process section background, card backgrounds, modal right panel |
| `--color-ink` | `#0B0D17` | Card titles, section H2, modal H2, OVERVIEW paragraph text |
| `--color-accent` | `#6366F1` | Section eyebrow, numbered circles fill, STEP X OF 6, all section labels, Evidence State highlighted cell, "Read more →" links, Next Step button fill, icon fills in accent context |
| `--color-accent-tint` | `rgba(99,102,241,0.08)` | Icon container backgrounds in cards (48×48 circles) and modal substeps (40×40 circles); modal left-panel lavender tint background (same as `--color-modal-left-bg`) |
| `--color-accent-strong` | `#7C3AED` | Next Step button hover state only — same pattern as primary CTA button |
| `--color-ink-secondary` | `#6B7280` | Card descriptions, modal body paragraphs, substep descriptions, info grid list items, inactive Evidence State labels, WHY THIS MATTERS body |
| `--color-card-border` | `rgba(0,0,0,0.08)` | Process card borders on white surface; dividers inside modal |
| `--color-modal-left-bg` | `rgba(99,102,241,0.06)` | Modal left panel background |
| `--color-modal-backdrop` | `rgba(0,0,0,0.70)` | Detail modal backdrop (same `DetailModal` component reused) |
| `--color-focus-ring` | `#8B7CF6` | Focus rings on all interactive elements: "Read more →" buttons, modal close, Next Step button |
| `--radius-card` | `16px` | Process card border-radius; modal container border-radius |
| `--radius-button` | `999px` | Next Step pill button border-radius; "Read more →" button focus ring shape |

### 60/30/10 Color Split for Phase 3

**Process overview section (light):**
- 60% `--color-surface` (#FFFFFF) — section background, card backgrounds
- 30% card borders (`--color-card-border`), muted text (`--color-ink-secondary`), connector glyph color
- 10% `--color-accent` — eyebrow, numbered circles, icon fills, "Read more →" links

**Process modal:**
- 60% `--color-surface` (#FFFFFF) — right panel (majority of modal area), modal container background
- 30% `--color-modal-left-bg` (rgba(99,102,241,0.06)) — left panel lavender tint; `--color-accent-tint` icon circles
- 10% `--color-accent` — STEP label, numbered circle, section labels, Evidence State active cell, Next Step button, shield icon, target icon, lightbulb icon

**Bottom "WHY THIS MATTERS" bar:**
- Background: `--color-accent-tint` (rgba(99,102,241,0.08)) **[ASSUMED — light lavender tint matching accent-tint token; correct after 1440 screenshot comparison]**

Accent reserved for: section eyebrow label ("OUR PROCESS"), numbered circle fill in overview cards, STEP X OF 6 label, all modal section labels (PRIMARY OBJECTIVE, EVIDENCE FIRST, OVERVIEW, WHAT WE DO, info grid column headers), Evidence State highlighted cell background, "Read more →" trigger links, Next Step button fill, lightbulb icon (WHY THIS MATTERS), shield icon (EVIDENCE FIRST), target/crosshair icon (OVERVIEW). **Never** for default body text, inactive card borders, connector arrows, info grid list items, or substep descriptions.

Destructive: N/A — Phase 3 has no destructive actions. Token remains undeclared until Phase 7.

---

## Layout & Component Contracts

### Process Section (Overview Strip)

**Section ID:** `id="process"` — required for hash navigation. Apply `scroll-margin-top: 72px` (same as IntelligenceSection — accounts for sticky header height **[ASSUMED at 72px; verify after Phase 1 header implementation]**)

**Section background:** `--color-surface` (#FFFFFF) — same as Intelligence section. Full-width light surface. **PITFALL: Do NOT use `--color-shell` — process section is white, not dark.**

**Container:** `--container-max` (1280px), same horizontal padding as preceding sections (48px desktop / 32px tablet / 24px mobile).

**Vertical padding:** 96px top / 96px bottom desktop; 64px tablet/mobile **[ASSUMED — consistent with Phase 2 pattern; correct after screenshot]**.

**Section header layout:**
```
[Eyebrow]
[H2 "A Proven 6-Step Methodology"]
```
Left-aligned, stacked. No carousel navigation controls (no carousel for the overview strip). No visible subheading in the overview reference **[A6 in RESEARCH: if 1440 screenshot reveals a subheading, add it as Body (16px/400/1.6) in `--color-ink-secondary` below the H2]**.

**Section eyebrow:**
- Font: Caption (12px / 400 / 1.4), uppercase, tracking 0.08em
- Color: `--color-accent`
- Copy: **OUR PROCESS**

**Section heading (`<h2>`):**
- Font: Heading (32px / 600 / 1.2)
- Color: `--color-ink`
- Copy: **A Proven 6-Step Methodology**

---

### Process Card Grid

**Desktop (≥1280px / `xl:`):** 6-column horizontal row. Cards share equal width. `→` connector glyphs rendered between each adjacent pair of cards (5 total). Connectors: `aria-hidden="true"`, `hidden xl:flex`, `--color-ink-secondary`, `text-xl`, vertically centered at the card top (approximately at the icon centroid height — `self-start mt-16` **[ASSUMED — correct from 1440 screenshot]**).

**Tablet (768–1279px):** 3-column × 2-row grid (no connectors). Connectors hidden. Cards wrap naturally **[A7 in RESEARCH — implementer choice if reference suggests horizontal scroll instead; capture screenshot at 768px to confirm]**.

**Mobile (≤767px):** Horizontal scroll-snap (`scroll-snap-type: x mandatory`), one card visible at a time, `scroll-snap-align: start` per card. No connector arrows. Same scroll-snap pattern as Intelligence section mobile carousel.

**Card anatomy:**

```
[Numbered circle: 32×32px, rounded-full, bg=--color-accent, text=white Caption/600]
[Icon: IntelligenceIcon size=48, inside --color-accent-tint circle 64×64px rounded-[12px]] [ASSUMED size — correct against reference]
[Step title: Body-Bold (16px/600/1.5), --color-ink]
[Description: Body (16px/400/1.6), --color-ink-secondary]
[Divider: 1px solid --color-card-border]
["Read more →": Body (16px/400/1.6), --color-accent, <button> element]
```

**Card container:**
- Background: `--color-surface`
- Border: 1px solid `--color-card-border`
- Border-radius: `--radius-card` (16px)
- Padding: 24px (lg) all sides **[ASSUMED]**
- flex-col, items-start (left-aligned per RESEARCH Q2), text-left
- Cursor: default — card container is NOT clickable; only the "Read more →" button is interactive

**No active/selected card state** in the overview strip — the modal is the primary detail view, not an active border pattern.

---

### ProcessDetail Modal Content (New Component)

**Shell:** Reuse existing `DetailModal` component **without any changes**. Only the content slot changes. Pass:
```tsx
<DetailModal
  open={selectedIndex !== null}
  onClose={closeModal}
  title={step?.title ?? ""}
  labelledById="process-modal-heading"
>
  {step && (
    <ProcessDetail
      step={step}
      headingId="process-modal-heading"
      onNextStep={goNextStep}
    />
  )}
</DetailModal>
```

**Modal container dimensions:** Same `DetailModal` sizing from Phase 2: max-width 1120px, width `calc(100% - 48px)`, max-height 85vh, `--radius-card` border-radius, `--color-modal-backdrop` backdrop.

**ProcessDetail layout:** Two-panel row + full-width bottom bar (stacks to single column on mobile).

**Desktop layout (≥768px):**
```
[Left panel: ~25%]  |  [Right panel: ~75%]
                       [Bottom bar: full width — spans both columns]
```

**Mobile layout (<768px):**
- Left panel content → right panel content → bottom bar, all stacked vertically as single column
- Modal: 100vw × 100dvh, border-radius 0, margin 0 (native DetailModal mobile contract)

---

#### Left Panel (~25% width)

Background: `--color-modal-left-bg` (rgba(99,102,241,0.06))  
Padding: `panel` 40px top/bottom, xl 32px left/right

Content stack (top to bottom):

1. **STEP X OF 6 label** — Caption (12px / 400 / 1.4), `--color-accent`, uppercase, tracking 0.1em. Rendered as `STEP {step.stepNumber} OF 6`.

2. **Numbered circle** — 64×64px, `rounded-full`, `bg-[var(--color-accent)]`. White numeral inside: Caption (12px / 600 / 1.4) **[ASSUMED at 12px — number is large enough to read; if 1440 screenshot shows it larger, Body-Bold 16px is acceptable as it's within the size ceiling]**.

3. **Icon container** — 64×64px `rounded-full` (or rounded-[12px] per reference), `bg-[var(--color-accent-tint)]`. Contains `<IntelligenceIcon name={step.icon} size={32} />`. **[ASSUMED 32px icon inside 64px container — correct from reference]**

4. **Step title** `<h2 id={headingId}>` — Heading (32px / 600 / 1.2), `--color-ink`. This ID must match `labelledById="process-modal-heading"` passed to `DetailModal`.

5. **Overview description** — Body (16px / 400 / 1.6), `--color-ink-secondary`

6. **Divider** — 1px solid `--color-card-border`, margin 24px 0

7. **PRIMARY OBJECTIVE section:**
   - Label: Caption (12px / 400 / 1.4), `--color-accent`, uppercase, tracking 0.1em. Copy: "PRIMARY OBJECTIVE"
   - Body: Body (16px / 400 / 1.6), `--color-ink-secondary` — `step.primaryObjective`

8. **Divider** — 1px solid `--color-card-border`, margin 24px 0

9. **EVIDENCE FIRST box:**
   - Shield icon: `<IntelligenceIcon name="shield" size={20} />` in `--color-accent`
   - Label: Caption (12px / 400 / 1.4), `--color-accent`, uppercase, tracking 0.1em. Copy: "EVIDENCE FIRST"
   - Body: Caption (12px / 400 / 1.4), `--color-ink-secondary` — `step.evidenceFirst`

---

#### Right Panel (~75% width)

Background: `--color-surface`  
Padding: `panel` 40px top/bottom, xl 32px left/right

Content sections (top to bottom):

**Section 1 — OVERVIEW:**
- Target/crosshair icon + label row: `<IntelligenceIcon name="crosshair" size={16} />` (or `"target"`) in `--color-accent`, followed by Caption "OVERVIEW" label
- Paragraph: Body (16px / 400 / 1.6), `--color-ink` — `step.overview`
- Divider: 1px solid `--color-card-border`, margin 24px 0

**Section 2 — WHAT WE DO (horizontal substep flow):**
- Label row: Caption "WHAT WE DO", `--color-accent`, uppercase, tracking 0.1em
- Horizontal flex row of `step.whatWeDo` (5 or 6 substeps depending on step):
  - Each substep:
    - Icon circle: 40×40px `rounded-full`, `bg-[var(--color-accent-tint)]`, `<IntelligenceIcon name={substep.icon} size={20} />`
    - Substep title: Body-Bold (16px / 600 / 1.5), `--color-ink`, centered
    - Substep description: Caption (12px / 400 / 1.4), `--color-ink-secondary`, centered
  - `→` connector between substeps: `aria-hidden="true"`, `--color-ink-secondary`, `shrink-0`, `mt-4`
  - Container: `flex overflow-x-auto [scroll-snap-type:x_mandatory]` — scrollable on mobile; `md:overflow-x-visible` (no scroll at ≥768px)
  - Each substep item: `shrink-0`, `[scroll-snap-align:start]`, width ~128px
- Divider: 1px solid `--color-card-border`, margin 24px 0

**Section 3 — Four-column info grid:**

Desktop: 4 columns. Tablet (768px): 2 columns. Mobile: 1 column.

| Column | Header Label | Content | Header Icon |
|--------|-------------|---------|-------------|
| 1 | TYPICAL INPUTS | Bulleted list (5–8 items) from `step.typicalInputs` | `clipboardList` |
| 2 | KEY OUTPUTS | Bulleted list (5–8 items) from `step.keyOutputs` | `fileCheck` |
| 3 | SUCCESS CRITERIA | Bulleted list (4–6 items) from `step.successCriteria` | `trophy` |
| 4 | EVIDENCE & CONFIDENCE | Evidence State E0–E5 bar + Confidence Range | `shieldCheck` |

Column header style: Caption (12px / 400 / 1.4), `--color-accent`, uppercase, tracking 0.1em, with icon (16×16px) inline before text.

List items style: Body (16px / 400 / 1.6), `--color-ink-secondary`, with bullet character "•" in `--color-accent`.

**Evidence State E0–E5 bar (within column 4):**
```
[E0] [E1] [E2] [E3] [E4] [E5]
```
- 6 cells in a horizontal flex row
- Each cell: `flex-col`, `items-center`, `gap-0.5`, `rounded`, `px-2 py-1`
- Active cell (`i === step.evidenceState`): `bg-[var(--color-accent)] text-white`
- Inactive cells: `bg-[var(--color-accent-tint)] text-[var(--color-ink-secondary)]`
- E-code: Caption (12px / 600 / 1.4) "E0"–"E5"
- Level label: 10px / 400 — ["Hypothesis", "Weak", "Limited", "Moderate", "Strong", "Validated"] **[10px exception — see Typography notes]**
- Static, non-interactive — rendered from `step.evidenceState: number` (0–5)

**Confidence Range visual:**
- Static visual indicator (non-interactive — reference shows a dot on a line)
- Rendered as a simple horizontal line with a positioned dot **[ASSUMED — a plain `<div>` progress-bar-style element; confirm from reference]**
- Source: `step.confidenceRange` if typed, or omit from data type and render as decorative static element

---

#### Bottom Bar (Full Width)

Rendered below both panels (or below the right panel on desktop if inside a `grid-rows` layout; full-width spanning both columns).

Background: `--color-accent-tint` (rgba(99,102,241,0.08)) **[ASSUMED — matches the lightest lavender tint; correct after 1440 screenshot]**  
Padding: lg 24px all sides

Layout: horizontal flex row on desktop (icon + label + body text left; Next Step button right). Stack on mobile.

- Lightbulb icon: `<IntelligenceIcon name="lightbulb" size={24} />`, `--color-accent`
- "WHY THIS MATTERS" label: Body-Bold (16px / 600 / 1.5), `--color-ink`
- Body text: Body (16px / 400 / 1.6), `--color-ink-secondary` — `step.whyItMatters`
- **Next Step button:**
  - Element: `<button type="button">`
  - Background: `--color-accent`; hover: `--color-accent-strong`
  - Border-radius: `--radius-button` (999px pill)
  - Padding: 12px 24px (py-3 px-6)
  - Text: Body-Bold (16px / 600 / 1.5), white
  - Copy: `"→ Next Step: {step.nextStepTitle}"`
  - `aria-label={`Next Step: ${step.nextStepTitle}`}`
  - At step 6: `step.nextStepTitle` = "Discover Opportunities" (cycle wraps — PITFALL #3)
  - Focus ring: `focus-visible:ring-2 focus-visible:ring-[var(--color-focus-ring)] focus-visible:ring-offset-2`

---

### Icon Extension Contract

The following names must be added to `IntelligenceIcon.tsx` before process components use them. Do NOT import lucide-react directly in process components.

| Key (lookup name) | lucide-react export | Used in Phase 3 for |
|-------------------|--------------------|--------------------|
| `telescope` | `Telescope` | Step 1 (Discover) overview card + modal |
| `layoutTemplate` | `LayoutTemplate` | Step 3 (Architect) overview card + modal |
| `lightbulb` | `Lightbulb` | WHY THIS MATTERS bar icon |
| `fileCheck` | `FileCheck` | KEY OUTPUTS column header icon |
| `clipboardList` | `ClipboardList` | TYPICAL INPUTS column header icon |
| `trophy` | `Trophy` | SUCCESS CRITERIA column header icon |
| `crosshair` | `Crosshair` | OVERVIEW section label icon |

**Already present in IntelligenceIcon.tsx (no addition needed):** `search` (Step 2), `boxes` (Step 4), `shield` / `shieldCheck` (Step 5, EVIDENCE FIRST), `trendingUp` / `trend` (Step 6), `target` (alternative OVERVIEW icon).

All exports verified in lucide-react 1.24.0 at research time.

---

## Copywriting Contract

### Process Overview Section

| Element | Copy |
|---------|------|
| Section eyebrow | OUR PROCESS |
| Section H2 | A Proven 6-Step Methodology |
| Card trigger (all 6 cards) | Read more → |
| Card trigger aria-label | `Read more about {step.title}` |

### Process Cards (6 steps — transcribed from reference)

| Step | Title | Overview Description |
|------|-------|---------------------|
| 1 | Discover Opportunities | Identify high-potential opportunities based on data, market signals, customer insights, and strategic priorities. |
| 2 | Identify Constraints | Uncover the root constraints limiting growth, performance, or customer value. |
| 3 | Architect Solutions | Design tailored, high-impact solutions that directly address root causes and unlock opportunities. |
| 4 | Build Capabilities | Develop the systems, processes, tools, and assets needed to execute with excellence. |
| 5 | De-Risk Projects | Anticipate risks early, validate assumptions, and remove roadblocks before they impact results. |
| 6 | Measure Impact | Track what matters, learn continuously, and optimize to improve outcomes over time. |

### Modal Labels (all steps use identical structural labels)

| Element | Copy |
|---------|------|
| Step position label | STEP {N} OF 6 |
| Left-panel section 1 label | PRIMARY OBJECTIVE |
| Left-panel section 2 label | EVIDENCE FIRST |
| Right-panel section 1 label | OVERVIEW |
| Right-panel section 2 label | WHAT WE DO |
| Info grid column 1 | TYPICAL INPUTS |
| Info grid column 2 | KEY OUTPUTS |
| Info grid column 3 | SUCCESS CRITERIA |
| Info grid column 4 | EVIDENCE & CONFIDENCE |
| Bottom bar label | WHY THIS MATTERS |
| Next Step button | → Next Step: {nextStep.title} |
| Modal close | aria-label="Close" (×) |

### States

| State | Copy |
|-------|------|
| Empty process list | N/A — data is static/typed; no empty state possible |
| Error / failed render | N/A — static Client Component; data import cannot fail at runtime |
| Destructive actions | None — Phase 3 has no destructive actions |
| Next Step at step 6 | → Next Step: Discover Opportunities (wraps, not disabled) |

---

## Accessibility Contract (HOME-09)

Phase 3 reuses `DetailModal` for all HOME-09 requirements. The following table confirms contract compliance:

| Behavior | Implementation |
|----------|----------------|
| Keyboard open modal | "Read more →" is `<button>` — activates on Enter and Space |
| Escape close | Native `<dialog>` cancel event (in `DetailModal`) |
| Focus trap | Native `showModal()` manages inert background |
| Focus restore | `DetailModal` triggerRef pattern — returns focus to triggering `<button>` |
| Tab cycle inside modal | All interactive elements (close ×, Next Step, scroll) in natural tab order |
| Within-modal next step | Next Step `<button>` inside modal — advances index via `onNextStep()` prop without closing/reopening dialog |
| Modal mobile full/near-full | `width: 100vw; height: 100dvh; border-radius: 0; margin: 0` (DetailModal Phase 2 contract) |
| prefers-reduced-motion | All modal open/close CSS animations wrapped in `@media (prefers-reduced-motion: no-preference)` — default is instant appear/disappear |
| Section heading order | `<h1>` on hero (Phase 1); `<h2>` on Intelligence section (Phase 2); `<h2>` on Process section ("A Proven 6-Step Methodology"); `<h2 id="process-modal-heading">` inside modal — no heading levels skipped |
| ARIA dialog label | `<dialog aria-labelledby="process-modal-heading">` via `labelledById` prop on `DetailModal` |
| Icon-only buttons | Modal close: `aria-label="Close"`. Next Step: `aria-label="Next Step: {title}"` |
| Card icon containers | `aria-hidden="true"` on icon containers (decorative; step title provides the label) |
| Connector arrows | `aria-hidden="true"` on all `→` glyph connectors |
| WHAT WE DO substeps | `aria-hidden="true"` on `→` connectors between substeps |
| scroll-mt on section | `scroll-mt-[72px]` on `<section id="process">` |

---

## Responsive Behavior

Target widths: **1440, 1280, 1024, 768, 390, 360**

| Viewport | Overview Strip | Modal |
|----------|---------------|-------|
| 1440px | 6-column row, `→` connectors visible (`xl:flex`) | 1120px max-width, 25%/75% two-panel |
| 1280px | 6-column row (narrower cards), connectors still at `xl:` | ~90vw, two-panel |
| 1024px | 3-column × 2-row grid, connectors hidden | ~90vw, two-panel |
| 768px | 3-column × 2-row grid (or horizontal scroll if 3-col is too cramped — implementer decision; capture screenshot) | ~90vw, two-panel |
| 390px | Horizontal scroll-snap, 1 card visible | 100vw × 100dvh, single-column stack, border-radius 0 |
| 360px | Same as 390, ensure no horizontal overflow | Same as 390 |

**WHAT WE DO substep strip:** `overflow-x-auto scroll-snap-type: x mandatory` on all viewports. At ≥768px: if all substeps fit without overflow, `overflow-x-visible`. At <768px: always scrollable.

**Info grid (right panel):** 4 cols at ≥1024px, 2 cols at 768–1023px, 1 col at <768px.

No horizontal page overflow permitted at any target width.

---

## Screenshot Verification (mandatory before Phase 3 sign-off)

Per `qiera-visual-fidelity.mdc`, capture and compare at **1440, 1024, 390**:

| Component | Reference file |
|-----------|---------------|
| Process overview strip | `docs/qiera-reference/Homepage/3rd Section - process.png` |
| Step 1 modal (Discover) | `docs/qiera-reference/Homepage/Process cards/Discover opportunities - process.png` |
| Step 2 modal (Identify) | `docs/qiera-reference/Homepage/Process cards/Identify Constraints -process.png` |
| Step 3 modal (Architect) | `docs/qiera-reference/Homepage/Process cards/architect solutions - process.png` |
| Step 4 modal (Build) | `docs/qiera-reference/Homepage/Process cards/build capabilities - process.png` |
| Step 5 modal (De-Risk) | `docs/qiera-reference/Homepage/Process cards/de-risk projects - process.png` |
| Step 6 modal (Measure) | `docs/qiera-reference/Homepage/Process cards/measure impacts - process.png` |

**PITFALL:** `docs/qiera-reference/Homepage/Process cards/3rd Section - process.png` is a duplicate of the overview reference. Ignore it — use only the 6 step-specific files above.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable — no third-party registry declared |

No registries in use. Design system tool is `none` (locked). Phase 3 introduces zero new npm dependencies.

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| `03-RESEARCH.md` | All 6 card titles + descriptions, all modal section labels, all step-specific copy (PRIMARY OBJECTIVE, EVIDENCE FIRST, WHY THIS MATTERS, substep names), visual layout geometry (25/75 split, numbered circle sizes, Evidence State bar pattern, icon sizes), icon name mapping to lucide-react exports, pitfalls and assumptions |
| `02-UI-SPEC.md` | Typography ceiling (≤4 sizes 12/16/32/60, ≤2 weights 400/600), full spacing scale, all CSS token values, DetailModal reuse contract, responsive breakpoints, accessibility behaviors, 60/30/10 color model |
| `globals.css` | Confirmed all required tokens already declared — zero new tokens needed |
| `DECISIONS.md` | Decision #1 (6 steps only, no Orchestrate Competency), #4 (modal only, no routes) |
| User input | 0 questions asked — all fields pre-populated from upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
