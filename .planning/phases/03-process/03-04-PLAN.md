---
phase: 03-process
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/components/sections/ProcessSection.tsx
  - src/components/sections/ProcessSection.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-05, HOME-09]
user_setup: []

must_haves:
  truths:
    - "Homepage Process section id=process shows exactly 6 cards with arrow connectors and Read more triggers"
    - "Read more opens DetailModal with ProcessDetail; Next Step cycles indices modulo 6; Escape closes and restores focus"
    - "No seventh process step is rendered"
  artifacts:
    - path: "src/components/sections/ProcessSection.tsx"
      provides: "Client process section + modal orchestration"
      exports: ["ProcessSection"]
      min_lines: 60
    - path: "src/components/sections/ProcessSection.test.tsx"
      provides: "HOME-05/HOME-09 integration tests"
    - path: "src/app/page.tsx"
      provides: "Hero + IntelligenceSection + ProcessSection"
      contains: "ProcessSection"
  key_links:
    - from: "src/components/sections/ProcessSection.tsx"
      to: "src/components/ui/DetailModal.tsx"
      via: "DetailModal open/onClose/labelledById"
      pattern: "DetailModal"
    - from: "src/app/page.tsx"
      to: "src/components/sections/ProcessSection.tsx"
      via: "render ProcessSection after IntelligenceSection"
      pattern: "ProcessSection"
---

<objective>
Assemble ProcessSection (client): header, 6-card strip with connectors, DetailModal + ProcessDetail orchestration with Next Step cycling, Vitest coverage, and wire onto homepage under Intelligence (HOME-05, HOME-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-process/03-UI-SPEC.md
@.planning/phases/03-process/03-VALIDATION.md
@src/components/sections/IntelligenceSection.tsx
@src/components/ui/DetailModal.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProcessSection client island</name>
  <files>src/components/sections/ProcessSection.tsx</files>
  <read_first>
    @src/components/sections/IntelligenceSection.tsx
    @src/components/ui/DetailModal.tsx
    @src/data/process.ts
    @.planning/phases/03-process/03-UI-SPEC.md (section id, header copy, connectors)
  </read_first>
  <action>
    Create `'use client'` ProcessSection. State: selectedIndex number|null; refs array for Read more buttons. Section id=process, scroll-mt-[72px], bg surface. Header: eyebrow OUR PROCESS, H2 A Proven 6-Step Methodology (no subheading unless in UI-SPEC). Desktop: 6-col grid with gap-10, render ProcessCard for each processSteps entry with → connector between cards (not after last) — hide connectors below lg and stack/scroll intentionally. Modal: DetailModal open={selectedIndex!==null} onClose clears index, title=step.title, labelledById=process-modal-heading; children ProcessDetail with onNextStep setting (i+1)%6. Focus restore relies on DetailModal + opening from the Read more button. Do not hardcode a 7th step. Keep Home as RSC by only importing this client child.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exports ProcessSection; section has id=process.
    - Maps processSteps only (length 6); DetailModal + ProcessDetail wired; Next Step cycles with modulo 6.
    - Eyebrow and H2 copy match UI-SPEC verbatim.
    - tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ProcessSection tests + page wire</name>
  <files>src/components/sections/ProcessSection.test.tsx, src/app/page.tsx</files>
  <read_first>
    @src/components/sections/IntelligenceSection.test.tsx
    @src/components/ui/DetailModal.test.tsx
    @src/components/sections/ProcessSection.tsx
    @src/app/page.tsx
  </read_first>
  <action>
    Write ProcessSection.test.tsx covering: (1) exactly 6 Read more about … buttons / no Orchestrate; (2) open step 1 modal visible with Discover Opportunities; (3) Next Step advances content toward Identify Constraints; (4) Escape closes modal; (5) focus restores to trigger after close. Update page.tsx to render ProcessSection after IntelligenceSection inside the fragment. Run vitest for ProcessSection and full suite.
  </action>
  <verify>
    <automated>npx vitest run src/components/sections/ProcessSection.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ProcessSection.test.tsx passes all cases above.
    - page.tsx contains ProcessSection after IntelligenceSection; Home has no use client.
    - Full `npx vitest run` stays green; tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client island ↔ static process data | No request input |

## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-03-04 | DoS | mitigate | Index clamped via modulo 6 |
| T-03-05 | Injection | mitigate | Typed data + JSX |
| T-03-SC | Tampering | mitigate | No new packages |
</threat_model>

<verification>
- vitest ProcessSection + full suite green
- id=process present; 6 cards only
</verification>

<success_criteria>
Interactive Process section live on homepage with accessible modal behaviors proven by tests.
</success_criteria>

<output>
Create `.planning/phases/03-process/03-04-SUMMARY.md` when done.
</output>
