---
phase: 04-solutions-leverages
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/components/sections/SolutionPanel.tsx
  - src/components/sections/SolutionsSection.tsx
  - src/components/sections/SolutionsSection.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-06]
user_setup: []

must_haves:
  truths:
    - "Homepage #solutions shows WAI-ARIA tablist with exactly 10 tabs; default selected is Revenue-Leaks"
    - "Clicking Conversion-Problems or ArrowRight from first tab updates aria-selected and visible panel content"
    - "Active panel renders hero, gauge/metric, category ProgressBars, opportunity Sparkline, How We Create Impact (5), Outcomes — no Solutions DetailModal"
  artifacts:
    - path: "src/components/sections/SolutionPanel.tsx"
      provides: "Data-driven challenge dashboard panel"
      exports: ["SolutionPanel"]
      min_lines: 80
    - path: "src/components/sections/SolutionsSection.tsx"
      provides: "Client Solutions tablist + panel island"
      exports: ["SolutionsSection"]
      min_lines: 80
    - path: "src/components/sections/SolutionsSection.test.tsx"
      provides: "HOME-06 Nyquist tests"
    - path: "src/app/page.tsx"
      provides: "Process then Solutions wired"
      contains: "SolutionsSection"
  key_links:
    - from: "src/components/sections/SolutionsSection.tsx"
      to: "src/data/solutions.ts"
      via: "import { solutions }"
      pattern: "solutions"
    - from: "src/components/sections/SolutionPanel.tsx"
      to: "src/components/ui/ProgressBar.tsx"
      via: "ProgressBar + GaugeChart + Sparkline"
      pattern: "ProgressBar"
    - from: "src/app/page.tsx"
      to: "src/components/sections/SolutionsSection.tsx"
      via: "render after ProcessSection"
      pattern: "SolutionsSection"
---

<objective>
Build SolutionsSection as an accessible WAI-ARIA tabs client island with inline SolutionPanel dashboards (CSS/SVG charts), Vitest coverage per VALIDATION, and wire onto the homepage after Process (HOME-06). No Solutions DetailModal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@.planning/phases/04-solutions-leverages/04-VALIDATION.md
@src/data/solutions.ts
@src/components/ui/ProgressBar.tsx
@src/components/ui/GaugeChart.tsx
@src/components/ui/Sparkline.tsx
@src/components/ui/IntelligenceIcon.tsx
@src/app/page.tsx
@src/components/sections/ProcessSection.test.tsx

<interfaces>
From src/types/solutions.ts (Plan 03):
- SolutionChallenge with id, tabLabel, icon, title, description, bullets, metricCard, categoryCard, opportunityCard, impactSteps, outcomes
From src/data/solutions.ts:
- export const solutions: SolutionChallenge[]
From chart primitives (Plan 02):
- ProgressBar({ value, label, tone? })
- GaugeChart({ value, max? })
- Sparkline({ values, tone? })
From IntelligenceIcon:
- IntelligenceIcon({ name, className?, size?, strokeWidth? })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SolutionPanel + SolutionsSection tab island</name>
  <files>src/components/sections/SolutionPanel.tsx, src/components/sections/SolutionsSection.tsx</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Solutions Section anatomy + a11y + copy)
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (Pattern 1 WAI-ARIA tabs automatic activation)
    @src/data/solutions.ts
    @src/components/ui/ProgressBar.tsx
    @src/components/ui/GaugeChart.tsx
    @src/components/ui/Sparkline.tsx
  </read_first>
  <action>
    Create presentational `SolutionPanel({ challenge })` rendering the six bands from UI-SPEC: hero (title Body-Bold/assumed ~24px, description, check bullets via IntelligenceIcon check, optional next/image illustration), metric/gauge card (Metric 40px/600 numeral + GaugeChart + callout), category card (rows with icon/label/ProgressBar/%), opportunity card (positive/accent Metric + Sparkline + rows), How We Create Impact (label + 5 numbered accent circles with dashed connector + icon/title/description), Outcomes You Can Expect (icon list + optional outcomes image). Use tokens; min-w-0; no page overflow.

    Create `'use client'` `SolutionsSection`: state activeId default `solutions[0].id` (revenue-leaks); useId for tab/panel ids; section `id="solutions"` `scroll-mt-[72px]` surface bg; container max-w + px-4 md:px-6 lg:px-8 + py-12 md:py-14 lg:py-16. Header copy verbatim: eyebrow `3. SOLUTIONS`, H2 `Solving Your Most Critical Challenges.`, subhead from UI-SPEC. Tablist role=tablist aria-label Challenge areas; each tab role=tab with aria-selected, aria-controls, roving tabindex, IntelligenceIcon + Caption tabLabel; active uses card-active-border + card-active-bg + accent text/icon (not color alone). Keyboard: ArrowLeft/Right wrap, Home/End — automatic activation (setActiveId + focus). Mobile: horizontal scroll-snap on tabs only. Single tabpanel for active challenge. Do not open DetailModal. Do not import lucide outside IntelligenceIcon. Do not install chart libraries.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SolutionsSection and SolutionPanel export; section id=solutions; 10 role=tab buttons; default Revenue-Leaks selected.
    - Panel uses ProgressBar, GaugeChart, Sparkline; no DetailModal import in SolutionsSection.
    - Eyebrow/H2/subhead match UI-SPEC; tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SolutionsSection tests + page wire</name>
  <files>src/components/sections/SolutionsSection.test.tsx, src/app/page.tsx</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-VALIDATION.md (Wave 0 Solutions tests)
    @src/components/sections/ProcessSection.test.tsx
    @src/components/sections/SolutionsSection.tsx
    @src/app/page.tsx
  </read_first>
  <action>
    Write `SolutionsSection.test.tsx` covering VALIDATION Wave 0: (1) exactly 10 role=tab; (2) default aria-selected on Revenue-Leaks; (3) click Conversion-Problems updates visible panel heading/text; (4) ArrowRight from first tab selects second (aria-selected); (5) section id=solutions present (document.getElementById or container query). Update `page.tsx` to render `<SolutionsSection />` after `<ProcessSection />` inside the fragment; Home remains RSC (no use client on page). Run vitest for SolutionsSection then full suite.
  </action>
  <verify>
    <automated>npx vitest run src/components/sections/SolutionsSection.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SolutionsSection.test.tsx passes all five behaviors.
    - page.tsx contains SolutionsSection after ProcessSection; no use client on page.
    - Full `npx vitest run` stays green; tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client island ↔ static solutions data | No request input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | tab keyboard handlers | mitigate | Only cycle known solutions[] indices; no user HTML |
| T-04-06 | Injection | SolutionPanel copy | mitigate | JSX text escaping; icons via IntelligenceIcon map |
| T-04-SC | Tampering | npm installs | mitigate | No chart packages; no new deps |
</threat_model>

<verification>
- vitest SolutionsSection green; 10 tabs; default revenue-leaks
- id=solutions on homepage after Process
</verification>

<success_criteria>
Accessible Solutions tabs with inline dashboards live on homepage; HOME-06 automated tests pass.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-04-SUMMARY.md` when done.
</output>
