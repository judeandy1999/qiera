---
phase: 04-solutions-leverages
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/types/leverages.ts
  - src/data/leverages.ts
autonomous: true
requirements: [HOME-07]
user_setup: []

must_haves:
  truths:
    - "A typed Leverage contract covers overview bullets plus optional modal sections (valueDrivers vs gapAnalysis/drivers) and required cta"
    - "leverages exports exactly 11 objects in UI-SPEC order starting with revenue"
    - "Share and Download never appear as fields or copy; Expert CTA body lives in cta for /contact footer"
  artifacts:
    - path: "src/types/leverages.ts"
      provides: "Leverage and nested modal types"
      exports: ["Leverage"]
    - path: "src/data/leverages.ts"
      provides: "Eleven typed leverages"
      exports: ["leverages"]
      contains: "Revenue Leverage"
  key_links:
    - from: "src/data/leverages.ts"
      to: "src/types/leverages.ts"
      via: "import type { Leverage }"
      pattern: "Leverage"
---

<objective>
Create the typed Leverages data layer: flexible `Leverage` contract and exactly eleven leverage objects (HOME-07) transcribed from Leverages menu + Leverage cards references, omitting Share/Download (DECISIONS #5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@.planning/DECISIONS.md
@docs/qiera-reference/Homepage/Leverages menu.png
@docs/qiera-reference/Homepage/Leverage cards/
@src/types/solutions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Leverage type contract</name>
  <files>src/types/leverages.ts</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (TypeScript Type Contracts — leverages.ts + Pattern 4 flexible sections)
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Detail modal structure)
  </read_first>
  <action>
    Create `src/types/leverages.ts` exporting `Leverage` matching RESEARCH: id, title, icon, overviewBullets tuple of 3 strings, description, impactMetrics[], optional valueDrivers / valueDriversCallout, optional gapAnalysis, opportunities[], optional opportunitiesFooterLink, timeToValue[], roadmap[], optional roadmapCallout, outcomes[], successFactors[], optional drivers[], required cta `{ title, body }`. Use tone unions for badges/tags (positive|accent|warning|danger). Types only — no runtime. No Share/Download fields. No `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exports Leverage with optional Trust-style and Revenue-style mid-band fields.
    - No Share/Download-related fields; tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Transcribe all 11 leverages</name>
  <files>src/data/leverages.ts</files>
  <read_first>
    @src/types/leverages.ts
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (11 cards table + modal structure)
    @docs/qiera-reference/Homepage/Leverages menu.png
    @docs/qiera-reference/Homepage/Leverage cards/
  </read_first>
  <action>
    Create `src/data/leverages.ts` exporting `const leverages: Leverage[]` with exactly 11 objects in order: revenue, conversion, trust, customer, visibility, positioning, product-offer, retention, operational, decision, governance — titles matching UI-SPEC (Revenue Leverage … Governance Leverage). Transcribe overview bullets from Leverages menu.png; fill modal fields from each Leverage cards PNG. Use optional sections where layouts differ (Trust: gapAnalysis + drivers; Revenue: valueDrivers). Omit opportunitiesFooterLink or leave unused (no destination — do not invent routes). cta.title/body for Talk to an Expert footer. Never include Share or Download strings. Icon keys via IntelligenceIcon names. Mark unreadable `// TODO: unreadable`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - leverages.length === 11; ids match UI-SPEC; first title Revenue Leverage.
    - File has no Share or Download substrings (case-insensitive search should be empty of those chrome labels).
    - Each object has overviewBullets length 3, impactMetrics, opportunities, timeToValue, roadmap, outcomes, successFactors, cta.
    - tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer-transcribed content → typed module | No user input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Injection | leverages.ts strings | mitigate | Plain strings; JSX escapes |
| T-04-08 | Spoofing | CTA href data | mitigate | CTA has title/body only; href hardcoded to /contact in UI plan — no user-supplied URL in data |
| T-04-SC | Tampering | npm installs | mitigate | No packages |
</threat_model>

<verification>
- leverages.length === 11; no Share/Download chrome strings
- tsc clean
</verification>

<success_criteria>
Strict Leverage contract + 11 reference-faithful objects ready for cards and DetailModal body.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-05-SUMMARY.md` when done. List any TODO: unreadable strings.
</output>
