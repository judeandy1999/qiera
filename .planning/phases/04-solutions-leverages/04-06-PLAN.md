---
phase: 04-solutions-leverages
plan: 06
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-04, 04-05]
files_modified:
  - src/components/sections/LeverageCard.tsx
  - src/components/sections/LeverageDetail.tsx
  - src/components/sections/LeveragesSection.tsx
  - src/components/sections/LeveragesSection.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [HOME-07, HOME-09]
user_setup: []

must_haves:
  truths:
    - "Homepage #leverages shows 11 cards with Learn more buttons; desktop prefers 6+5 grid; carousel counter N / 11"
    - "Learn more opens DetailModal with LeverageDetail; Escape closes and restores focus; no Share or Download controls"
    - "DetailModal footer Talk to an Expert links to /contact; Back to Leverages maps to onClose"
  artifacts:
    - path: "src/components/sections/LeverageCard.tsx"
      provides: "Overview leverage card"
      exports: ["LeverageCard"]
    - path: "src/components/sections/LeverageDetail.tsx"
      provides: "Modal body for one Leverage"
      exports: ["LeverageDetail"]
      min_lines: 100
    - path: "src/components/sections/LeveragesSection.tsx"
      provides: "Client leverages grid/carousel + modal orchestration"
      exports: ["LeveragesSection"]
      min_lines: 80
    - path: "src/components/sections/LeveragesSection.test.tsx"
      provides: "HOME-07/HOME-09 Nyquist tests"
    - path: "src/app/page.tsx"
      provides: "Process → Solutions → Leverages"
      contains: "LeveragesSection"
  key_links:
    - from: "src/components/sections/LeveragesSection.tsx"
      to: "src/components/ui/DetailModal.tsx"
      via: "DetailModal open/onClose/labelledById/footer"
      pattern: "DetailModal"
    - from: "src/components/sections/LeverageDetail.tsx"
      to: "src/components/ui/ProgressBar.tsx"
      via: "ProgressBar for valueDrivers when present"
      pattern: "ProgressBar"
    - from: "src/app/page.tsx"
      to: "src/components/sections/LeveragesSection.tsx"
      via: "render after SolutionsSection"
      pattern: "LeveragesSection"
---

<objective>
Build LeverageCard, LeverageDetail, and LeveragesSection (Intelligence-style grid/carousel + DetailModal with footer CTA to /contact), Vitest coverage for modal a11y and Share/Download absence, and wire Leverages after Solutions on the homepage (HOME-07, HOME-09; DECISIONS #4, #5, #13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@.planning/phases/04-solutions-leverages/04-VALIDATION.md
@.planning/DECISIONS.md
@src/data/leverages.ts
@src/components/sections/IntelligenceSection.tsx
@src/components/ui/DetailModal.tsx
@src/components/sections/ProcessSection.test.tsx
@src/app/page.tsx

<interfaces>
From src/types/leverages.ts / src/data/leverages.ts (Plan 05):
- Leverage; export const leverages: Leverage[]
From DetailModal:
- DetailModal({ open, onClose, title, labelledById?, children, footer? })
From IntelligenceSection pattern:
- activeIndex + selected state; prev/next; live region N / TOTAL; dots as buttons — do NOT copy role=tablist onto Leverages dots (use role=group + aria-current)
From chart primitives:
- ProgressBar for valueDrivers rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LeverageCard + LeverageDetail</name>
  <files>src/components/sections/LeverageCard.tsx, src/components/sections/LeverageDetail.tsx</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Card anatomy + Detail modal structure)
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (Pattern 2 + Pattern 4)
    @src/types/leverages.ts
    @src/components/sections/IntelligenceCard.tsx
    @src/components/ui/ProgressBar.tsx
    @src/components/ui/IntelligenceIcon.tsx
  </read_first>
  <action>
    Create `'use client'` `LeverageCard` props: leverage, onLearnMore, optional learnMoreRef. Layout: icon in accent-tint circle + Body-Bold title; three overview bullets; button `Learn more →` with aria-label `Learn more about ${title}`, min 44px hit area, accent color. Border card-border, radius-card. No Share/Download.

    Create `LeverageDetail({ leverage, headingId, onClose })`: data-driven sections — header (icon + h2 id=headingId + description); optional Back to Leverages text button calling onClose; impactMetrics grid (4 cards; positive tone uses --color-positive); mid band renders valueDrivers+ProgressBar OR gapAnalysis table OR drivers score cards when present (omit empty); opportunities list (omit inventing View-all routes — no nav link unless destinations exist); timeToValue 4-stage flow; roadmap 1–5 with duration pills; outcomes + successFactors. Pair severity tags with text labels (not color alone). No Share/Download controls or stubs. No lucide imports outside IntelligenceIcon. No per-id hardcode switch — optional fields drive layout.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - LeverageCard and LeverageDetail export; Learn more is a button with aria-label including title.
    - LeverageDetail h2 uses headingId; omits Share/Download; optional sections omit when undefined.
    - tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LeveragesSection + tests + page wire</name>
  <files>src/components/sections/LeveragesSection.tsx, src/components/sections/LeveragesSection.test.tsx, src/app/page.tsx</files>
  <read_first>
    @src/components/sections/IntelligenceSection.tsx
    @src/components/ui/DetailModal.tsx
    @src/data/leverages.ts
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Leverages header + responsive grid)
    @.planning/phases/04-solutions-leverages/04-VALIDATION.md (Wave 0 Leverages tests)
    @src/app/page.tsx
  </read_first>
  <action>
    Create `'use client'` LeveragesSection: state activeIndex + selected Leverage|null; section id=leverages scroll-mt-[72px] surface; header eyebrow `2. OUR LEVERAGES`, H2 `High-Impact Leverages Across Every Growth Dimension`, subhead from UI-SPEC (DECISIONS #13 plural). Desktop xl: row1 6 cards + row2 5 (justify-center ASSUMED — correct at screenshot); tablet 3-col; mobile 1-col + scroll-snap carousel chrome like Intelligence (prev/next, `N / 11` live region, dots as role=group buttons with aria-current — NOT role=tablist). Learn more opens selected; DetailModal open/onClose/title/labelledById=`leverage-modal-heading`; children LeverageDetail; footer CTA bar: lightbulb IntelligenceIcon + cta copy + Link/anchor Talk to an Expert href=`/contact` (locked discretion — no query param) closing modal on navigate if needed. Map Back to Leverages → onClose only (DECISIONS #4). Omit Share/Download (DECISIONS #5).

    Write LeveragesSection.test.tsx: (1) 11 Learn more about … buttons; (2) open Revenue Leverage dialog; (3) queryByText(/Share|Download/i) absent inside dialog; (4) Escape closes; (5) focus restores to trigger; (6) id=leverages. Update page.tsx to render LeveragesSection after SolutionsSection; Home stays RSC. Run vitest LeveragesSection + full suite.
  </action>
  <verify>
    <automated>npx vitest run src/components/sections/LeveragesSection.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - LeveragesSection.test.tsx passes modal open/close/Escape/focus and no Share/Download.
    - page.tsx order: ProcessSection → SolutionsSection → LeveragesSection; Expert CTA href is /contact.
    - Full vitest green; tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client island ↔ static leverage data | No request input |
| CTA Link → /contact | Hardcoded internal path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing | Talk to an Expert href | mitigate | Hardcode `/contact` only; never bind user-supplied URLs |
| T-04-10 | Tampering | LeverageDetail chrome | mitigate | Explicitly omit Share/Download; tests assert absence |
| T-04-11 | Elevation | DetailModal | accept | Reuse native dialog a11y from Phase 2; integration tests cover Escape/focus |
| T-04-SC | Tampering | npm installs | mitigate | No new packages |
</threat_model>

<verification>
- vitest LeveragesSection green; 11 cards; modal HOME-09 behaviors
- page order Process → Solutions → Leverages
</verification>

<success_criteria>
Leverages overview + accessible DetailModal without Share/Download live on homepage with tests proving HOME-07/HOME-09.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-06-SUMMARY.md` when done.
</output>
