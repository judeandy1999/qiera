---
phase: 04-solutions-leverages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/IntelligenceIcon.tsx
autonomous: true
requirements: [HOME-06, HOME-07]
user_setup: []

must_haves:
  truths:
    - "globals.css @theme declares --color-positive, --color-positive-tint, --color-warning, and --color-danger"
    - "IntelligenceIcon resolves Phase 4 icon keys (eye, funnel, lock, gauge, activity, messageCircle, clock, zap, percent, dollarSign, layers) without throwing"
    - "No new npm packages installed; section files still must not import lucide-react directly"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Positive/warning/danger metric tokens for Solutions + Leverages"
      contains: "--color-positive"
    - path: "src/components/ui/IntelligenceIcon.tsx"
      provides: "Extended lucide map for Solutions tabs and Leverage cards/modals"
      exports: ["IntelligenceIcon"]
      contains: "Eye"
  key_links:
    - from: "src/components/ui/IntelligenceIcon.tsx"
      to: "lucide-react"
      via: "import Eye, Funnel, Lock, Gauge, Activity, MessageCircle, Clock, Zap, Percent, DollarSign, Layers"
      pattern: "messageCircle"
---

<objective>
Add Phase 4 metric color tokens and extend `IntelligenceIcon` with Solutions/Leverage lucide keys so downstream panels and cards can use accent/positive/warning/danger styling and icons without importing lucide in section files (HOME-06, HOME-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@src/app/globals.css
@src/components/ui/IntelligenceIcon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add positive/warning/danger tokens to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    @src/app/globals.css
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Color → New tokens)
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (New color tokens code example)
  </read_first>
  <action>
    Inside the existing `@theme inline` block in `globals.css`, add exactly these tokens (ASSUMED hex from UI-SPEC; screenshot gate may retune later): `--color-positive: #10b981`, `--color-positive-tint: rgba(16, 185, 129, 0.1)`, `--color-warning: #f59e0b`, `--color-danger: #ef4444`. Do not redeclare inherited tokens. Do not add a second `@theme` block. Do not install packages.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains all four new token names inside `@theme inline`.
    - Inherited Phase 1–3 tokens remain unchanged.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend IntelligenceIcon with Phase 4 lucide keys</name>
  <files>src/components/ui/IntelligenceIcon.tsx</files>
  <read_first>
    @src/components/ui/IntelligenceIcon.tsx
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (Icon Extension Contract)
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Icon Extension Contract — lucide only via IntelligenceIcon)
  </read_first>
  <action>
    Import from lucide-react and register these keys on the existing `icons` map: `eye`→Eye, `funnel`→Funnel, `lock`→Lock, `gauge`→Gauge, `activity`→Activity, `messageCircle`→MessageCircle, `clock`→Clock, `zap`→Zap, `percent`→Percent, `dollarSign`→DollarSign, `layers`→Layers. Keep public API `IntelligenceIcon({ name, className?, size?, strokeWidth? })` unchanged. Preserve Circle fallback. Do not remove existing Phase 1–3 keys. Do not install packages. Reuse existing keys elsewhere (`shield`, `conversion`, `visibility`, `sparkles`, `lightbulb`, `check`, `target`, `warning`, etc.) — do not duplicate aliases unless needed for clarity.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - IntelligenceIcon.tsx imports Eye, Funnel, Lock, Gauge, Activity, MessageCircle, Clock, Zap, Percent, DollarSign, Layers.
    - icons map contains keys: eye, funnel, lock, gauge, activity, messageCircle, clock, zap, percent, dollarSign, layers.
    - package.json dependencies unchanged; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer icon map → rendered SVG | name prop indexes a fixed map |
| CSS theme tokens → class/style consumers | compile-time design tokens only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Injection | IntelligenceIcon name prop | mitigate | name only indexes fixed Record; no dangerouslySetInnerHTML |
| T-04-SC | Tampering | npm installs | mitigate | Zero new packages this plan |
</threat_model>

<verification>
- Four color tokens present in globals.css
- Eleven new icon keys resolve via IntelligenceIcon
- tsc clean; no package.json dependency changes
</verification>

<success_criteria>
Phase 4 tokens and icon gateway ready for Solutions/Leverage UI without lucide imports in section files.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-01-SUMMARY.md` when done.
</output>
