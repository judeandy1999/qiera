---
phase: 04-solutions-leverages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ProgressBar.tsx
  - src/components/ui/GaugeChart.tsx
  - src/components/ui/Sparkline.tsx
autonomous: true
requirements: [HOME-06, HOME-07]
user_setup: []

must_haves:
  truths:
    - "ProgressBar renders a CSS-width bar with role=img and an aria-label including label and percent"
    - "GaugeChart renders an SVG semi-circle arc driven by a 0–max value with no chart library"
    - "Sparkline renders an SVG polyline from a number[] with accent or positive stroke"
  artifacts:
    - path: "src/components/ui/ProgressBar.tsx"
      provides: "CSS progress bar primitive"
      exports: ["ProgressBar"]
    - path: "src/components/ui/GaugeChart.tsx"
      provides: "SVG gauge primitive"
      exports: ["GaugeChart"]
    - path: "src/components/ui/Sparkline.tsx"
      provides: "SVG sparkline primitive"
      exports: ["Sparkline"]
  key_links:
    - from: "src/components/ui/ProgressBar.tsx"
      to: "CSS width style"
      via: "style width percent clamped 0–100"
      pattern: "width:"
    - from: "src/components/ui/GaugeChart.tsx"
      to: "SVG path"
      via: "strokeDasharray or arc geometry"
      pattern: "svg"
---

<objective>
Create shared CSS/SVG chart primitives (`ProgressBar`, `GaugeChart`, `Sparkline`) for Solutions panels and Leverage detail bars — zero chart npm libraries (HOME-06, HOME-07; DECISIONS/UI-SPEC lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@src/lib/cn.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProgressBar CSS width primitive</name>
  <files>src/components/ui/ProgressBar.tsx</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (Pattern 3 + ProgressBar code example)
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Charts: CSS width bars only)
  </read_first>
  <action>
    Create presentational `ProgressBar` (RSC-safe, no use client unless needed) with props: `value: number`, `label: string`, optional `tone?: "accent" | "positive"` (default accent). Clamp value to 0–100. Track uses accent-tint background; fill uses `var(--color-accent)` or `var(--color-positive)`. Root has `role="img"` and `aria-label={`${label}: ${value}%`}`. No animation required; if any transition is added, gate behind prefers-reduced-motion. Do not install Chart.js/Recharts/D3 or any chart package.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exports ProgressBar; uses style width percent; role=img with aria-label.
    - tone positive uses --color-positive; no chart library imports.
    - tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build GaugeChart and Sparkline SVG primitives</name>
  <files>src/components/ui/GaugeChart.tsx, src/components/ui/Sparkline.tsx</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (GaugeChart + Sparkline guidance)
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (metric/gauge + opportunity sparkline)
  </read_first>
  <action>
    Create `GaugeChart({ value, max?: number })` — SVG semi-circle track + accent progress arc (pathLength/strokeDasharray or equivalent). Mark decorative SVG `aria-hidden` (numeric value rendered by parent). Create `Sparkline({ values: number[], tone?: "accent" | "positive" })` — SVG polyline normalized to viewBox from values array; stroke via CSS var for tone; empty/single-point arrays render safely without throw. Geometry may be ASSUMED and tuned at screenshot gate. No chart libraries. No lucide imports.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GaugeChart and Sparkline export from their files; both use native SVG only.
    - package.json unchanged (no chart deps).
    - tsc exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| typed numeric props → SVG/CSS | Developer/static data only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | chart deps | mitigate | Forbid npm chart libs; hand-built SVG/CSS only |
| T-04-03 | DoS | ProgressBar value | mitigate | Clamp 0–100 before width style |
| T-04-SC | Tampering | npm installs | mitigate | Zero packages |
</threat_model>

<verification>
- Three UI primitives exist and typecheck
- No recharts/chart.js/d3 in package.json or imports
</verification>

<success_criteria>
Shared chart primitives ready for Solutions and Leverage dashboards without third-party chart libraries.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-02-SUMMARY.md` when done.
</output>
