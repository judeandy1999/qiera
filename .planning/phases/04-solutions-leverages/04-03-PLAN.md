---
phase: 04-solutions-leverages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/solutions.ts
  - src/data/solutions.ts
autonomous: true
requirements: [HOME-06]
user_setup: []

must_haves:
  truths:
    - "A typed SolutionChallenge contract covers hero, metric/gauge, category bars, opportunity/sparkline, 5 impact steps, and outcomes"
    - "solutions exports exactly 10 challenges in UI-SPEC order with default first id revenue-leaks"
    - "Copy is transcribed from Solutions card references; unreadable strings flagged with // TODO: unreadable"
  artifacts:
    - path: "src/types/solutions.ts"
      provides: "SolutionChallenge and nested panel types"
      exports: ["SolutionChallenge", "SolutionBarRow", "SolutionImpactStep"]
    - path: "src/data/solutions.ts"
      provides: "Ten typed solution challenges"
      exports: ["solutions"]
      contains: "revenue-leaks"
  key_links:
    - from: "src/data/solutions.ts"
      to: "src/types/solutions.ts"
      via: "import type { SolutionChallenge }"
      pattern: "SolutionChallenge"
---

<objective>
Create the typed Solutions data layer: `SolutionChallenge` contract and exactly ten challenge objects (HOME-06) transcribed from `docs/qiera-reference/Homepage/Solutions cards/*`, ready for the tabbed SolutionsSection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-solutions-leverages/04-UI-SPEC.md
@.planning/phases/04-solutions-leverages/04-RESEARCH.md
@.planning/REQUIREMENTS.md
@docs/qiera-reference/Homepage/Solutions cards/
@src/types/process.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SolutionChallenge type contract</name>
  <files>src/types/solutions.ts</files>
  <read_first>
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (TypeScript Type Contracts — solutions.ts)
    @src/types/process.ts (typed-data convention)
  </read_first>
  <action>
    Create `src/types/solutions.ts` exporting types matching RESEARCH exactly: `SolutionBarRow`, `SolutionImpactStep`, and `SolutionChallenge` with fields id, tabLabel, icon, title, description, bullets[], optional illustrationSrc/illustrationAlt, metricCard, categoryCard, opportunityCard (including valueTone accent|positive and sparkline number[]), impactSteps (length 5), outcomes[], optional outcomesIllustrationSrc. Types only — no runtime values. No `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exports SolutionChallenge, SolutionBarRow, SolutionImpactStep with all RESEARCH fields.
    - No runtime data; no `any`; tsc exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Transcribe all 10 solutions challenges</name>
  <files>src/data/solutions.ts</files>
  <read_first>
    @src/types/solutions.ts
    @.planning/phases/04-solutions-leverages/04-UI-SPEC.md (Tablist — exactly 10 ids/labels)
    @.planning/phases/04-solutions-leverages/04-RESEARCH.md (Pitfall 1 filename drift; Asset Extraction Plan)
    @docs/qiera-reference/Homepage/Solutions cards/
  </read_first>
  <action>
    Create `src/data/solutions.ts` exporting `const solutions: SolutionChallenge[]` with exactly 10 objects in this order/ids/tabLabels: revenue-leaks / Revenue-Leaks; growth-constraints / Growth-Constraints (source file may be misspelled `growth constrains - solutions.png`); conversion-problems / Conversion-Problems; ai-visibility / AI-Visibility; seo-authority / SEO-Authority; trust-issues / Trust-Issues; retention-problems / Retention-Problems; positioning-problems / Positioning-Problems; competitive-pressure / Competitive-Pressure; operational-bottlenecks / Operational-Bottlenecks. Transcribe panel copy, metrics, bars, opportunity values, 5 How We Create Impact steps, and outcomes from each Solutions card PNG. Set icon keys to IntelligenceIcon names (existing or Phase 4 keys). Optional illustrationSrc paths under `/images/qiera/solutions/…` may be stubbed as comments or paths that Plan 07 will populate — prefer paths for conversion funnel, trust shield, outcomes target when known. Mark unreadable strings `// TODO: unreadable`. Never invent a Solutions DetailModal route or 11th tab.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - solutions.length === 10; first id is revenue-leaks; ids match UI-SPEC exactly.
    - Each object has non-empty title, description, bullets, metricCard, categoryCard.rows, opportunityCard, impactSteps.length === 5, outcomes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| developer-transcribed content → typed module | No user input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Injection | solutions.ts strings | mitigate | Plain strings; JSX consumers escape; no HTML injection |
| T-04-SC | Tampering | npm installs | mitigate | No packages |
</threat_model>

<verification>
- solutions.length === 10; revenue-leaks first
- tsc clean
</verification>

<success_criteria>
Strict SolutionChallenge contract + 10 reference-faithful challenges ready for SolutionsSection tabs/panels.
</success_criteria>

<output>
Create `.planning/phases/04-solutions-leverages/04-03-SUMMARY.md` when done. List any TODO: unreadable strings.
</output>
